<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>切炸弹小游戏</title>
    <url>/2021/08/27/2021-08-27-%E5%88%87%E7%82%B8%E5%BC%B9%E5%B0%8F%E6%B8%B8%E6%88%8F/</url>
    <content><![CDATA[<p>这是一个我自己做的小游戏：</p>
<p>鼠标点击操控三角形，切除或躲避从四周扔来的炸弹，不被炸到即可</p>
<p>游戏共25关，每5关会出现一种新的敌人，默认模式下按住鼠标会有缓时效果，难度不大</p>
<p>鼠标右键打开设置，可以开启release模式，取消缓时（难度较高，欢迎挑战！</p>
<p>欢迎体验，游戏愉快 ^_^</p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/littlegameofcut/1.png" alt="1"></p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/littlegameofcut/2.png" alt="2"></p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/littlegameofcut/4.png" alt="3"></p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/littlegameofcut/3.png" alt="4"></p>
<p><strong>下载链接：<a href="https://pan.baidu.com/s/1sByr24vguePcj3u9CMZ1Hg">https://pan.baidu.com/s/1sByr24vguePcj3u9CMZ1Hg</a></strong> </p>
<p><strong>提取码：</strong>5207</p>
]]></content>
      <categories>
        <category>游戏</category>
      </categories>
      <tags>
        <tag>unity</tag>
        <tag>自制游戏</tag>
      </tags>
  </entry>
  <entry>
    <title>简易对拍器</title>
    <url>/2021/09/23/2021-09-23-%E7%AE%80%E6%98%93%E5%AF%B9%E6%8B%8D%E5%99%A8/</url>
    <content><![CDATA[<h4 id="Github项目地址"><a href="#Github项目地址" class="headerlink" title="Github项目地址"></a><a href="https://github.com/lxtyin/SimpleComparator">Github项目地址</a></h4><h4 id="用法："><a href="#用法：" class="headerlink" title="用法："></a>用法：</h4><p>需要装有MinGW并且添加到环境变量</p>
<ol>
<li>将两份程序（一般是你的不知道哪错了的程序和标答）扔进program1.cpp和program2.cpp</li>
<li>在constructor.cpp里写随机输入数据生成器，对应你的这道题目</li>
<li>双击run.bat运行，输入希望对拍的次数，等待对拍结果</li>
</ol>
<p>对拍结果完全一样显示 <strong>[Accepted]</strong></p>
<p>不同则会显示 <strong>[Wrong Answer]</strong></p>
<p>有不同时，会同时显示使两份程序结果不一样的数据，你就可以复制拿去debug了</p>
<p>同时所有结果都会保存在result.txt里，关掉控制台后仍然可以查看</p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/simplecomparator/1.png" alt="1"></p>
<h4 id="原理："><a href="#原理：" class="headerlink" title="原理："></a>原理：</h4><p>原理其实没啥好说的（（</p>
<p>先调用随机数生成器构造输入数据data.txt，再分别扔进两份代码跑</p>
<p>用另一个脚本比较输出内容，返回结果即可。</p>
<p>ps：一开始是用纯c语言和c++的文件读写搞的，但奈何我太菜搞不出来</p>
<p>然后学习了队友教的.bat大法，很快就搞好了。</p>
<h4 id="BUG："><a href="#BUG：" class="headerlink" title="BUG："></a>BUG：</h4><p>尝试了makefile不知道为啥不管用，而且用makefile对mingw还有特殊要求，<del>所以直接给删了</del></p>
<p>坏处就是每次运行要先编译个几秒，<del>问题不大</del></p>
]]></content>
      <categories>
        <category>奇怪的东西</category>
      </categories>
      <tags>
        <tag>工具</tag>
        <tag>小项目</tag>
      </tags>
  </entry>
  <entry>
    <title>初来乍到</title>
    <url>/2022/03/25/2021-10-01-%E5%88%9D%E6%9D%A5%E4%B9%8D%E5%88%B0/</url>
    <content><![CDATA[<p>2021.10.1</p>
<p>你好！</p>
<p><del>搞了几天这个github博客终于能用了</del></p>
<p>我以前的博客在<a href="https://www.cnblogs.com/tyin/">这里</a></p>
<p>低质量内容就不搬过来了（（</p>
<h3 id="x3D-x3D-gt-关于我"><a href="#x3D-x3D-gt-关于我" class="headerlink" title="&#x3D;&#x3D;&gt; 关于我"></a><a href="https://lxtyin.github.io/about/">&#x3D;&#x3D;&gt; 关于我</a></h3><hr>
<p>2022.3.25</p>
<p>又迁移了一波（</p>
<p>这次是hexo了</p>
]]></content>
      <categories>
        <category>介绍</category>
      </categories>
      <tags>
        <tag>介绍</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 746 div.2 CDE</title>
    <url>/2021/11/17/2021-10-07-Codeforces%20Round%20746%20div.2/</url>
    <content><![CDATA[<p>这场没打，赛后看了一下题值得补</p>
<h4 id="C-Bakry-and-Partitioning"><a href="#C-Bakry-and-Partitioning" class="headerlink" title="C. Bakry and Partitioning"></a>C. Bakry and Partitioning</h4><p>题意：给出一棵 $n$ 节点的树，每个点有权值 $a_i$，问能否割去至少一条，至多 $k-1$ 条边，使得割完后各个连通块的权值异或和相等。</p>
<p>显然，如果整棵树的异或和为0，那么任选一条边割得到的两个连通块异或和都相等。（异或的性质）</p>
<p>如果整棵树异或和为 $x$ ，那么问题就等价于能不能把树拆出三个异或和为 $x$ 的连通块（三个可以合并成一个，所以能拆成更多块的也一定能拆成三块）</p>
<p>dfs地去找，当某个子树的异或和为 $x$ 时，记录并将这个子树清零，跑完之后看找到了几个即可</p>
<p>记得对 $k&#x3D;2$ 特判</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, k;</span><br><span class="line"><span class="type">int</span> a[maxn], xs[maxn];</span><br><span class="line"><span class="type">int</span> xsum, ans;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> t, nt;</span><br><span class="line">&#125;e[maxn &lt;&lt; <span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> head[maxn], cnt = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span></span>&#123;</span><br><span class="line">    e[++cnt].t = y;</span><br><span class="line">    e[cnt].nt = head[x];</span><br><span class="line">    head[x] = cnt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> fa)</span></span>&#123;</span><br><span class="line">    xs[p] = a[p];</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=head[p];i;i=e[i].nt)&#123;</span><br><span class="line">        <span class="type">int</span> v = e[i].t;</span><br><span class="line">        <span class="keyword">if</span>(v != fa)&#123;</span><br><span class="line">            <span class="built_in">dfs</span>(v, p);</span><br><span class="line">            xs[p] ^= xs[v];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(xs[p] == xsum)&#123;</span><br><span class="line">        ans++;</span><br><span class="line">        xs[p] = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; k;</span><br><span class="line">    xsum = cnt = ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) head[i] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; a[i];</span><br><span class="line">        xsum ^= a[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y;</span><br><span class="line">        <span class="built_in">add</span>(x, y);</span><br><span class="line">        <span class="built_in">add</span>(y, x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(xsum == <span class="number">0</span>)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(k &lt;= <span class="number">2</span>)&#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">dfs</span>(<span class="number">1</span>, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span>(ans &gt; <span class="number">2</span>) cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">        <span class="keyword">else</span> cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D-Hemose-in-ICPC"><a href="#D-Hemose-in-ICPC" class="headerlink" title="D. Hemose in ICPC ?"></a>D. Hemose in ICPC ?</h4><p>题意：交互题，现有一棵 $n$ 节点的树，每个边有边权（边权未知），可以进行至多12次询问：每次询问一个点集，回复是这个点集中 $Dist(a,b)$ 的最大值，$Dist(a,b)$ 定义为 $a$ 到 $b$ 路径上所有边权的gcd，求使 $Dist(a,b)$ 最大的 $a,b$</p>
<p>首先可以看出要找的就是权值最大的边，gcd是纯唬人的。</p>
<p>第一次查询先查询所有的点，这样就可以知道最大的边权是多少。</p>
<p>查询次数为12，数据规模 $n&lt;1000$，如果我们能以某种方式把树映射到序列上，且序列上连续的点在树上也连续，就可以二分地询问，找最大边的位置（因为连续，所以序列上两点之间就是边）。</p>
<p>欧拉序即可保证连续性。</p>
<p>ps：一开始用dfs序糊里糊涂过了，不能保证连续性但也能通过某种玄学的巧合AC，正确性不是很显然，我也讲不清楚，就不介绍了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> fr, t, nt;</span><br><span class="line">&#125;e[maxn &lt;&lt; <span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> head[maxn], cnt = <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> vis[maxn], vp = <span class="number">0</span>;</span><br><span class="line"><span class="type">int</span> a[maxn &lt;&lt; <span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span></span>&#123;</span><br><span class="line">    e[++cnt].t = y;</span><br><span class="line">    e[cnt].fr = x;</span><br><span class="line">    e[cnt].nt = head[x];</span><br><span class="line">    head[x] = cnt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> fa)</span></span>&#123;</span><br><span class="line">    a[++cnt] = p;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=head[p];i;i=e[i].nt)&#123;</span><br><span class="line">        <span class="type">int</span> v = e[i].t;</span><br><span class="line">        <span class="keyword">if</span>(v != fa)&#123;</span><br><span class="line">            <span class="built_in">dfs</span>(v, p);</span><br><span class="line">            a[++cnt] = p;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">getans</span><span class="params">(<span class="type">int</span> l, <span class="type">int</span> r)</span></span>&#123;</span><br><span class="line">    vp++;</span><br><span class="line">    <span class="type">int</span> k = <span class="number">0</span>, res;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=l;i&lt;=r;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(vis[a[i]] != vp) vis[a[i]] = vp, k++;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;? &quot;</span> &lt;&lt; k;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(vis[i] == vp) cout &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; i;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; endl;</span><br><span class="line">    <span class="built_in">fflush</span>(stdout);</span><br><span class="line"></span><br><span class="line">    cin &gt;&gt; res;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    cnt = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y;</span><br><span class="line">        <span class="built_in">add</span>(x, y);</span><br><span class="line">        <span class="built_in">add</span>(y, x);</span><br><span class="line">    &#125;</span><br><span class="line">    cnt = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>, <span class="number">-1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> mx;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;? &quot;</span> &lt;&lt; n &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++) cout &lt;&lt; i &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; endl;</span><br><span class="line">    <span class="built_in">fflush</span>(stdout);</span><br><span class="line">    cin &gt;&gt; mx;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> l = <span class="number">1</span>, r = cnt, mid;</span><br><span class="line">    <span class="keyword">while</span>(l + <span class="number">1</span> &lt; r)&#123;</span><br><span class="line">        mid = (l + r) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">getans</span>(l, mid) == mx)&#123;</span><br><span class="line">            r = mid;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            l = mid;<span class="comment">//二分部分小改一下 因为要找的是两点间的连边</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;! &quot;</span> &lt;&lt; a[l] &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; a[r] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="E-Bored-Bakry"><a href="#E-Bored-Bakry" class="headerlink" title="E. Bored Bakry"></a>E. Bored Bakry</h4><p>题意：给一个长度为 $n$ 的序列 $a$，找到最长的子区间 $a_l..a_r$ 使得子区间所有元素的与运算和大于异或和</p>
<p>对每一位考虑，在第 $k$ 位上，与和为1的条件是区间所有数第 $k$ 位全是1；异或和为1的条件是区间中第 $k$ 位为1的数量是奇数个</p>
<p>那么可以注意到如果区间长度为奇数，任一位上与和为1的时候异或和也一定为1，与和不可能大于异或和</p>
<p>现在考虑，对于特定一个区间，如何判断它是否满足条件：</p>
<ul>
<li>首先它的长度必须为偶数</li>
<li>从高位到低位枚举，如果它在第 $k$ 位时第一次满足了<strong>区间第 $k$ 位全为1</strong>，那么比 $k$ 更高的位上1数量必须都为偶数</li>
</ul>
<p>可以维护一个前缀异或和（记为 $xsum$），从高位到低位枚举到第 $k$ 位时，$xsum(i)$表示仅考虑前 $k-1$ 位值时，前 $i$ 位的异或和</p>
<p>第二个条件其实就相当于 $xsum(r) &#x3D; xsum(l-1)$，因为前面这些位上的1都是偶数个</p>
<p>那么做法就很显然了：从高位到低位枚举，每一层中对于每一个1的位置 $r$，去找最小的满足条件的 $l$，同时维护 $xsum$ 即可</p>
<p>关于找最小的 $l$ ，记录同一段连续1内每一个 $xsum$ 值最早出现的位置即可（详见代码）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">gtw</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> w)</span></span>&#123; <span class="keyword">return</span> (x &gt;&gt; (w<span class="number">-1</span>)) &amp; <span class="number">1</span>;&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> n, a[maxn], p[maxn];</span><br><span class="line"><span class="type">int</span> xsum[maxn], pos[maxn * <span class="number">2</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; a[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> k=<span class="number">21</span>;k&gt;=<span class="number">1</span>;k--)&#123;</span><br><span class="line">        <span class="type">int</span> l = <span class="number">1</span>, r;</span><br><span class="line">        <span class="keyword">while</span>(l &lt; n)&#123;</span><br><span class="line">            <span class="keyword">while</span>(l &lt;= n &amp;&amp; <span class="built_in">gtw</span>(a[l], k) == <span class="number">0</span>) l++;</span><br><span class="line">            <span class="keyword">if</span>(l &gt; n) <span class="keyword">break</span>;</span><br><span class="line">            r = l + <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">while</span>(r &lt;= n &amp;&amp; <span class="built_in">gtw</span>(a[r], k) == <span class="number">1</span>) r++;</span><br><span class="line">            r--;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=l<span class="number">-1</span>;j&lt;=r;j+=<span class="number">2</span>) pos[xsum[j]] = <span class="number">-1</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=l<span class="number">-1</span>;j&lt;=r;j+=<span class="number">2</span>)&#123;</span><br><span class="line">                <span class="keyword">if</span>(pos[xsum[j]] == <span class="number">-1</span>) pos[xsum[j]] = j;</span><br><span class="line">                <span class="keyword">else</span> ans = <span class="built_in">max</span>(ans, j - pos[xsum[j]]);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=l;j&lt;=r;j+=<span class="number">2</span>) pos[xsum[j]] = <span class="number">-1</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=l;j&lt;=r;j+=<span class="number">2</span>)&#123;</span><br><span class="line">                <span class="keyword">if</span>(pos[xsum[j]] == <span class="number">-1</span>) pos[xsum[j]] = j;</span><br><span class="line">                <span class="keyword">else</span> ans = <span class="built_in">max</span>(ans, j - pos[xsum[j]]);</span><br><span class="line">            &#125;</span><br><span class="line">            l = r + <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">            p[i] += a[i] &amp; (<span class="number">1</span> &lt;&lt; (k<span class="number">-1</span>));</span><br><span class="line">            xsum[i] = xsum[i<span class="number">-1</span>] ^ p[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>不稳定扫雷 - LudumDare49</title>
    <url>/2021/10/03/2021-10-03-%E4%B8%8D%E7%A8%B3%E5%AE%9A%E6%89%AB%E9%9B%B7/</url>
    <content><![CDATA[<h4 id="Unstable-Sweeping"><a href="#Unstable-Sweeping" class="headerlink" title="Unstable-Sweeping"></a>Unstable-Sweeping</h4><p>It’s a crazy game different from the original minesweeping:</p>
<ul>
<li>Each mine has a countdown! When a grid around the mine is opened, the countdown will begin.</li>
<li>The numbers no longer show the number of mines, but the minimum time left to count down in the surrounding circle of mines.</li>
<li>The mine will explode at the end of the countdown, then destroy the land within 3x3.</li>
<li>The mine explosion will detonate other mines around at the same time!</li>
<li>Left click to open a plot (point to the mine will explode directly!), right click to delete the mine in a plot (wrong deletion will accelerate the countdown of the surrounding mines!)</li>
</ul>
<p>Remove as many mines as possible, protect more land plots and get higher scores.</p>
<p>It may be difficult. Relax. It’s better to enjoy the explosion quietly…</p>
<p>Well, have a good time!</p>
<p>这是一个疯狂的游戏，他在原版扫雷的基础上加上了——</p>
<ul>
<li>每个地雷都有一个倒计时，当地雷周围一圈有格子被打开时，就会开始倒计时——</li>
<li>数字显示的不再是地雷数量，而是周围一圈雷中倒计时剩下的最少时间——</li>
<li>地雷倒计时结束就会爆炸，不过它不会直接导致你Gameover，而是摧毁3x3范围内的地块——</li>
<li>地雷爆炸同时会引爆周围的其他地雷——</li>
<li>左键点开一个地块（点到地雷会直接爆炸！），右键删去一个地块的地雷（删错会导致周围的地雷倒计时加速！）</li>
</ul>
<p>尽可能地切除更多地雷，保护更多地块，获得更高的评分——</p>
<p>可能会很难，放轻松，不如静静地欣赏爆炸——</p>
<p>那么，游戏愉快！</p>
<p><img src="https://raw.githubusercontent.com/lxtyin/Unstable-Sweeping/main/screenshot/QQ%E6%88%AA%E5%9B%BE20211003232438.png" alt="1"></p>
<p><img src="https://raw.githubusercontent.com/lxtyin/Unstable-Sweeping/main/screenshot/QQ%E6%88%AA%E5%9B%BE20211003232528.png" alt="2"></p>
<p><img src="https://raw.githubusercontent.com/lxtyin/Unstable-Sweeping/main/screenshot/QQ%E6%88%AA%E5%9B%BE20211003233057.png" alt="3"></p>
<p>这已经是我最好的成绩了（在Easy难度下</p>
<p>&#x3D;》<a href="https://lx-tyin.itch.io/unstable-sweeping">下载链接(windows)</a></p>
<p>&#x3D;》<a href="https://github.com/lxtyin/lxtyin.github.io">Github源码</a></p>
<p>&#x3D;》<a href="https://ldjam.com/events/ludum-dare/49/unstable-sweeping">参赛界面</a>（多多支持！感谢！</p>
]]></content>
      <categories>
        <category>游戏</category>
      </categories>
      <tags>
        <tag>unity</tag>
        <tag>自制游戏</tag>
        <tag>LudumDare</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 748 div.3 A-G</title>
    <url>/2021/10/13/2021-10-13-Codeforces%20Round%20748%20div.3/</url>
    <content><![CDATA[<h4 id="A-Elections"><a href="#A-Elections" class="headerlink" title="A. Elections"></a>A. Elections</h4><p>题意：有三个数ABC，问他们分别需要加多少才能成为三个数中最大的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> a, b, c;</span><br><span class="line">    cin &gt;&gt; a &gt;&gt; b &gt;&gt; c;</span><br><span class="line">    <span class="type">int</span> mx = <span class="built_in">max</span>(a, <span class="built_in">max</span>(b, c));</span><br><span class="line">    <span class="type">int</span> mx2 = a+b+c - mx - <span class="built_in">min</span>(a, <span class="built_in">min</span>(b, c));</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span>(a == mx &amp;&amp; mx2 != mx) cout &lt;&lt; <span class="string">&quot;0 &quot;</span>;</span><br><span class="line">    <span class="keyword">else</span> cout &lt;&lt; mx+<span class="number">1</span>-a &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    <span class="keyword">if</span>(b == mx &amp;&amp; mx2 != mx) cout &lt;&lt; <span class="string">&quot;0 &quot;</span>;</span><br><span class="line">    <span class="keyword">else</span> cout &lt;&lt; mx+<span class="number">1</span>-b &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">if</span>(c == mx &amp;&amp; mx2 != mx) cout &lt;&lt; <span class="string">&quot;0\n&quot;</span>;</span><br><span class="line">    <span class="keyword">else</span> cout &lt;&lt; mx+<span class="number">1</span>-c &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="B-Make-it-Divisible-by-25"><a href="#B-Make-it-Divisible-by-25" class="headerlink" title="B. Make it Divisible by 25"></a>B. Make it Divisible by 25</h4><p>题意：给定一个数字，问至少从中删除几位数，可以使他能被25整除</p>
<p>只要最后两位是00,25,50,75即可，那么从低位到高位找，分别找这几种第一次出现的位置，取最小即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[maxn], ans;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">getnum</span><span class="params">(<span class="type">int</span> a1, <span class="type">int</span> a2)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;<span class="number">20</span>;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i] == a2)&#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=i+<span class="number">1</span>;j&lt;<span class="number">20</span>;j++)&#123;</span><br><span class="line">                <span class="keyword">if</span>(a[j] == a1)&#123;</span><br><span class="line">                    ans = <span class="built_in">min</span>(ans, j - <span class="number">2</span>);</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ll n;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    ans = <span class="number">1e9</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">20</span>;i++)&#123;</span><br><span class="line">        a[i] = n%<span class="number">10</span>;</span><br><span class="line">        n /= <span class="number">10</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">getnum</span>(<span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">getnum</span>(<span class="number">5</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">getnum</span>(<span class="number">2</span>, <span class="number">5</span>);</span><br><span class="line">    <span class="built_in">getnum</span>(<span class="number">7</span>, <span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="C-Save-More-Mice"><a href="#C-Save-More-Mice" class="headerlink" title="C. Save More Mice"></a>C. Save More Mice</h4><p>题意：X轴上有若干个老鼠，每个老鼠的位置都是整数，$(n,0)$ 处有一个洞，$(0,0)$ 中有一只猫，每回合可以选择一只老鼠向右移动一格，抵达洞即安全，每回合猫也向右移动一格，吃掉该格上的老鼠。问最多可以让多少老鼠存活</p>
<p>因为老鼠和猫的移动速度相同，任何一只老鼠都不可能和猫拉开距离，所以每次都选择离洞最近的老鼠让它进洞，答案一定最大。模拟即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ll n, k;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; k;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=k;i++)&#123;</span><br><span class="line">        cin &gt;&gt; a[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">sort</span>(a+<span class="number">1</span>, a+k+<span class="number">1</span>);</span><br><span class="line">    ll ans = <span class="number">0</span>, p = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=k;i&gt;=<span class="number">1</span>;i--)&#123;</span><br><span class="line">        <span class="keyword">if</span>(p &gt;= a[i]) <span class="keyword">break</span>;</span><br><span class="line">        p += n - a[i];</span><br><span class="line">        ans++;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D1-All-are-Same"><a href="#D1-All-are-Same" class="headerlink" title="D1. All are Same"></a>D1. All are Same</h4><p>题意：给一个序列，要找一个最大的 $k$，使得所有数都可以减去若干个 $k$ 后变成相同的数字</p>
<p>找到最小的数 $mi$，然后计算每个数和 $mi$ 的差的 $gcd$ 和即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> st[maxn];</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> t, nt, tp;</span><br><span class="line">&#125;e[maxn &lt;&lt; <span class="number">1</span>];</span><br><span class="line"><span class="type">int</span> head[maxn], ecnt = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y, <span class="type">int</span> tp)</span></span>&#123;</span><br><span class="line">    e[++ecnt].t = y;</span><br><span class="line">    e[ecnt].tp = tp;</span><br><span class="line">    e[ecnt].nt = head[x];</span><br><span class="line">    head[x] = ecnt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> num[<span class="number">2</span>];</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=head[p];i;i=e[i].nt)&#123;</span><br><span class="line">        <span class="type">int</span> v = e[i].t;</span><br><span class="line">        <span class="keyword">if</span>(st[v] == <span class="number">-1</span>)&#123;</span><br><span class="line">            st[v] = st[p] ^ e[i].tp;</span><br><span class="line">            num[st[v]]++;</span><br><span class="line">            <span class="keyword">if</span>(!<span class="built_in">dfs</span>(v)) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">if</span>(st[v] != (st[p] ^ e[i].tp)) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> n, m;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line"></span><br><span class="line">    ecnt = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        head[i] = <span class="number">0</span>;</span><br><span class="line">        st[i] = <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y; string s;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y &gt;&gt; s;</span><br><span class="line">        <span class="type">int</span> tp = (s[<span class="number">0</span>] == <span class="string">&#x27;i&#x27;</span>);</span><br><span class="line">        <span class="built_in">add</span>(x, y, tp);</span><br><span class="line">        <span class="built_in">add</span>(y, x, tp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(st[i] == <span class="number">-1</span>)&#123;</span><br><span class="line">            st[i] = <span class="number">1</span>;</span><br><span class="line">            num[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">            num[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">if</span>(!<span class="built_in">dfs</span>(i))&#123;</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            ans += <span class="built_in">max</span>(num[<span class="number">0</span>], num[<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D2-Half-of-Same"><a href="#D2-Half-of-Same" class="headerlink" title="D2. Half of Same"></a>D2. Half of Same</h4><p>题意：和D1一样，区别在于只需要有一半数减去若干个 $k$ 后变成相同的数字</p>
<p>$n\le40, -10^6\le a_i \le 10^6$</p>
<p>先考虑，如果有一个 $k$，我们怎么判断它是否合法：</p>
<p>可以让每个数都减 $k$ 减到<strong>第一次小于 $mi$ 为止</strong>，然后数一下有多少数是相同的</p>
<p>这个Judge过程是 $O(n)$ 的</p>
<p>那么有多少个 $k$ 需要judge呢？</p>
<p>可以从1到2e6全部枚举一遍，$O(2e6\times n)$ 看似可行，然而被卡常了</p>
<p>想到这个 $k$ 一定是某两个数的差的约数，又因为 $n$ 很小，可以非常暴力地枚举任意两个数，再枚举它们差的约数，总复杂度 $O(n^2\sqrt{2\times10^6}\times n)$</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">50</span>];</span><br><span class="line"><span class="type">int</span> mia, mxa;</span><br><span class="line"><span class="type">int</span> num[<span class="number">5000009</span>];</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">judge</span><span class="params">(<span class="type">int</span> k)</span></span>&#123;</span><br><span class="line">    <span class="type">bool</span> re = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> t = a[i] - (a[i]-mia+k<span class="number">-1</span>)/k*k;</span><br><span class="line">        num[t]++;</span><br><span class="line">        <span class="keyword">if</span>(num[t] &gt;= n/<span class="number">2</span>) re = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> t = a[i] - (a[i]-mia+k<span class="number">-1</span>)/k*k;</span><br><span class="line">        num[t]--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> re;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    mia = <span class="number">2e8</span>, mxa = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; a[i];</span><br><span class="line">        a[i] += <span class="number">4000000</span>;</span><br><span class="line">        mia = <span class="built_in">min</span>(mia, a[i]);</span><br><span class="line">        mxa = <span class="built_in">max</span>(mxa, a[i]);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">sort</span>(a+<span class="number">1</span>, a+n+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=i;j&lt;=n;j++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(a[j] != a[i])&#123;</span><br><span class="line">                i = j - <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(j - i + <span class="number">1</span> &gt;= n/<span class="number">2</span>)&#123;</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;j++)&#123;</span><br><span class="line">            <span class="type">int</span> d = <span class="built_in">abs</span>(a[j] - a[i]);</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> k=<span class="number">1</span>;k&lt;=<span class="built_in">sqrt</span>(d);k++)&#123;</span><br><span class="line">                <span class="keyword">if</span>(d % k != <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">judge</span>(k)) ans = <span class="built_in">max</span>(ans, k);</span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">judge</span>(d/k)) ans = <span class="built_in">max</span>(ans, d/k);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="E-Gardener-and-Tree"><a href="#E-Gardener-and-Tree" class="headerlink" title="E. Gardener and Tree"></a>E. Gardener and Tree</h4><p>题意：有一棵树，每次操作可以删除所有的叶子节点（度数为1的节点），问几次操作后树为空</p>
<p>很明显拓扑删一遍即可，队友说也可以换根dp，改天了解一下（</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="type">int</span>&gt; vp[maxn];</span><br><span class="line"><span class="type">int</span> du[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> n, k;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; k;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) vp[i].<span class="built_in">clear</span>(), du[i] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y;</span><br><span class="line">        vp[x].<span class="built_in">push_back</span>(y);</span><br><span class="line">        vp[y].<span class="built_in">push_back</span>(x);</span><br><span class="line">        du[x]++;</span><br><span class="line">        du[y]++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(n == <span class="number">1</span>)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;0\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(du[i] == <span class="number">1</span>)&#123;</span><br><span class="line">            q.<span class="built_in">push</span>(i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=k;i++)&#123;</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; tmp;</span><br><span class="line">        <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())&#123;</span><br><span class="line">            <span class="type">int</span> u = q.<span class="built_in">front</span>();</span><br><span class="line">            q.<span class="built_in">pop</span>();</span><br><span class="line">            du[u]--;</span><br><span class="line">            n--;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">auto</span> &amp;j:vp[u])&#123;</span><br><span class="line">                <span class="keyword">if</span>(du[j] &gt; <span class="number">0</span>)&#123;</span><br><span class="line">                    du[j]--;</span><br><span class="line">                    <span class="keyword">if</span>(du[j] == <span class="number">1</span>) tmp.<span class="built_in">push_back</span>(j);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">auto</span> &amp;x:tmp) q.<span class="built_in">push</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="F-Red-Black-Number"><a href="#F-Red-Black-Number" class="headerlink" title="F. Red-Black Number"></a>F. Red-Black Number</h4><p>题意：<del>红黑数</del> 给一个可能含前导0的数字，要求对将每一位染色成红或者黑，在满足红色数字拼起来后可以被 $A$ 整除，黑色数字拼起来可以被 $B$ 整除的条件下，涂红和涂黑的数量差最小，输出涂色方案。</p>
<p>乍一看有点不好下手，一看数据范围 $n\le40,A,B\le40$，考虑暴力dp</p>
<p>设 $dp[i][r][m_1][m_2]$ 表示前 $i$ 位中，涂了 $r$ 个红色，红色部分模 $A$ 结果为 $m_1$，黑色部分模 $B$ 结果为 $m_2$ 时，选择的红色位置（状态压缩，保存方案）</p>
<p>这样设置状态的原因在于，在后方添加一个新数位时（比如涂成红色），$m_1$ 可以转移到 $(m_1\times10+a[i+1])\mod A$，转移很方便，模结果为0即为合法答案</p>
<p>想到了状态后，转移不难写</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"> <span class="type">int</span> n, mda, mdb;</span><br><span class="line">string a;</span><br><span class="line">ll dp[<span class="number">50</span>][<span class="number">50</span>][<span class="number">50</span>][<span class="number">50</span>];<span class="comment">//pos, rednum, redres, blkres = respos</span></span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; mda &gt;&gt; mdb;</span><br><span class="line">    cin &gt;&gt; a; a = <span class="string">&quot;s&quot;</span> + a;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">memset</span>(dp, <span class="number">-1</span>, <span class="built_in">sizeof</span>(dp));</span><br><span class="line">    dp[<span class="number">0</span>][<span class="number">0</span>][<span class="number">0</span>][<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> r = <span class="number">0</span>; r &lt;= i; r++)&#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> rs = <span class="number">0</span>; rs &lt; mda; rs++)&#123;</span><br><span class="line">                <span class="keyword">for</span>(<span class="type">int</span> rb = <span class="number">0</span>; rb &lt; mdb; rb++)&#123;</span><br><span class="line">                    ll t = dp[i][r][rs][rb];</span><br><span class="line">                    <span class="keyword">if</span>(t &gt;= <span class="number">0</span>)&#123;</span><br><span class="line">                        dp[i+<span class="number">1</span>][r][rs][(rb * <span class="number">10</span> + a[i+<span class="number">1</span>] - <span class="string">&#x27;0&#x27;</span>)%mdb] = t;</span><br><span class="line">                        dp[i+<span class="number">1</span>][r+<span class="number">1</span>][(rs * <span class="number">10</span> + a[i+<span class="number">1</span>] - <span class="string">&#x27;0&#x27;</span>)%mda][rb] = t | (<span class="number">1ll</span> &lt;&lt; i);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> ans = <span class="number">1e9</span>;</span><br><span class="line">    ll redp;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> r = n<span class="number">-1</span>; r &gt;= <span class="number">1</span>; r--)&#123;</span><br><span class="line">        <span class="keyword">if</span>(dp[n][r][<span class="number">0</span>][<span class="number">0</span>] &gt;= <span class="number">0</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(ans &gt; <span class="built_in">abs</span>(<span class="number">2</span> * r - n))&#123;</span><br><span class="line">                ans = <span class="built_in">abs</span>(<span class="number">2</span> * r - n);</span><br><span class="line">                redp = dp[n][r][<span class="number">0</span>][<span class="number">0</span>];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(ans &gt;= n) cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(redp &amp; (<span class="number">1ll</span> &lt;&lt; (i<span class="number">-1</span>))) cout &lt;&lt; <span class="string">&quot;R&quot;</span>;</span><br><span class="line">            <span class="keyword">else</span> cout &lt;&lt; <span class="string">&quot;B&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cout &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="G-Changing-Brackets"><a href="#G-Changing-Brackets" class="headerlink" title="G. Changing Brackets"></a>G. Changing Brackets</h4><p>题意：有一个括号序列，包含 ( ) [ ] 四种括号，可以免费将括号翻转，或花费1块把圆括号变方括号（或者反过来），有多个询问，每次询问 $[l,r]$ ，需要最少花费多少使这个区间的括号合法，保证 $[l,r]$ 长度为偶数</p>
<p>一道纯思维题，考虑怎么样的一个括号序列才合法：我们不断将序列中相邻的同类括号删掉，两边的拼在一起，不断这样删去，最后可能全删了（即这个括号序合法），或者剩下形如 ( [ ( [ 这样交替出现的括号，剩下的长度的一半就是最小的花费</p>
<p>一个很容易理解（但可能比较难想到）的性质：圆括号我们直接不管了，剩下的方括号要么全在奇数位，要么全在偶数位</p>
<p>而这个序列中被删掉的那些方括号，也一定是一奇一偶一起删的</p>
<p>所以区间 $[l,r]$ 的答案就是区间中 奇数位方括号数量 - 偶数位方括号数量 再取个绝对值。前缀和搞一下就好了</p>
<p>感觉很不错的一个题，思维略有难度，但正解很容易让人接受</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> sum[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    string s;</span><br><span class="line">    cin &gt;&gt; s;</span><br><span class="line">    <span class="type">int</span> n = s.<span class="built_in">size</span>();</span><br><span class="line">    s = <span class="string">&quot;0&quot;</span> + s;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        sum[i] = sum[i<span class="number">-1</span>];</span><br><span class="line">        <span class="keyword">if</span>(s[i] == <span class="string">&#x27;[&#x27;</span> || s[i] == <span class="string">&#x27;]&#x27;</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(i%<span class="number">2</span> == <span class="number">1</span>) sum[i]++;</span><br><span class="line">            <span class="keyword">else</span> sum[i]--;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> q;</span><br><span class="line">    cin &gt;&gt; q;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=q;i++)&#123;</span><br><span class="line">        <span class="type">int</span> l, r;</span><br><span class="line">        cin &gt;&gt; l &gt;&gt; r;</span><br><span class="line">        cout &lt;&lt; <span class="built_in">abs</span>(sum[r] - sum[l<span class="number">-1</span>]) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 747 div.2 A-E</title>
    <url>/2021/10/09/2021-10-09-Codeforces%20Round%20747%20div.2/</url>
    <content><![CDATA[<h4 id="A-Consecutive-Sum-Riddle"><a href="#A-Consecutive-Sum-Riddle" class="headerlink" title="A. Consecutive Sum Riddle"></a>A. Consecutive Sum Riddle</h4><p>题意：给定一个 $n$，要找出 $l,r$ 使得 $\sum_{i&#x3D;l}^{r}i&#x3D;n$ </p>
<p>解：显然，从 $-n+1$ 加到 $n$ 即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ll n;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    cout &lt;&lt; -n+<span class="number">1</span> &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; n &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="B-Special-Numbers"><a href="#B-Special-Numbers" class="headerlink" title="B. Special Numbers"></a>B. Special Numbers</h4><p>题意：找第 $k$ 个 $n$ 好数，$n$ 好数就是可以被 $n$ 的不同次幂组成，比如 $n^0+n^2…$ 的方式表示</p>
<p>解：把 $k$ 转化为二进制，然后每一位按照 $n$ 的次幂加权计算即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ll n, k;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; k;</span><br><span class="line">    ll ans = <span class="number">0</span>, t = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(k &gt; <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(k &amp; <span class="number">1</span>)&#123;</span><br><span class="line">            ans = (ans + t) % mode;</span><br><span class="line">        &#125;</span><br><span class="line">        k &gt;&gt;= <span class="number">1</span>;</span><br><span class="line">        t = t * n % mode;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="C-Make-Them-Equal"><a href="#C-Make-Them-Equal" class="headerlink" title="C. Make Them Equal"></a>C. Make Them Equal</h4><p>题意：给出一个字符串 $s$ 和字符 $c$，可以进行若干次操作，每次操作可以选择一个数 $x$，然后令字符串中所有下标不能被 $x$ 的整除的位置上的字符变成 $c$，问最少多少次操作可以使字符串全变成 $c$</p>
<p>解：由于 $n&gt;2$，两次操作分别选择 $n$ 和 $n-1$ 一定可以完成，那么只需要考虑能不能一次完成</p>
<p>一次完成要求存在一个数 $x$，串中所有可以被 $x$ 整除的位置上都已经是 $c$ 了，暴力枚举即可，复杂度比较玄学但它就是 $nlogn$</p>
<p>后来又听了无敌的会长的优化方案：只需要枚举 $n&#x2F;2$ 以上的 $x$ 即可，因为更小的 $x$ 总有一个倍数比它更优，复杂度 $2n$</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    <span class="type">char</span> c;</span><br><span class="line">    string s;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; c &gt;&gt; s;</span><br><span class="line">    s = <span class="string">&quot;0&quot;</span> + s;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="type">bool</span> flg = <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=i;j&lt;=n;j+=i)&#123;</span><br><span class="line">            <span class="keyword">if</span>(s[j] != c)&#123;</span><br><span class="line">                flg = <span class="literal">false</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(flg)&#123;</span><br><span class="line">            <span class="keyword">if</span>(i == <span class="number">1</span>)&#123;</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;0\n&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            cout &lt;&lt; <span class="number">1</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            cout &lt;&lt; i &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="number">2</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    cout &lt;&lt; n<span class="number">-1</span> &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; n &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D-The-Number-of-Imposters"><a href="#D-The-Number-of-Imposters" class="headerlink" title="D. The Number of Imposters"></a>D. The Number of Imposters</h4><p>题意：有 $n$ 个人和 $m$ 个陈述，每个陈述形如：$i,j,imposter&#x2F;crewmate$ 表示 $i$ 说 $j$ 是说真话的人&#x2F;说谎者，问说谎话的人最多有几个</p>
<p>解：注意到一个性质：如果 $i$ 说 $j$ 说的是谎话，那么 $i$ 和 $j$ 必定相反；如果 $i$ 说 $j$ 说的是真话，那么 $i$ 和 $j$ 相同。</p>
<p>那么每个陈述都可以转化为一条边，对每个联通块进行染色后取较多的颜色加入贡献即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> st[maxn];</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> t, nt, tp;</span><br><span class="line">&#125;e[maxn &lt;&lt; <span class="number">1</span>];</span><br><span class="line"><span class="type">int</span> head[maxn], ecnt = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y, <span class="type">int</span> tp)</span></span>&#123;</span><br><span class="line">    e[++ecnt].t = y;</span><br><span class="line">    e[ecnt].tp = tp;</span><br><span class="line">    e[ecnt].nt = head[x];</span><br><span class="line">    head[x] = ecnt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> num[<span class="number">2</span>];</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=head[p];i;i=e[i].nt)&#123;</span><br><span class="line">        <span class="type">int</span> v = e[i].t;</span><br><span class="line">        <span class="keyword">if</span>(st[v] == <span class="number">-1</span>)&#123;</span><br><span class="line">            st[v] = st[p] ^ e[i].tp;</span><br><span class="line">            num[st[v]]++;</span><br><span class="line">            <span class="keyword">if</span>(!<span class="built_in">dfs</span>(v)) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">if</span>(st[v] != (st[p] ^ e[i].tp)) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> n, m;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line"></span><br><span class="line">    ecnt = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        head[i] = <span class="number">0</span>;</span><br><span class="line">        st[i] = <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y; string s;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y &gt;&gt; s;</span><br><span class="line">        <span class="type">int</span> tp = (s[<span class="number">0</span>] == <span class="string">&#x27;i&#x27;</span>);</span><br><span class="line">        <span class="built_in">add</span>(x, y, tp);</span><br><span class="line">        <span class="built_in">add</span>(y, x, tp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(st[i] == <span class="number">-1</span>)&#123;</span><br><span class="line">            st[i] = <span class="number">1</span>;</span><br><span class="line">            num[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">            num[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">if</span>(!<span class="built_in">dfs</span>(i))&#123;</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            ans += <span class="built_in">max</span>(num[<span class="number">0</span>], num[<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="E1-Rubik’s-Cube-Coloring-easy-version"><a href="#E1-Rubik’s-Cube-Coloring-easy-version" class="headerlink" title="E1. Rubik’s Cube Coloring (easy version)"></a>E1. Rubik’s Cube Coloring (easy version)</h4><p>题意：给一颗 $k$ 层的完全二叉树，每个节点有一个颜色（共六种），要求每个节点和其相邻节点颜色不能相同，并且规定了三对不能相邻的颜色，问有多少中染色方案</p>
<p>解：答案为 $4^{2^n-2}\times 6$</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">ll <span class="title">qpow</span><span class="params">(ll x, ll p)</span></span>&#123;</span><br><span class="line">    ll r = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(p &gt; <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(p &amp; <span class="number">1</span>) r = r * x % mode;</span><br><span class="line">        x = x * x % mode;</span><br><span class="line">        p &gt;&gt;= <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    cout &lt;&lt; <span class="built_in">qpow</span>(<span class="number">4</span>, (<span class="number">1ll</span> &lt;&lt; n) - <span class="number">2</span>) * <span class="number">6</span> % mode &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 749 div.1 2 A-E</title>
    <url>/2021/10/17/2021-10-17-Codeforces%20Round%20749%20div.1%202/</url>
    <content><![CDATA[<h4 id="A-Windblume-Ode"><a href="#A-Windblume-Ode" class="headerlink" title="A. Windblume Ode"></a>A. Windblume Ode</h4><p>题意：给一个数字集合（$n\ge3$），要从中取出尽可能多的数字，使之和为合数</p>
<p>判断总和是否为质数，是质数就随便去掉一个奇数就好了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="type">int</span> a[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="type">int</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; a[i];</span><br><span class="line">        sum += a[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">2</span>;i&lt;sum;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(sum % i == <span class="number">0</span>)&#123;</span><br><span class="line">            cout &lt;&lt; n &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++) cout &lt;&lt; i &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">            cout &lt;&lt; n &lt;&lt;<span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i] %<span class="number">2</span> == <span class="number">1</span>)&#123;</span><br><span class="line">            cout &lt;&lt; n - <span class="number">1</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            <span class="type">bool</span> f = <span class="literal">false</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;j++)&#123;</span><br><span class="line">                <span class="keyword">if</span>(j == i) <span class="keyword">continue</span>;</span><br><span class="line">                <span class="keyword">if</span>(f) cout &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">                <span class="keyword">else</span> f = <span class="literal">true</span>;</span><br><span class="line">                cout &lt;&lt; j;</span><br><span class="line">            &#125;</span><br><span class="line">            cout &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="B-Omkar-and-Heavenly-Tree"><a href="#B-Omkar-and-Heavenly-Tree" class="headerlink" title="B. Omkar and Heavenly Tree"></a>B. Omkar and Heavenly Tree</h4><p>题意：要构造一个 $n$ 个节点的数，满足 $m$ 个约束。一个约束描述为 $a,b,c$，表示节点 $b$ 不能在 $a,c$ 之间的简单路径上，（$m\lt n$）</p>
<p>这题乍一看没什么思路，突破口在于 $m \lt n$ 这个条件，我们发现，至少有一个节点没有被约束，设它为 $mid$</p>
<p>只需要把其他所有点都直接连在 $mid$ 上即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="type">int</span> vis[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        vis[i] = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y, z;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y &gt;&gt; z;</span><br><span class="line">        vis[y] = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> mid;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(!vis[i])&#123;</span><br><span class="line">            mid = i;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(i != mid)&#123;</span><br><span class="line">            cout &lt;&lt; mid &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; i &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="C-Omkar-and-Determination"><a href="#C-Omkar-and-Determination" class="headerlink" title="C. Omkar and Determination"></a>C. Omkar and Determination</h4><p>题意：给出一个 $n\times m$ 的地图，每一格可以是空地或墙，每个空地上存在一个机器人，它可以向左或向上走；</p>
<p>​        对于一个地图，我们可以把所有<strong>能走出地图</strong>的空格染成白色，其他格子染成黑色；</p>
<p>​        如果一个地图可以<strong>仅根据染色后的结果</strong>推断出原先的图（哪些是空地哪些是墙），那么称这个地图是<strong>有决心的</strong>；</p>
<p>​        接下来有 $q$ 个询问，每次询问从 $x_1$ 列到 $x_2$ 列的这个子图是不是有决心的。</p>
<p>解：考虑什么样的图的有决心的，可以想到如果存在这样的两个墙，一定不是有决心的（右下角无法确定）<br>$$<br>0 1\<br>1 ?<br>$$<br>​        再想一想可以发现，所有<strong>有决心的</strong>图都一定不包含这种东西</p>
<p>​        对于每一列 $j$，如果 $j$ 和 $j+1$ 列中存在这样的形状就记这列为1，前缀和搞一下查询就好了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">string s[<span class="number">1000004</span>];</span><br><span class="line"><span class="type">int</span> n, m, sum[<span class="number">1000004</span>];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; s[i];</span><br><span class="line">        s[i] = <span class="string">&#x27;0&#x27;</span> + s[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) sum[i] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">2</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;m;j++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(s[i][j] == <span class="string">&#x27;X&#x27;</span> &amp;&amp; s[i<span class="number">-1</span>][j+<span class="number">1</span>] == <span class="string">&#x27;X&#x27;</span>)&#123;</span><br><span class="line">                sum[j] = <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">2</span>;j&lt;=m;j++) sum[j] += sum[j<span class="number">-1</span>];</span><br><span class="line">    <span class="type">int</span> Q;</span><br><span class="line">    cin &gt;&gt; Q;</span><br><span class="line">    <span class="keyword">while</span>(Q--)&#123;</span><br><span class="line">        <span class="type">int</span> l, r;</span><br><span class="line">        cin &gt;&gt; l &gt;&gt; r;</span><br><span class="line">        <span class="keyword">if</span>(sum[r<span class="number">-1</span>]-sum[l<span class="number">-1</span>] == <span class="number">0</span>) cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">        <span class="keyword">else</span> cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D-Omkar-and-the-Meaning-of-Life"><a href="#D-Omkar-and-the-Meaning-of-Life" class="headerlink" title="D. Omkar and the Meaning of Life"></a>D. Omkar and the Meaning of Life</h4><p>题意：交互题，要猜一个 $n$ 的排列 $p_1,p_2…p_n$ ，每次猜测可以输入一个长度为 $n$ 的数列 $a_1,a_2…a_n$，随后形成一个新的序列 $s$，根据         $s_i &#x3D; a_i+p_i$ ，然后会返回 $s$ 中<strong>第一次重复出现元素的下标</strong>，都不重复则返回0，询问至多 $2n$ 次</p>
<p>解：交互题往往有个构造题的思路，先考虑给所有数都加上1，唯独最后一个数加 $x$，如果 $p_n + x \gt n+1$，那么输出为0，否则有其他输出，那么可以从小到大枚举 $x$，最多 $n$ 次即可猜出 $p_n$ 的数值</p>
<p>现在知道最后一位数的值了，接下来给所有数都加上 $p_n$，唯独最后一位数加上1，就可以知道1所在的位置</p>
<p>依次类推，再 $n$ 次查询即可知道所有数的位置。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, a[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    a[n] = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> k=n;k&gt;=<span class="number">2</span>;k--)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;? &quot;</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++) cout &lt;&lt; <span class="number">1</span> &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">        cout &lt;&lt; n-k+<span class="number">2</span> &lt;&lt; endl;</span><br><span class="line">        cout.<span class="built_in">flush</span>();</span><br><span class="line">        <span class="type">int</span> re;</span><br><span class="line">        cin &gt;&gt; re;</span><br><span class="line">        <span class="keyword">if</span>(re == <span class="number">0</span>)&#123;</span><br><span class="line">            a[n] = k;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(i == a[n]) <span class="keyword">continue</span>;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;? &quot;</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;n;j++) cout &lt;&lt; a[n] &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">        cout &lt;&lt; i &lt;&lt; endl;</span><br><span class="line">        cout.<span class="built_in">flush</span>();</span><br><span class="line"> </span><br><span class="line">        <span class="type">int</span> re;</span><br><span class="line">        cin &gt;&gt; re;</span><br><span class="line">        a[re] = i;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;!&quot;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cout &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; a[i];</span><br><span class="line">    cout &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D2-Half-of-Same"><a href="#D2-Half-of-Same" class="headerlink" title="D2. Half of Same"></a>D2. Half of Same</h4><p>题意：和D1一样，区别在于只需要有一半数减去若干个 $k$ 后变成相同的数字</p>
<p>$n\le40, -10^6\le a_i \le 10^6$</p>
<p>先考虑，如果有一个 $k$，我们怎么判断它是否合法：</p>
<p>可以让每个数都减 $k$ 减到<strong>第一次小于 $mi$ 为止</strong>，然后数一下有多少数是相同的</p>
<p>这个Judge过程是 $O(n)$ 的</p>
<p>那么有多少个 $k$ 需要judge呢？</p>
<p>可以从1到2e6全部枚举一遍，$O(2e6\times n)$ 看似可行，然而被卡常了</p>
<p>想到这个 $k$ 一定是某两个数的差的约数，又因为 $n$ 很小，可以非常暴力地枚举任意两个数，再枚举它们差的约数，总复杂度 $O(n^2\sqrt{2\times10^6}\times n)$</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">50</span>];</span><br><span class="line"><span class="type">int</span> mia, mxa;</span><br><span class="line"><span class="type">int</span> num[<span class="number">5000009</span>];</span><br><span class="line"><span class="type">int</span> n;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">judge</span><span class="params">(<span class="type">int</span> k)</span></span>&#123;</span><br><span class="line">    <span class="type">bool</span> re = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> t = a[i] - (a[i]-mia+k<span class="number">-1</span>)/k*k;</span><br><span class="line">        num[t]++;</span><br><span class="line">        <span class="keyword">if</span>(num[t] &gt;= n/<span class="number">2</span>) re = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> t = a[i] - (a[i]-mia+k<span class="number">-1</span>)/k*k;</span><br><span class="line">        num[t]--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> re;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    mia = <span class="number">2e8</span>, mxa = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; a[i];</span><br><span class="line">        a[i] += <span class="number">4000000</span>;</span><br><span class="line">        mia = <span class="built_in">min</span>(mia, a[i]);</span><br><span class="line">        mxa = <span class="built_in">max</span>(mxa, a[i]);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="built_in">sort</span>(a+<span class="number">1</span>, a+n+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=i;j&lt;=n;j++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(a[j] != a[i])&#123;</span><br><span class="line">                i = j - <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(j - i + <span class="number">1</span> &gt;= n/<span class="number">2</span>)&#123;</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;j++)&#123;</span><br><span class="line">            <span class="type">int</span> d = <span class="built_in">abs</span>(a[j] - a[i]);</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> k=<span class="number">1</span>;k&lt;=<span class="built_in">sqrt</span>(d);k++)&#123;</span><br><span class="line">                <span class="keyword">if</span>(d % k != <span class="number">0</span>) <span class="keyword">continue</span>;</span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">judge</span>(k)) ans = <span class="built_in">max</span>(ans, k);</span><br><span class="line">                <span class="keyword">if</span>(<span class="built_in">judge</span>(d/k)) ans = <span class="built_in">max</span>(ans, d/k);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="E-Moment-of-Bloom"><a href="#E-Moment-of-Bloom" class="headerlink" title="E. Moment of Bloom"></a>E. Moment of Bloom</h4><p>个人感觉最妙的一道题，做的时候一点思路没有，一看题解恍然大悟</p>
<p>题意：有一个 $n$ 节点，$m$ 条边的连通图（无重边自环），有 $q$ 个操作，每次操作仅给出 $a,b$ 两个点，可以在两点间任选一条简单路径，使路径上的边权都+1（初始为0），问这 $q$ 个操作后能否使所有边权均为偶数？如果不能输出需要额外操作的数量</p>
<p>解：一个点<strong>相邻边的边权之和</strong> &#x3D; 以这个点为端点的路径条数 + 其他经过这个点的路径条数 * 2</p>
<p>​        那么如果一个点在操作中出现了奇数次，那么它连的边权之和也为奇数，也就是说必然存在一条边不满足条件</p>
<p>​        所以可以统计所有操作中各个点出现的次数，如果存在奇数次的点，答案即为NO，额外需要奇数点个数&#x2F;2次操作</p>
<p>​        所有点均出现偶数次，答案即为YES，这题还要求输出每一个操作所选择的路径，怎么构造呢——</p>
<p>​        其实很简单，把图改造成任一个生成树，然后走唯一路径就好了（（具体可以颅内理解一下，不难证明正确性    </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="type">int</span> cnt[maxn];</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; vp[maxn];</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; vt[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Query</span>&#123;</span><br><span class="line">    <span class="type">int</span> x, y;</span><br><span class="line">&#125;q[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> vis[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p)</span></span>&#123;</span><br><span class="line">    vis[p] = <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> &amp;v : vp[p])&#123;</span><br><span class="line">        <span class="keyword">if</span>(!vis[v])&#123;</span><br><span class="line">            vt[p].<span class="built_in">push_back</span>(v);</span><br><span class="line">            vt[v].<span class="built_in">push_back</span>(p);</span><br><span class="line">            <span class="built_in">dfs</span>(v);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> ans[maxn], hd;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dfsf</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> fa, <span class="type">int</span> t)</span></span>&#123;</span><br><span class="line">    ans[++hd] = p;</span><br><span class="line">    <span class="keyword">if</span>(p == t) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> &amp;v : vt[p])&#123;</span><br><span class="line">        <span class="keyword">if</span>(v != fa)&#123;</span><br><span class="line">            <span class="keyword">if</span>(<span class="built_in">dfsf</span>(v, p, t)) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    hd--;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y;</span><br><span class="line">        vp[x].<span class="built_in">push_back</span>(y);</span><br><span class="line">        vp[y].<span class="built_in">push_back</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> qm;</span><br><span class="line">    cin &gt;&gt; qm;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=qm;i++)&#123;</span><br><span class="line">        cin &gt;&gt; q[i].x &gt;&gt; q[i].y;</span><br><span class="line">        cnt[q[i].x]++;</span><br><span class="line">        cnt[q[i].y]++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> odd = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) odd += cnt[i]%<span class="number">2</span>;</span><br><span class="line">    <span class="keyword">if</span>(odd &gt; <span class="number">0</span>)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">        cout &lt;&lt; odd/<span class="number">2</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">        <span class="built_in">dfs</span>(<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=qm;i++)&#123;</span><br><span class="line">            hd = <span class="number">0</span>;</span><br><span class="line">            <span class="built_in">dfsf</span>(q[i].x, <span class="number">-1</span>, q[i].y);</span><br><span class="line">            cout &lt;&lt; hd &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;hd;j++) cout &lt;&lt; ans[j] &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">            cout &lt;&lt; ans[hd] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 755 div.1 ABC</title>
    <url>/2021/11/14/2021-11-14-Codeforces%20Round%20755%20div.1/</url>
    <content><![CDATA[<p>第一次打div1，头被锤烂了（</p>
<h4 id="A-Two-Arrays"><a href="#A-Two-Arrays" class="headerlink" title="A. Two Arrays"></a>A. Two Arrays</h4><p>题意：有两个 $1-n$ 的排列 $a,b$，可以对 $a$ 序列进行如下操作：</p>
<ol>
<li>将一些位置上的数+1</li>
<li>任意改变 $a$ 的顺序</li>
</ol>
<p>问能否一次操作将 $a$ 变成 $b$</p>
<p>排序后依次判断每一位上的 $a$ 能否变成 $b$ 即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="type">int</span> a[maxn], b[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; b[i];</span><br><span class="line">    <span class="built_in">sort</span>(a+<span class="number">1</span>, a+n+<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">sort</span>(b+<span class="number">1</span>, b+n+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i] &gt; b[i])&#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(a[i] &lt; b[i] - <span class="number">1</span>)&#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>7分钟签完这题，剩下时间都在坐牢了（（</p>
<h4 id="B-Guess-the-Permutation"><a href="#B-Guess-the-Permutation" class="headerlink" title="B. Guess the Permutation"></a>B. Guess the Permutation</h4><p>题意：交互题，有一个 $1-n$ 的排列 $a$，初始时 $a_i &#x3D; i$，电脑对这个序列进行了一次更改：将 $[i,j-1]$，$[j,k]$ 两个区间翻转了，你可以询问：$[l,r]$ 上有多少个逆序对？询问至多40次，要猜出 $i,j,k$</p>
<p>先二分找到 $i$，用至多30次，然后可以根据 $[i,n]$ 的结果减去 $[i+1,n]$ 直接得出第一个逆序串的长度。。同理第二个也很好弄了。</p>
<p>考场上剩下时间全在搞这个题的玄学二分优化、三分</p>
<p>赛后一看代码，发现核心思维压根就没想到，很服气</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ll n;</span><br><span class="line"><span class="function">ll <span class="title">query</span><span class="params">(ll l, ll r)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;? &quot;</span> &lt;&lt; l &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; r &lt;&lt; endl;</span><br><span class="line">    cout.<span class="built_in">flush</span>();</span><br><span class="line">    ll x;</span><br><span class="line">    cin &gt;&gt; x;</span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line"> </span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    ll l = <span class="number">1</span>, r = n;</span><br><span class="line">    <span class="keyword">while</span>(l &lt; r)&#123;</span><br><span class="line">        ll mid = (l + r + <span class="number">1</span>) / <span class="number">2</span>;</span><br><span class="line">        ll x = <span class="built_in">query</span>(<span class="number">1</span>, mid);</span><br><span class="line">        <span class="keyword">if</span>(x == <span class="number">0</span>)&#123;</span><br><span class="line">            l = mid;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            r = mid - <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ll s1 = <span class="number">1</span> + <span class="built_in">query</span>(l, n) - <span class="built_in">query</span>(l + <span class="number">1</span>, n);</span><br><span class="line">    ll j = l + s1;</span><br><span class="line">    ll s2 = <span class="number">1</span> + <span class="built_in">query</span>(j, n) - <span class="built_in">query</span>(j + <span class="number">1</span>, n);</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;! &quot;</span> &lt;&lt; l &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; l + s1 &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; l + s1 + s2 - <span class="number">1</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line"> </span><br><span class="line">    cout.<span class="built_in">flush</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="C-Game-with-Stones"><a href="#C-Game-with-Stones" class="headerlink" title="C. Game with Stones"></a>C. Game with Stones</h4><p>题意：有一列石头堆 $a$，$a_i$ 表示第 $i$ 堆石头的数量，Bob每次可以选择两个相邻的石头堆，同时从中拿走一个石头（不能选择空石头堆），如果Bob可以把这列石头堆全部清空，则Bob获胜。现在问有多少组 $[l,r]$ 使得单独拿出 $l$ 到 $r$ 位置上的石头，可以使Bob获胜。</p>
<p>首先可以注意到，任意一列石头，Bob必须从两端开始取，比如从左到右取，那么就依次让每个位置上的值减去上一位的值，设 $c_i&#x3D;a_i-c_{i-1}$，如果取的中途出现了一位 $c_i$ 为负数，则一定不能获胜；取到结尾处如果 $c_n&#x3D;0$ 则恰能获胜，否则还是不能获胜。</p>
<p>考虑左端点为1的情况：直接一遍计算出所有的 $c$，然后统计 $c$ <strong>在第一个负数之前0的个数</strong>，即为左端点为1的答案。</p>
<p>接下来考虑如何快速转移：忽略第一堆石头，将左端点为1情况下的 $c$ 转移到左端点为2的情况。</p>
<p>很显然，将最左边的 $a_1$ 去掉会使得 $c_2$ 加上 $a_1$，$c_3$ 减去 $a_1$，$c_4$ 又加上 $a_1$ …</p>
<p>即使得后面的所有奇数位加上或减去 $a_1$，偶数位反之；在任何一次转移中，所有的奇数位都同时变化，偶数位也同时变化。</p>
<p>那么可以考虑将奇偶数位分开维护，这样我们只需要一种数据结构，需要支持<strong>区间加减法</strong>和区间查找 $c$ <strong>在第一个负数之前0的个数</strong></p>
<p>这里给出一种线段树维护的思路：</p>
<p>对于奇数位和偶数位各开一个线段树，维护区间<strong>最小值</strong>和<strong>最小值的数量</strong>，这个在区间加减法下很容易维护。</p>
<p>写两种操作：查询<strong>区间第一个负数的位置</strong> 和查询<strong>不含负数区间中0的数量</strong>，显然这两个操作能够满足需求。</p>
<p>第一种操作，查找时若左段的最小值为负，则查找左边，否则查找右边；</p>
<p>第二种操作，因为保证区间没有负数了，找到对应区间后直接返回最小值数量即可，注意要筛掉最小值不为0的区间。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ll n, a[maxn], c[maxn];</span><br><span class="line"></span><br><span class="line"><span class="function">ll <span class="title">f1</span><span class="params">(ll x)</span></span>&#123; <span class="keyword">return</span> <span class="number">2</span> * x - <span class="number">1</span>;&#125;</span><br><span class="line"><span class="function">ll <span class="title">f2</span><span class="params">(ll x)</span></span>&#123; <span class="keyword">return</span> <span class="number">2</span> * x;&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">SegTree</span>&#123;</span><br><span class="line">    ll num[maxn &lt;&lt; <span class="number">2</span>], mi[maxn &lt;&lt; <span class="number">2</span>], tad[maxn &lt;&lt; <span class="number">2</span>];</span><br><span class="line">    ll ls[maxn &lt;&lt; <span class="number">2</span>], rs[maxn &lt;&lt; <span class="number">2</span>];</span><br><span class="line">    <span class="built_in">ll</span> (*fp)(ll);</span><br><span class="line">    <span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">push_up</span><span class="params">(ll p)</span></span>&#123;</span><br><span class="line">        num[p] = num[ls[p]] + num[rs[p]];</span><br><span class="line">        mi[p] = <span class="built_in">min</span>(mi[ls[p]], mi[rs[p]]);</span><br><span class="line">        num[p] = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span>(mi[p] == mi[ls[p]]) num[p] += num[ls[p]];</span><br><span class="line">        <span class="keyword">if</span>(mi[p] == mi[rs[p]]) num[p] += num[rs[p]];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">push_add</span><span class="params">(ll p, ll ad)</span></span>&#123;</span><br><span class="line">        mi[p] += ad;</span><br><span class="line">        tad[p] += ad;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">push_down</span><span class="params">(ll p)</span></span>&#123;</span><br><span class="line">        <span class="built_in">push_add</span>(ls[p], tad[p]);</span><br><span class="line">        <span class="built_in">push_add</span>(rs[p], tad[p]);</span><br><span class="line">        tad[p] = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">build</span><span class="params">(ll p, ll l, ll r)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(l &gt; r) <span class="keyword">return</span>;</span><br><span class="line">        tad[p] = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span>(l == r)&#123;</span><br><span class="line">            num[p] = <span class="number">1</span>;</span><br><span class="line">            mi[p] = c[<span class="built_in">fp</span>(l)];</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        ll mid = (l + r) / <span class="number">2</span>;</span><br><span class="line">        ls[p] = p*<span class="number">2</span>, rs[p] = p*<span class="number">2</span>+<span class="number">1</span>;</span><br><span class="line">        <span class="built_in">build</span>(p*<span class="number">2</span>, l, mid);</span><br><span class="line">        <span class="built_in">build</span>(p*<span class="number">2</span>+<span class="number">1</span>, mid+<span class="number">1</span>, r);</span><br><span class="line">        <span class="built_in">push_up</span>(p);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">modify</span><span class="params">(ll p, ll l, ll r, ll L, ll R, ll ad)</span></span>&#123;</span><br><span class="line">        <span class="comment">//lr为线段树中节点管辖下标范围，LR为实际查询范围</span></span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">fp</span>(l) &gt; R || <span class="built_in">fp</span>(r) &lt; L) <span class="keyword">return</span>;</span><br><span class="line">        <span class="keyword">if</span>(L &lt;= <span class="built_in">fp</span>(l) &amp;&amp; <span class="built_in">fp</span>(r) &lt;= R)&#123;</span><br><span class="line">            <span class="built_in">push_add</span>(p, ad);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">push_down</span>(p);</span><br><span class="line">        ll mid = (l + r) / <span class="number">2</span>;</span><br><span class="line">        <span class="built_in">modify</span>(ls[p], l, mid, L, R, ad);</span><br><span class="line">        <span class="built_in">modify</span>(rs[p], mid+<span class="number">1</span>, r, L, R, ad);</span><br><span class="line">        <span class="built_in">push_up</span>(p);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">ll <span class="title">query_lz</span><span class="params">(ll p, ll l, ll r, ll L, ll R)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">fp</span>(l) &gt; R || <span class="built_in">fp</span>(r) &lt; L) <span class="keyword">return</span> <span class="number">1e9</span>;</span><br><span class="line">        <span class="keyword">if</span>(mi[p] &gt;= <span class="number">0</span>) <span class="keyword">return</span> <span class="number">1e9</span>;</span><br><span class="line">        <span class="keyword">if</span>(l == r) <span class="keyword">return</span> <span class="built_in">fp</span>(l);</span><br><span class="line">        <span class="built_in">push_down</span>(p);</span><br><span class="line">        ll mid = (l + r) / <span class="number">2</span>, re = <span class="number">1e9</span>;</span><br><span class="line">        re = <span class="built_in">min</span>(re, <span class="built_in">query_lz</span>(ls[p], l, mid, L, R));</span><br><span class="line">        <span class="keyword">if</span>(re &gt; <span class="number">1e8</span>) re = <span class="built_in">min</span>(re, <span class="built_in">query_lz</span>(rs[p], mid+<span class="number">1</span>, r, L, R));</span><br><span class="line">        <span class="keyword">return</span> re;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">ll <span class="title">query_num</span><span class="params">(ll p, ll l, ll r, ll L, ll R)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">fp</span>(l) &gt; R || <span class="built_in">fp</span>(r) &lt; L) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span>(mi[p] &gt; <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span>(L &lt;= <span class="built_in">fp</span>(l) &amp;&amp; <span class="built_in">fp</span>(r) &lt;= R) <span class="keyword">return</span> num[p];<span class="comment">//已经确认mi[p] == 0</span></span><br><span class="line">        <span class="built_in">push_down</span>(p);</span><br><span class="line">        ll mid = (l + r) / <span class="number">2</span>, re = <span class="number">0</span>;</span><br><span class="line">        re += <span class="built_in">query_num</span>(ls[p], l, mid, L, R);</span><br><span class="line">        re += <span class="built_in">query_num</span>(rs[p], mid+<span class="number">1</span>, r, L, R);</span><br><span class="line">        <span class="keyword">return</span> re;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;st1, st2;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">1</span>;i&lt;=n;i++) c[i] = a[i] - c[i<span class="number">-1</span>];</span><br><span class="line"></span><br><span class="line">    st1.fp = f1;</span><br><span class="line">    st2.fp = f2;</span><br><span class="line">    st1.<span class="built_in">build</span>(<span class="number">1</span>, <span class="number">1</span>, (n+<span class="number">1</span>)/<span class="number">2</span>);</span><br><span class="line">    st2.<span class="built_in">build</span>(<span class="number">1</span>, <span class="number">1</span>, n/<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    ll ans = <span class="number">0</span>;</span><br><span class="line">    ll zp = <span class="built_in">min</span>(st1.<span class="built_in">query_lz</span>(<span class="number">1</span>, <span class="number">1</span>, (n+<span class="number">1</span>)/<span class="number">2</span>, <span class="number">1</span>, n),</span><br><span class="line">                 st2.<span class="built_in">query_lz</span>(<span class="number">1</span>, <span class="number">1</span>, n/<span class="number">2</span>, <span class="number">1</span>, n));</span><br><span class="line">    <span class="keyword">if</span>(zp &gt; <span class="number">1e8</span>) zp = n + <span class="number">1</span>;</span><br><span class="line">    ans += st1.<span class="built_in">query_num</span>(<span class="number">1</span>, <span class="number">1</span>, (n+<span class="number">1</span>)/<span class="number">2</span>, <span class="number">1</span>, zp<span class="number">-1</span>);</span><br><span class="line">    ans += st2.<span class="built_in">query_num</span>(<span class="number">1</span>, <span class="number">1</span>, n/<span class="number">2</span>, <span class="number">1</span>, zp<span class="number">-1</span>);</span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">1</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(i % <span class="number">2</span> == <span class="number">0</span>)&#123;</span><br><span class="line">            st1.<span class="built_in">modify</span>(<span class="number">1</span>, <span class="number">1</span>, (n+<span class="number">1</span>)/<span class="number">2</span>, i+<span class="number">1</span>, n, a[i]);</span><br><span class="line">            st2.<span class="built_in">modify</span>(<span class="number">1</span>, <span class="number">1</span>, n/<span class="number">2</span>, i+<span class="number">1</span>, n, -a[i]);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            st1.<span class="built_in">modify</span>(<span class="number">1</span>, <span class="number">1</span>, (n+<span class="number">1</span>)/<span class="number">2</span>, i+<span class="number">1</span>, n, -a[i]);</span><br><span class="line">            st2.<span class="built_in">modify</span>(<span class="number">1</span>, <span class="number">1</span>, n/<span class="number">2</span>, i+<span class="number">1</span>, n, a[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        ll zp = <span class="built_in">min</span>(st1.<span class="built_in">query_lz</span>(<span class="number">1</span>, <span class="number">1</span>, (n+<span class="number">1</span>)/<span class="number">2</span>, i+<span class="number">1</span>, n),</span><br><span class="line">                     st2.<span class="built_in">query_lz</span>(<span class="number">1</span>, <span class="number">1</span>, n/<span class="number">2</span>, i+<span class="number">1</span>, n));</span><br><span class="line">        <span class="keyword">if</span>(zp &gt; <span class="number">1e8</span>) zp = n + <span class="number">1</span>;</span><br><span class="line">        ans += st1.<span class="built_in">query_num</span>(<span class="number">1</span>, <span class="number">1</span>, (n+<span class="number">1</span>)/<span class="number">2</span>, i+<span class="number">1</span>, zp<span class="number">-1</span>);</span><br><span class="line">        ans += st2.<span class="built_in">query_num</span>(<span class="number">1</span>, <span class="number">1</span>, n/<span class="number">2</span>, i+<span class="number">1</span>, zp<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>C++小记</title>
    <url>/2021/11/17/2021-11-17-C++%E5%B0%8F%E8%AE%B0/</url>
    <content><![CDATA[<hr>
<h3 id="const，引用和指针"><a href="#const，引用和指针" class="headerlink" title="const，引用和指针"></a>const，引用和指针</h3><h4 id="const，顶层与底层"><a href="#const，顶层与底层" class="headerlink" title="const，顶层与底层"></a>const，顶层与底层</h4><p>const 类型必须在定义时附上初始值；class中的const成员必须在初始化列表中有初始值；</p>
<p>分辨const修饰的对象：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> <span class="type">const</span> * <span class="type">const</span> p;</span><br><span class="line"><span class="comment">//上面有三个位置可能放const限定符，按照*划开，*左端为指针指向的类型，右端才是对这个指针的限定修饰；</span></span><br><span class="line"><span class="comment">//举例：</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> *p;<span class="comment">//p指向的是int常量，p本身不是常量，可以更改指向</span></span><br><span class="line"><span class="type">int</span> <span class="type">const</span> *p;<span class="comment">//和上条完全等价</span></span><br><span class="line"><span class="type">int</span> *<span class="type">const</span> p;<span class="comment">//p指向的是int变量，p本身是一个常量，不能改变指向</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> *<span class="type">const</span> p;<span class="comment">//p指向int常量，且p本身是一个常量，不能改变指向</span></span><br></pre></td></tr></table></figure>

<p>按照上述方式划分清楚const的修饰对象即可，如果一定要顶层和底层const的概念的话：</p>
<p>通常，顶层const指本身为const，底层const指其指向的对象为const；</p>
<p>可见前两条为底层const，第三条为顶层const，第四条既是底层又是顶层const；</p>
<p>个人感觉顶层底层的概念把问题复杂化了。。。其实就是“这个变量本身是否为常量”</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//const用于函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">find</span><span class="params">(<span class="type">const</span> <span class="type">int</span> &amp;x)</span></span>&#123;</span><br><span class="line">    <span class="comment">//表明这个x是不可变的，一般和引用传递同时使用以保护实参变量</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">int</span>&amp; <span class="title">find</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//修饰引用、指针型的返回值，加const意味着返回的值不能修改（不可作为左值）</span></span><br><span class="line">    <span class="comment">//不太理解对非引用、指针型返回值加const的意义</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">compare</span><span class="params">()</span> <span class="type">const</span></span>&#123;</span><br><span class="line">    <span class="comment">//const修饰整个函数，只能对成员函数（类中的函数）使用；</span></span><br><span class="line">    <span class="comment">//意味着这个函数不能更改任何自身数据成员，也只能调用同被const修饰的方法。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>另外提一句：返回值为非引用、指针的内置类型时，返回的值不能作为左值（当然），而返回的对象则可以作为左值，可以被赋值或者立即调用方法等，不过在这一行过后它会立刻析构（因为也没有变量指向它）。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">square <span class="title">f</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="built_in">square</span>(x, y);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	cout &lt;&lt; <span class="built_in">f</span>(<span class="number">3</span>, <span class="number">4</span>).<span class="built_in">area</span>() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>void*指针可以指向任意类型的对象，但因为没有存储对象类型，不能通过其修改对象；</p>
<h4 id="引用：变量的别名"><a href="#引用：变量的别名" class="headerlink" title="引用：变量的别名"></a>引用：变量的别名</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="type">int</span> &amp;b = a;<span class="comment">//相当于a, b使用同一块存储区域，且都标记为了int类型，两者没有区别</span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span>&amp; c = a * <span class="number">2</span>;<span class="comment">//常量可以初始化成任意值，引用无意义</span></span><br><span class="line"><span class="type">int</span> &amp;d = <span class="number">42</span>;<span class="comment">//错误，不能将非常量引用绑定到常量</span></span><br></pre></td></tr></table></figure>

<p>对象作为参数时加入&amp;引用可以让传参更快（免去了拷贝的过程），对于内置类型，这样的加速无必要。</p>
<hr>
<h3 id="继承、多态"><a href="#继承、多态" class="headerlink" title="继承、多态"></a>继承、多态</h3><h4 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h4><p>子类继承了父类的所有非私有的成员和方法，除了以下几种：</p>
<ul>
<li>构造函数、析构函数、</li>
<li>重载的运算符</li>
<li>父类的友元函数</li>
</ul>
<p>子类在没有显示调用父类的构造函数时，会默认先调用一遍父类的无参构造函数；</p>
<p>注意此时父类的无参构造必须存在，否则会报错；</p>
<p>或者，在子类中显示地调用父类的构造函数，形如：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">square</span>(<span class="type">int</span> x, <span class="type">int</span> y) : <span class="built_in">shape</span>(x, y) &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>可以加 <code>final</code> 关键字阻止其他类继承此类</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Last</span> <span class="keyword">final</span>: <span class="keyword">public</span> base&#123;..&#125;</span><br></pre></td></tr></table></figure>



<h4 id="重载，重写，隐藏"><a href="#重载，重写，隐藏" class="headerlink" title="重载，重写，隐藏"></a>重载，重写，隐藏</h4><p>重载是同名但参数列表不同方法，依据参数列表决定调用哪个（仅返回值不同，不能构成重载，因为依然不能根据调用时传递的参数判断调用的是哪一版）；</p>
<p>隐藏是指子类中方法，会隐藏父类中同名的方法（<strong>即使参数列表不同</strong>），子类有一个find()方法后，再要通过这个子类调用它父类的名为find的方法，要使用作用域符号<code>::</code>；</p>
<p>重写指将父类中的方法定义为虚函数，然后子类的同名同参的方法可以覆盖父类中这一方法，相较于隐藏在父类方法中多写了一个virtual，和隐藏的区别在于：</p>
<ul>
<li>不加virtual时，通过父类指针&#x2F;引用调用方法，固定调用父类中的方法；</li>
<li>加virtual时，通过父类指针&#x2F;引用调用方法，指向哪个类就调用哪个类的方法；</li>
</ul>
<p>重写可以加上一个 <code>override</code> 表示这个函数试图重写父类虚函数，它是一种标识符，标记后如果他没有重写（比如你自己写错名字了），编译器则会报错。个人暂未理解其实际作用。</p>
<h4 id="隐式转换"><a href="#隐式转换" class="headerlink" title="隐式转换"></a>隐式转换</h4><p>可以将子类对象转换为父类对象（派生方式必须为public），这完全可行，子类本身就包含了父类信息；</p>
<p>将父类对象转化为子类的对象，是不安全的。</p>
<h4 id="动态绑定"><a href="#动态绑定" class="headerlink" title="动态绑定"></a>动态绑定</h4><p>C++中，父类指针或引用可以指向子类对象，这会导致以下情况：</p>
<p>在通过<strong>父类指针&#x2F;引用</strong>调用一个<strong>虚函数</strong>时，按照上述原则，应该根据指针的指向决定调用的函数；</p>
<p>但在编译期，我们还不知道这个父类指针具体指向了哪个类，所以这次调用的函数版本不是在编译期决定的，而是在运行期才决定，所以被称为动态绑定。</p>
<p>或者换一个说法，父类指针的绑定对象是可以更改的，所以在编译期不能直接确定。</p>
<h4 id="多态的含义"><a href="#多态的含义" class="headerlink" title="多态的含义"></a>多态的含义</h4><p>我们可以对“多种形式”的同一类物体进行同一操作，而无需在意他们的差异；</p>
<p>一个接口，多种实现；</p>
<hr>
<h3 id="构造函数和初始化列表"><a href="#构造函数和初始化列表" class="headerlink" title="构造函数和初始化列表"></a>构造函数和初始化列表</h3><p>初始化列表的特殊用处在于：初始化const类型的成员变量。</p>
<p>直接在构造函数中为const类型赋值显然是不规范的，所以用到了初始化列表，可以认为它比构造函数“更早地”初始化了成员变量，const修饰的变量必须在初始化列表中初始化它</p>
<p>初始化列表中还可以使用类（或其父类）中的其他构造函数来初始化（委托构造函数）：</p>
<p>（且对于父类成员，只能使用父类构造函数来初始化）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">shape</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">shape</span>(<span class="type">int</span> x, <span class="type">int</span> y) : <span class="built_in">w</span>(x), <span class="built_in">h</span>(y) &#123;&#125;</span><br><span class="line">	<span class="built_in">shape</span>(<span class="type">const</span> shape &amp;t) : <span class="built_in">shape</span>(t.w, t.h) &#123;&#125;</span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">area</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="type">int</span> w, h;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">box</span> : <span class="keyword">public</span> shape &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">box</span>(<span class="type">int</span> x) : <span class="built_in">shape</span>(x, x) &#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">box</span> : <span class="keyword">public</span> shape &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">box</span>(<span class="type">int</span> x) : <span class="built_in">shape</span>(x, x) &#123;&#125;</span><br><span class="line">    <span class="comment">//explicit box(int x) : shape(x, x) &#123;&#125; 加上explicit限定，禁止其隐式转换</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	box t = <span class="number">3</span>;<span class="comment">//构造函数中若只有一个实参，可以隐式地将这个实参直接转化为类类型</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<hr>
<h3 id="拷贝控制"><a href="#拷贝控制" class="headerlink" title="拷贝控制"></a>拷贝控制</h3><p>在类的复制拷贝构造函数中参数一定要+引用，因为非引用的传参本就包含了拷贝过程，会造成递归调用。</p>
<p>一般也同时加上一个const来保护，这是一种典型的拷贝构造函数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">student</span>&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">student</span>(<span class="type">const</span> student &amp;x);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>注意：写了拷贝构造函数的同时覆盖了默认的拷贝构造函数，依据上一节中的隐式转换规则，在初始化时直接使用等于号，也将隐式调用<strong>仅有一个参数</strong>的拷贝构造函数；</p>
<p>可以给构造函数加上 <code>explicit</code> 关键字来阻止上述隐式转换；</p>
<p>而实际上的 <code>=</code> 赋值运算符是对一个已经初始化完毕的对象重新赋值时调用的，以此区分，见下代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">box</span> : <span class="keyword">public</span> shape &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">box</span>(<span class="type">int</span> x) : <span class="built_in">shape</span>(x, x) &#123;&#125;</span><br><span class="line">	<span class="built_in">box</span>(<span class="type">const</span> box&amp; t) : <span class="built_in">box</span>(t.w + <span class="number">1</span>) &#123;&#125;<span class="comment">//修改过的拷贝构造函数</span></span><br><span class="line">	<span class="type">void</span> <span class="keyword">operator</span> =(<span class="type">const</span> box&amp; t) &#123;<span class="comment">//赋值运算符</span></span><br><span class="line">		w = t.w;</span><br><span class="line">		h = t.h;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="function">box <span class="title">a</span><span class="params">(<span class="number">5</span>)</span></span>;</span><br><span class="line">	box b = a;					<span class="comment">//无初值的对象初始化，看似是等于号，其实调用的是拷贝构造函数</span></span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; b.<span class="built_in">area</span>() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;	<span class="comment">//结果为36</span></span><br><span class="line">	b = a;						<span class="comment">//有初值的对象，这里的等于号就是赋值运算符了</span></span><br><span class="line">	cout &lt;&lt; b.<span class="built_in">area</span>() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;	<span class="comment">//结果为25</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所以，拷贝构造函数除了直接调用之外，还会在用&#x3D;定义变量，传参，返回，初始化列表等等地方被调用，需要与operator&#x3D;辨别开来；</p>
<p>老规矩，operator&#x3D;和拷贝构造函数都有一个默认版本，自己定义后将默认版本覆盖</p>
<p>默认版本中，拷贝与赋值都是将所有成员一一对应赋值，对于指针成员，并不会为其开辟新的空间。</p>
<p>使用&#x3D;default显示地保留默认版本 </p>
<p><code>box() = default;</code></p>
<p>使用&#x3D;delete来删除默认版本，可以在不自定义的情况下阻止拷贝，赋值等等（析构不能被delete）</p>
<p> <code>box()  = delete</code></p>
<p>例如iostream类中就使用了上述方式阻止了拷贝和赋值。</p>
<hr>
<h3 id="Shared-ptr智能指针"><a href="#Shared-ptr智能指针" class="headerlink" title="Shared_ptr智能指针"></a>Shared_ptr智能指针</h3><p>这篇很好：<a href="https://blog.csdn.net/qq_29108585/article/details/78027867">https://blog.csdn.net/qq_29108585/article/details/78027867</a></p>
<p>当我们new一个对象时，会在堆中开辟一块空间，称之为$A$，这块空间的地址我们称为原始指针，new返回的即为此指针；</p>
<p>用原始指针初始化shared_ptr时，会在ptr中记录这个地址，同时开辟一块新的区域（可以这么理解）保存引用计数，这块区域称为$M$，ptr中也记录了$M$ 所在的地址；</p>
<p>当用ptr初始化或赋值另一个shared_ptr（假设为ptr2）时，ptr2不再会开辟一个新的$M$，而是和ptr指向同一个$M$，这个是他们的共同引用计数（这里没什么问题）</p>
<p>但如果用原始指针初始化或复制另一个shared_ptr时，就会出现另一个引用计数$M2$，并且两个引用计数都为1，这时任意一方计数归零时，都会直接回收对象的存储空间$A$，另一方则出大事；</p>
<p>这就是shared_ptr一些麻烦事的原因：</p>
<ul>
<li>不要重复用原始指针构造shared_ptr，而是用已经有的；</li>
<li>.get() 方法返回原始指针，不要用这个方法去赋值其他shared_ptr，原因同上；</li>
<li>不要混用指针和智能指针：智能指针不会对普通指针的引用计数，容易造成普通指针悬空；</li>
</ul>
<hr>
<h3 id="友元"><a href="#友元" class="headerlink" title="友元"></a>友元</h3><p>在类 $A$ 中声明一个友元函数，则这个函数（全局函数或者其它类的成员函数）可以访问 $A$ 中的私有成员</p>
<p>在类 $A$ 中声明一个友元类 $B$，则 $B$ 中所有成员函数都可以访问 $A$ 的私有成员</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">shape</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">friend</span> <span class="type">void</span> <span class="title">expend</span><span class="params">(shape x)</span></span>;</span><br><span class="line">	<span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">box</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="type">int</span> width, height;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">box</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">areaof</span><span class="params">(shape x)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> x.width * x.height;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">expend</span><span class="params">(shape x)</span> </span>&#123;</span><br><span class="line">	x.width++;</span><br><span class="line">	x.height++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>友元并不是类的成员，其声明不受访问控制符的影响（放在public还是private都可以），听说不能把私有成员函数声明为友元，我试了一下可以（？</p>
<h4 id="声明与定义"><a href="#声明与定义" class="headerlink" title="声明与定义"></a>声明与定义</h4><p>最好将声明和实现分开，不然问题会很多…（</p>
<p>…</p>
<p>一言以蔽之：在使用某东西之前需要先定义它；在声明用到某个东西之前需要先声明它</p>
<p>…</p>
<p>声明可以简单地在最上面进行，定义则必须完全写出这一函数&#x2F;类的实现</p>
<p>所以最好的方法是将声明与实现分开</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span>;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> &#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span></span></span><br><span class="line"><span class="function">	<span class="title">B</span><span class="params">(<span class="type">int</span> t)</span> : numb(t) &#123;</span>&#125;</span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">getA</span><span class="params">(A a)</span></span>;<span class="comment">//这里的声明用到了A而A还没有声明过，因此需要在最上面加一行声明；</span></span><br><span class="line">	<span class="type">int</span> numb;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">A</span>(<span class="type">int</span> t) : <span class="built_in">numa</span>(t) &#123;&#125;</span><br><span class="line">	<span class="function"><span class="type">int</span> <span class="title">getB</span><span class="params">(B b)</span></span>;</span><br><span class="line">	<span class="type">int</span> numa;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">B::getA</span><span class="params">(A a)</span> </span>&#123;<span class="comment">//实现分离，若两个函数都放在声明处实现，则必有一个类后定义，无法完成；</span></span><br><span class="line">	<span class="keyword">return</span> a.numa;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">A::getB</span><span class="params">(B b)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> b.numb;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<hr>
<h3 id="模板和泛型"><a href="#模板和泛型" class="headerlink" title="模板和泛型"></a>模板和泛型</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">compare</span><span class="params">(<span class="type">const</span> T &amp;a, <span class="type">const</span> T &amp;b)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a &lt; b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>模板原理：</p>
<p>编译时遇到模板定义，并不生成代码；</p>
<p>在调用一个函数模板处，编译器会根据<strong>实参</strong>自动生成一个新的函数，通常称为模板的实例。</p>
<p>模板也可以带有一些非类型的参数：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="type">unsigned</span> N, <span class="type">unsigned</span> M&gt;</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">compare</span><span class="params">(<span class="type">const</span> <span class="type">char</span> a[N], <span class="type">const</span> <span class="type">char</span> b[M])</span></span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">compare</span>(<span class="string">&quot;hello&quot;</span>, <span class="string">&quot;world&quot;</span>); <span class="comment">//调用，同样根据实参推断</span></span><br></pre></td></tr></table></figure>

<p>所以，<strong>模板参数列表不能为空</strong></p>
<p>由于实例化的时候编译器需要掌握模板函数的完整定义，而不是像其他函数那样仅需要声明，所以模板必须将<strong>定义和声明写在一个文件中</strong>。</p>
<h4 id="模板类"><a href="#模板类" class="headerlink" title="模板类"></a>模板类</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="comment">//也可以用typename，基本没有区别，typename一定能用</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Array</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line">Array&lt;<span class="type">int</span>&gt; a;<span class="comment">//之后，Array永远要跟着一个&lt;&gt;</span></span><br></pre></td></tr></table></figure>

<p>同样，在创建和使用类对象时需要掌握完全定义，故模板类成员的定义必须和声明<strong>必须在同一个文件中</strong>。</p>
<p>可以分离，定义时语法如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="type">int</span> Array&lt;T&gt;::<span class="built_in">front</span>()&#123;...&#125;</span><br></pre></td></tr></table></figure>

<p>仅在使用到一个成员函数时，才会进行这个成员函数的实例化；</p>
<p>故即使某种类型不能完全符合所有成员函数的操作，也可以正常使用其他成员函数。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="comment">//也可以用typename，基本没有区别，typename一定能用</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Array</span> &#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> Y&gt;</span></span><br><span class="line"><span class="function">    <span class="keyword">friend</span> <span class="type">void</span> <span class="title">printArray</span><span class="params">(<span class="type">const</span> Array&lt;Y&gt; &amp;a)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意区分 <code>template</code> 的作用范围，友元函数这里用到的是另一个类型了，需要另写一个。</p>
<hr>
<h3 id="内联函数"><a href="#内联函数" class="headerlink" title="内联函数"></a>内联函数</h3><p>在函数的<strong>定义</strong>前加上<code>inline</code>可以让这个函数在编译时就“展开”到调用它的地方，让执行时效率更高</p>
<p><code>inline</code>是一种“用于实现的关键字“，必须和定义写在一起，放在声明旁边没有用</p>
<p>类中定义的成员函数默认为 <code>inline</code></p>
<hr>
<h3 id="Lamda表达式"><a href="#Lamda表达式" class="headerlink" title="Lamda表达式"></a>Lamda表达式</h3><p>Lamda表达式可以造一个临时的匿名函数，省去了声明的麻烦，而且在用完后立刻销毁，非常绿色；</p>
<p>语法：<code>[捕获参数]（参数列表）修饰符-&gt;返回类型&#123;函数体&#125;</code></p>
<ul>
<li>捕获参数：可以获取这个函数所在处的一些局部变量来用，例如：<ul>
<li><code>[var]</code> 按值传递方式获取 var 变量</li>
<li><code>[&amp;var]</code> 按引用传递方式获取 var 变量</li>
<li><code>[=]</code> 按值传递方式获取局部所有变量，<code>[&amp;]</code> 按引用传递获取局部所有变量</li>
<li><code>[=, &amp;var]</code> var按引用传递，其他所有按值传递</li>
<li>其他同理</li>
</ul>
</li>
<li>参数列表：就是参数列表，若无可以和 () 一起省略</li>
<li>修饰符：捕获参数时默认为const，加mutable修饰符可以取消这个const</li>
<li>返回类型：如果函数体只有一次return或返回类型为void，可以连<code>-&gt;</code>一同省略</li>
</ul>
<p>Lamda表达式常用于<code>sort</code>等STL方法时，方便地自定义一个临时函数</p>
<hr>
<h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><hr>
<h4 id="typedef、decltype和auto"><a href="#typedef、decltype和auto" class="headerlink" title="typedef、decltype和auto"></a>typedef、decltype和auto</h4><p>有几个需要注意的点：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="type">double</span> aaa, *bbb;</span><br><span class="line"><span class="comment">//对bbb的理解：其实是typedef double* bbb;</span></span><br><span class="line"><span class="comment">//bbb p &lt;=&gt; double *p</span></span><br><span class="line"><span class="keyword">using</span> bbb = <span class="type">double</span>*;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">decltype</span>(x) p;<span class="comment">//根据x的类型推断出一个类型；</span></span><br><span class="line"><span class="comment">//分两种情况：若x是一个变量，则推断出变量本身的类型；</span></span><br><span class="line"><span class="comment">//若x是表达式，则推断出表达式本身类型之后，再根据这个表达式能否作为左值，决定是否保留引用；</span></span><br><span class="line"><span class="type">int</span> x = <span class="number">3</span>;</span><br><span class="line"><span class="type">int</span> &amp;y = x;</span><br><span class="line"><span class="keyword">decltype</span>(x) <span class="comment">//推断为int</span></span><br><span class="line"><span class="keyword">decltype</span>(&amp;x) <span class="comment">// 推断为int*</span></span><br><span class="line"><span class="keyword">decltype</span>((x)) <span class="comment">//因为是表达式，而(x)是左值，所以被推断为了int&amp;，最特殊的情况</span></span><br><span class="line"><span class="keyword">decltype</span>(y) <span class="comment">//推断为int&amp;</span></span><br><span class="line"><span class="keyword">decltype</span>(x*<span class="number">1.5</span>) <span class="comment">// 推断为double（经测试，占用了8个字节）</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//decltype和auto都可以直接加上const修饰符</span></span><br><span class="line"><span class="type">const</span> <span class="keyword">auto</span> t = <span class="number">32</span>;</span><br></pre></td></tr></table></figure>



<h4 id="函数的默认参数"><a href="#函数的默认参数" class="headerlink" title="函数的默认参数"></a>函数的默认参数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y, <span class="type">int</span> z, <span class="type">double</span> d = <span class="number">2.0</span>, <span class="type">int</span> f = <span class="number">3</span>)</span></span>&#123;</span><br><span class="line">    <span class="comment">//可以使用默认的参数，在调用时可以不填写这一参数；</span></span><br><span class="line">    <span class="comment">//默认参数必须都在最后面（即任意默认参数后不能再有非默认参数</span></span><br><span class="line">    <span class="comment">//这样调用时非默认参数依然是按顺序对应的</span></span><br><span class="line">    <span class="comment">//注意：默认参数和定义写在一起，单独声明时不管</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="函数指针"><a href="#函数指针" class="headerlink" title="函数指针"></a>函数指针</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="built_in">int</span> (*p)(<span class="type">int</span> x, <span class="type">int</span> y) = add;<span class="comment">//这个函数指针可以指向任意参数相同，返回值相同的函数；</span></span><br><span class="line">	cout &lt;&lt; <span class="built_in">p</span>(<span class="number">2</span>, <span class="number">3</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;<span class="comment">//直接用，结果是5</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>关于在类的成员函数内可以随意调用这个类（即使不是同一对象）的私有成员：</p>
<p>可以从作用域的角度来理解，私有成员的作用域仅在成员函数当中，类本身就是一个域。</p>
<h4 id="重载cin，-cout"><a href="#重载cin，-cout" class="headerlink" title="重载cin， cout"></a>重载cin， cout</h4><p>这两个东西的类型分别是<code>istream</code>和<code>ostream</code>，它们都是类；</p>
<p>这两种类的基本操作是 <code>&gt;&gt; &lt;&lt;</code> 两种运算符，返回值类型是其本身的引用，原理大概这样：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">istream&amp; <span class="keyword">operator</span> &gt;&gt;(<span class="type">int</span> &amp;x) &#123;</span><br><span class="line">	<span class="comment">//读取x</span></span><br><span class="line">	<span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//当然这不是可以运行的代码，只是大概展示原理</span></span><br></pre></td></tr></table></figure>

<p>运算后返回了cin本身，所以可以连着&gt;&gt;；</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//试图用一般类的方式重载，失败</span></span><br><span class="line">istream&amp; istream::<span class="keyword">operator</span> &gt;&gt;(<span class="type">int</span>&amp; x) &#123;</span><br><span class="line">	<span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line">	<span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//用全局变量的方式重载，成功</span></span><br><span class="line">istream&amp; <span class="keyword">operator</span> &gt;(istream&amp; f, <span class="type">int</span>&amp; x) &#123;</span><br><span class="line">	<span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line">	<span class="keyword">return</span> f;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>需要注意的是：传参和返回都一定要是引用类型，这两个类本身不支持传值拷贝</p>
<h4 id="C-风格的强制转换"><a href="#C-风格的强制转换" class="headerlink" title="C++风格的强制转换"></a>C++风格的强制转换</h4><p><code>static_cast&lt;类型&gt;(表达式)</code> C++风格的一般强制转换，暂且当做C语言的强制转换替代来用</p>
<p><code>const_cast&lt;类型&gt;(指针或引用)</code> 可以取消指针或引用中的底层常量性。也就是说，本来这个指针认为，它指向的内容是不可更改的，现在告诉它可以更改了。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> *p = &amp;a;<span class="comment">//定义指向常量的指针，指向了一个变量，指针也将其视为常量，不可通过*p更改</span></span><br><span class="line"><span class="type">int</span> *q = <span class="built_in">const_cast</span>&lt;<span class="type">int</span> *&gt;(p);<span class="comment">//现在指针认为它指向的不是常量了</span></span><br><span class="line">*q = <span class="number">4</span>;<span class="comment">//可行</span></span><br></pre></td></tr></table></figure>

<p><code>dynamic_cast&lt;类型&gt;(表达式)</code> 带有安全检查的强制转换，不能用于内置类型；</p>
<p>将子类转换为父类时，由于本身就是安全的，<code>dynamic_cast</code> 同 <code>static_cast</code> 一样；</p>
<p>将父类转换为子类时，<code>dynamic_cast</code> 会在转换失败时返回NULL，比 <code>static_cast</code> 更安全</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>面向对象</tag>
      </tags>
  </entry>
  <entry>
    <title>Educational Codeforces Round 117 A-E</title>
    <url>/2021/11/22/2021-11-22-Educational%20Codeforces%20Round%20117/</url>
    <content><![CDATA[<h4 id="A-Distance"><a href="#A-Distance" class="headerlink" title="A. Distance"></a>A. Distance</h4><p>题意：给出 $B$ 点坐标 $bx,by$，要输出 $C$ 的坐标使得 $C$ 到原点，$C$ 到 $B$ 点的曼哈顿距离都为 $B$ 到原点的曼哈顿距离的一半。</p>
<p>分奇偶讨论即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> x, y;</span><br><span class="line">    cin &gt;&gt; x &gt;&gt; y;</span><br><span class="line">    <span class="type">int</span> dis =(<span class="built_in">abs</span>(x) + <span class="built_in">abs</span>(y));</span><br><span class="line">    <span class="keyword">if</span>(dis % <span class="number">2</span> == <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">abs</span>(x) % <span class="number">2</span> == <span class="number">0</span>)&#123;</span><br><span class="line">            cout &lt;&lt; x/<span class="number">2</span> &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; y/<span class="number">2</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            cout &lt;&lt; <span class="built_in">floor</span>(x/<span class="number">2.0</span>) &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; <span class="built_in">ceil</span>(y/<span class="number">2.0</span>) &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;-1 -1\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="B-Special-Permutation"><a href="#B-Special-Permutation" class="headerlink" title="B. Special Permutation"></a>B. Special Permutation</h4><p>题意：要构造一个 $n$ 的排列（$n$ 为偶数），使得左半边最小值为 $a$，右半边最小值为 $b$</p>
<p>左边取 $a$ 和尽可能大的值，右边取 $b$ 和尽可能小的值，因为 $n$ 只给了100，可以 $n^2$ 暴力，省去了分类讨论的麻烦</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m, s[maxn];</span><br><span class="line"><span class="type">bool</span> vis[maxn];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> a, b;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; a &gt;&gt; b;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) vis[i] = <span class="number">0</span>;</span><br><span class="line">    s[<span class="number">1</span>] = a; vis[a] = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">2</span>;i&lt;=n/<span class="number">2</span>;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=n;j&gt;=a;j--)&#123;</span><br><span class="line">            <span class="keyword">if</span>(!vis[j] &amp;&amp; j != b)&#123;</span><br><span class="line">                s[i] = j;</span><br><span class="line">                vis[j] = <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(j == a)&#123;</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=n/<span class="number">2</span>+<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=b;j&gt;=<span class="number">1</span>;j--)&#123;</span><br><span class="line">            <span class="keyword">if</span>(!vis[j])&#123;</span><br><span class="line">                s[i] = j;</span><br><span class="line">                vis[j] = <span class="number">1</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(j == <span class="number">1</span>)&#123;</span><br><span class="line">                cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++) cout &lt;&lt; s[i] &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">    cout &lt;&lt; s[n] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="C-Chat-Ban"><a href="#C-Chat-Ban" class="headerlink" title="C. Chat Ban"></a>C. Chat Ban</h4><p>题意：你要依次发送 $2k-1$ 条消息，每条消息的字符个数分别为 1,2,3…k,k-1…2,1，输入达到 $x$ 个字符时就会终止，问能发出几条消息（发一半也算）</p>
<p>分类讨论写比较麻烦，写一个计算发送前 $x$ 条消息需要的字符的函数 $f(x)$，然后二分答案即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ll k, x;</span><br><span class="line"><span class="function">ll <span class="title">cntk</span><span class="params">(ll s)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(s &lt;= k) <span class="keyword">return</span> s*(s+<span class="number">1</span>)/<span class="number">2</span>;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        ll r = (<span class="number">2</span>*k<span class="number">-1</span>-s+k) * (s-k) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">return</span> k*(k+<span class="number">1</span>)/<span class="number">2</span> + r;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; k &gt;&gt; x;</span><br><span class="line">    ll l = <span class="number">1</span>, r = <span class="number">2</span>*k<span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">while</span>(l &lt; r)&#123;</span><br><span class="line">        ll mid = (l+r) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">cntk</span>(mid) &gt;= x)&#123;</span><br><span class="line">            r = mid;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            l = mid+<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; l &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D-X-Magic-Pair"><a href="#D-X-Magic-Pair" class="headerlink" title="D. X-Magic Pair"></a>D. X-Magic Pair</h4><p>题意：有两个数字 $a,b$，可以一次操作将 $a&#x3D;|a-b|$ 或 $b&#x3D;|a-b|$，问能否凑出 $x$（$a$ 或 $b$ 等于 $x$ 都可，可以进行任意次操作）</p>
<p>用手模拟一下可以发现，当前为 $a,b$（假设 $a\lt b$），不论怎么操作一定会经过 $a,b-a$ 这个状态</p>
<p>那么可以用类似辗转相除法的办法处理，如果 $x$ 在 $a,b$ 之间且 $x\equiv b\pmod a$，则可行，否则变化为 $b%a,a$ 继续；</p>
<p>记得判余数为 $0$ 的情况</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">judge</span><span class="params">(ll a, ll b, ll x)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(a == <span class="number">0</span> || b == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span>(x == a || x == b) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(x &gt; b) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span>(b%a == x%a) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">judge</span>(b%a, a, x);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ll a, b, x;</span><br><span class="line">    string sout[<span class="number">2</span>] = &#123;<span class="string">&quot;NO&quot;</span>, <span class="string">&quot;YES&quot;</span>&#125;;</span><br><span class="line">    cin &gt;&gt; a &gt;&gt; b &gt;&gt; x;</span><br><span class="line">    <span class="keyword">if</span>(a &gt; b) <span class="built_in">swap</span>(a, b);</span><br><span class="line">    cout &lt;&lt; sout[<span class="built_in">judge</span>(a, b, x)] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="E-Messages"><a href="#E-Messages" class="headerlink" title="E. Messages"></a>E. Messages</h4><p>题意：有 $n$ 个人，每个人有一个期望读到的信 $m_i$，和他将读的信数 $k_i$，每个人都会在信堆里等概率地随机抽取 $k_i$ 封信阅读（不会取走）。现在你可以选择将哪些信放进信堆，使得读到想读信的人数期望最大。</p>
<p>第一反应想到了二分答案，因为确定了要放信的数量后比较容易judge</p>
<hr>
<p>考虑怎么计算：选择一共放 $m$ 封信时的最大期望</p>
<p>对于每个人来说，它读到想要的信的概率是 $\frac{min(k_i, m)}{m}$，我们可以对于每一封信，统计想读它的人的 $\min(k_i, m)$ 之和（记为 $tot$），那么选这封信的收益就是 $\frac{tot}{m}$ ；</p>
<p>将信按照 $tot$ 排序后选取前 $m$ 封就可以计算出最大期望了</p>
<hr>
<p>然后很显然地发现，答案不具有单调性，二份答案X</p>
<p>输入数据保证了 $k\le20$，用手模拟了一下极端情况，大胆猜测选 $20$ 封左右信时收益达到阈值，再多选收益一定更低</p>
<p>然后暴力跑了 $m\le50$ 的情况，就过了！</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="type">int</span> a[maxn], b[maxn];</span><br><span class="line"><span class="type">bool</span> apr[maxn];</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; all;</span><br><span class="line">ll tot[maxn];</span><br><span class="line"><span class="type">double</span> ans[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">cmpf</span><span class="params">(ll x, ll y)</span></span>&#123; <span class="keyword">return</span> x &gt; y;&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">judge</span><span class="params">(<span class="type">int</span> m)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        tot[a[i]] += <span class="built_in">min</span>(b[i], m);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">sort</span>(tot+<span class="number">1</span>, tot+<span class="number">200000</span>+<span class="number">1</span>, cmpf);</span><br><span class="line">    ll fz = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)&#123;</span><br><span class="line">        fz += tot[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> i = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(tot[i] &gt; <span class="number">0</span>) tot[i++] = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> fz * <span class="number">1.0</span> / m;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; a[i] &gt;&gt; b[i];</span><br><span class="line">        <span class="keyword">if</span>(!apr[a[i]])&#123;</span><br><span class="line">            all.<span class="built_in">push_back</span>(a[i]);</span><br><span class="line">            apr[a[i]] = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> p = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="built_in">min</span>(<span class="number">50</span>, n);i++)&#123;</span><br><span class="line">        ans[i] = <span class="built_in">judge</span>(i);</span><br><span class="line">        <span class="keyword">if</span>(ans[i] &gt; ans[p]) p = i;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; p &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) tot[a[i]] += <span class="built_in">min</span>(b[i], p);</span><br><span class="line">    vector&lt;pair&lt;<span class="type">int</span>, <span class="type">int</span>&gt;&gt; vp;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> v:all) vp.<span class="built_in">push_back</span>(&#123;tot[v], v&#125;);</span><br><span class="line">    <span class="built_in">sort</span>(vp.<span class="built_in">begin</span>(), vp.<span class="built_in">end</span>());</span><br><span class="line">    <span class="type">bool</span> fg = <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">while</span>(p--)&#123;</span><br><span class="line">        <span class="keyword">if</span>(fg) cout &lt;&lt; <span class="string">&#x27; &#x27;</span>;</span><br><span class="line">        <span class="keyword">else</span> fg = <span class="number">1</span>;</span><br><span class="line">        cout &lt;&lt; vp.<span class="built_in">back</span>().second;</span><br><span class="line">        vp.<span class="built_in">pop_back</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>新生赛题解之chiking的俄罗斯方块</title>
    <url>/2021/12/10/2021-12-10-%E6%96%B0%E7%94%9F%E8%B5%9B%E9%A2%98%E8%A7%A3-%E4%BF%84%E7%BD%97%E6%96%AF%E6%96%B9%E5%9D%97/</url>
    <content><![CDATA[<h2 id="XX的俄罗斯方块"><a href="#XX的俄罗斯方块" class="headerlink" title="XX的俄罗斯方块"></a>XX的俄罗斯方块</h2><h6 id="时间限制：1000ms-空间限制：256M"><a href="#时间限制：1000ms-空间限制：256M" class="headerlink" title="时间限制：1000ms  空间限制：256M"></a>时间限制：1000ms  空间限制：256M</h6><h5 id="题目背景"><a href="#题目背景" class="headerlink" title="题目背景:"></a>题目背景:</h5><p>XX沉迷一款叫俄罗斯方块的游戏，但这个游戏对他来说实在太难了，于是它决定玩无敌版</p>
<p>无敌版俄罗斯方块中，有一个宽4格，高度无限的网格图，游戏流程如下：</p>
<ol>
<li><p>初始时网格图为空</p>
</li>
<li><p>一个由方块组成的砖块（只有1x4和2x2两种）出现在网格图内很高的地方，XX可以对它进行任意次操作：</p>
<ol>
<li>将这个方块整体左移一格（不能被已有的方块阻挡或超出边界）</li>
<li>将这个方块整体右移一格（不能被已有的方块阻挡或超出边界）</li>
</ol>
</li>
<li><p>砖块块每隔一段时间就会整体下落一格（当然由于XX的手速非常快，它可以在下落的间隙中进行无数次平移），当因被阻挡或即将超出下边界而无法下落时，它就会<strong>固定</strong>，紧接着出现下一个砖块</p>
</li>
<li><p>方块固定后，如果有一整行的四格方块都填满，这一行将被消除，XX得到1分；如果一次同时消除了多行，第二行将能得到2分，第三行就是3分，以此类推。</p>
<p>所以一次性消除4行就能获得一共10分！</p>
</li>
</ol>
<p><strong>注意</strong>：砖块不可分割，不可旋转，所有1x4的砖块均竖直，<strong>且保证1x4的方块仅出现偶数次</strong></p>
<p>下面这种插入方式也是允许的：</p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/%E4%BF%84%E7%BD%97%E6%96%AF%E6%96%B9%E5%9D%97/9.png" alt="9"></p>
<h5 id="题目描述："><a href="#题目描述：" class="headerlink" title="题目描述："></a>题目描述：</h5><p>XX现在正在打一局无敌版的俄罗斯方块，他知道接下来出现的 n 个砖块的种类，你能帮他算一算他最高能获得多少分吗？</p>
<h5 id="输入格式："><a href="#输入格式：" class="headerlink" title="输入格式："></a>输入格式：</h5><p>输入有两行</p>
<p>第一行一个正整数 n，表示接下来有 n 个砖块 (1&lt;&#x3D; n &lt;&#x3D; 10^6)</p>
<p>第二行 n 个正整数，依次表示接下来出现的砖块种类，0表示 2x2 的砖块，1表示 1x4 的砖块</p>
<h5 id="输出格式："><a href="#输出格式：" class="headerlink" title="输出格式："></a>输出格式：</h5><p>输出一个正整数，为XX能获得的最大分数</p>
<h5 id="样例输入："><a href="#样例输入：" class="headerlink" title="样例输入："></a>样例输入：</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">4</span><br><span class="line">0 1 1 0</span><br></pre></td></tr></table></figure>

<h5 id="样例输出："><a href="#样例输出：" class="headerlink" title="样例输出："></a>样例输出：</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">6</span><br></pre></td></tr></table></figure>

<p><strong>样例解释：</strong></p>
<p>按下面这种方式堆放：</p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/%E4%BF%84%E7%BD%97%E6%96%AF%E6%96%B9%E5%9D%97/10.png" alt="10"></p>
<p>第三个插入时连消了两行，获得1+2分</p>
<p>第五个插入时连消了两行，获得1+2分</p>
<p>一共6分，不存在比这得分更多的方案</p>
<hr>
<h2 id="题解："><a href="#题解：" class="headerlink" title="题解："></a>题解：</h2><p>先说结论：</p>
<p>用任何方式算出最大四连消的次数即可，其余尽可能多地三连消，最后可能剩余一个 $2\times2$ 或者 $2\times4$，可以发现一点：只要存在正方形的砖块，最后就不可能剩下 $2\times4$ 的情况。</p>
<hr>
<p>首先可以看出，只有2连消和4连消两种消法，分别对应3分和10分；</p>
<p>从贪心的角度来讲，我们需要尽可能多4连消；随便定一个数字 $k$，考虑怎么去判断<strong>能否进行 $k$ 次4连消</strong>：</p>
<ul>
<li>$k$ 至多为1的数量的一半；</li>
<li>先在第四列堆 $k$ 根1x4，在前两列尽可能堆高，最后 $k$ 根长条都插入第三列，这样能尽可能保证4连消；</li>
<li>在这种策略下，如果在第三列每根1*4插入时，前两列高度都 $\ge4$，则能够进行 $k$ 次4连消，否则一定不能；</li>
</ul>
<p>所以进行 $k$ 次4连消的条件就是：对于任意倒数第 $b$ 个1 ($b\le k$)，都满足到这里为止，前面可用的1和0在前两列能够堆的高度 $\ge$ $(k-b+1) \times4$，所谓“可用的0和1”就是指所有的0和去掉放在第四列的 $k$ 个和第三列的 $k-b+1$ 个1；</p>
<p>即对于任意倒数第 $b$ 个1（$b\le k$），假设它在第 $i$ 位，满足 $(cnt1[i]-k-(k-b+1))&#x2F;2\times4 + cnt0[i]\times2\ge(k-b+1)\times4$</p>
<p> 其中 $cnt0[i]$，$cnt1[i]$ 分别为前 $i$ 个块中0，1的数量；（式中除号为整除）</p>
<p>有了这个式子，最大的 $k$ 就不难算了：初始将 $k$ 设为 $cnt1[n]&#x2F;2$，我们从后往前枚举，每枚举到一个1时就依据上式判断，不断减小 $k$ 直到上式成立，这样就得到了最大的 $k$。</p>
<hr>
<p>有必要解释一下：$x$ 个 1可以在前两列堆出 $x&#x2F;2\times4$ 的高度：（式中除号为整除）</p>
<p><img src="https://raw.githubusercontent.com/lxtyin/lxtyin.github.io/main/_posts/%E4%BF%84%E7%BD%97%E6%96%AF%E6%96%B9%E5%9D%97/6.png" alt="2"></p>
<p>当插入一根1x4时，直接放在第一列，接下来如果是方块则正常放置不影响，下一根1x4用这种插入的方式补在第二列即可，因为直到最后 $k$ 个1之前第三列都是空的，这种方式一定行的通。</p>
<hr>
<p>一二列堆的多余部分要尽可能2连消，这个比较简单，堆的高度超出 $k\times4$ 后左右来回放就行了，总得分就是 $k\times4+(n-k)&#x2F;2\times3$，最后可能会剩余1个2x2，这个没有办法消除；（式中除号为整除）</p>
<p>最后也有可能不得不剩下两根1x4，这种情况当且仅当全场都是1的时候才成立，否则一定能通过改变一个0的位置消掉（感性理解即可，想要严格证明可以私聊我，不太好讲）</p>
<p>因为 $k$ 是最大的4连消次数，在此情况下也最多只会浪费两个方块，减少4连消次数损失的10收益远大于可能获得的3收益，所以这就是得分最大的方案。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, a[maxn];</span><br><span class="line"><span class="type">int</span> cnt0[maxn], cnt1[maxn];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cnt0[i] = cnt0[i<span class="number">-1</span>];</span><br><span class="line">        cnt1[i] = cnt1[i<span class="number">-1</span>];</span><br><span class="line">        <span class="keyword">if</span>(a[i] == <span class="number">0</span>) cnt0[i]++;</span><br><span class="line">        <span class="keyword">else</span> cnt1[i]++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> k = cnt1[n] / <span class="number">2</span>;</span><br><span class="line">    <span class="type">int</span> brk = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=n;i&gt;=<span class="number">1</span>;i--)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i] == <span class="number">1</span>)&#123;</span><br><span class="line">            brk++;</span><br><span class="line">            <span class="keyword">while</span>(k &gt;= brk &amp;&amp; (cnt1[i]-(k+k-brk+<span class="number">1</span>))/<span class="number">2</span>*<span class="number">4</span> + cnt0[i]*<span class="number">2</span> &lt; (k-brk+<span class="number">1</span>)*<span class="number">4</span>) k--;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(cnt0[n]) cout &lt;&lt; k*<span class="number">10</span>+(n-k*<span class="number">4</span>)/<span class="number">2</span>*<span class="number">3</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="keyword">else</span> cout &lt;&lt; n/<span class="number">4</span>*<span class="number">10</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="PS：由于这题最终答案偏差不大，其他贪心方式也有机会通过（"><a href="#PS：由于这题最终答案偏差不大，其他贪心方式也有机会通过（" class="headerlink" title="PS：由于这题最终答案偏差不大，其他贪心方式也有机会通过（"></a>PS：由于这题最终答案偏差不大，其他贪心方式也有机会通过（</h3><p>来自老老会长的ac代码：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="type">int</span> n;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">data</span><span class="params">(n)</span>, lPos</span>;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">bool</span>&gt; <span class="title">flag</span><span class="params">(n, <span class="literal">true</span>)</span></span>;</span><br><span class="line">    queue&lt;<span class="type">int</span>&gt; oPos;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) cin &gt;&gt; data[i];</span><br><span class="line">    <span class="type">int</span> res = <span class="number">0</span>, tot2 = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 2*2 + 2*2 + 1*4 + 1*4</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; ++i) &#123;</span><br><span class="line">        <span class="keyword">if</span> (data[i] == <span class="number">0</span>) &#123;</span><br><span class="line">            oPos.<span class="built_in">push</span>(i);</span><br><span class="line">            tot2++;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> ((<span class="type">int</span>) oPos.<span class="built_in">size</span>() &gt;= <span class="number">2</span> &amp;&amp; (<span class="type">int</span>) lPos.<span class="built_in">size</span>() &gt;= <span class="number">1</span>) &#123;</span><br><span class="line">                flag[oPos.<span class="built_in">front</span>()] = <span class="literal">false</span>;</span><br><span class="line">                oPos.<span class="built_in">pop</span>();</span><br><span class="line">                flag[oPos.<span class="built_in">front</span>()] = <span class="literal">false</span>;</span><br><span class="line">                oPos.<span class="built_in">pop</span>();</span><br><span class="line">                flag[lPos.<span class="built_in">back</span>()] = <span class="literal">false</span>;</span><br><span class="line">                flag[i] = <span class="literal">false</span>;</span><br><span class="line">                lPos.<span class="built_in">pop_back</span>();</span><br><span class="line">                res += <span class="number">10</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                lPos.<span class="built_in">push_back</span>(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> cnt1 = (<span class="type">int</span>) lPos.<span class="built_in">size</span>();</span><br><span class="line">    <span class="type">int</span> cnt2 = (<span class="type">int</span>) oPos.<span class="built_in">size</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1*4 + 1*4 + 1*4 + 1*4</span></span><br><span class="line">    <span class="type">int</span> tmp = cnt1 / <span class="number">4</span>;</span><br><span class="line">    res += tmp * <span class="number">10</span>;</span><br><span class="line">    cnt1 -= tmp * <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1*4 + 1*4 + 2*2 + 2*2</span></span><br><span class="line">    <span class="keyword">if</span> (cnt1 == <span class="number">2</span> &amp;&amp; cnt2 &gt;= <span class="number">2</span>) &#123;</span><br><span class="line">        res += <span class="number">6</span>;</span><br><span class="line">        cnt2 -= <span class="number">2</span>;</span><br><span class="line">        cnt1 -= <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2*2 + 2*2</span></span><br><span class="line">    res += <span class="number">3</span> * (cnt2 / <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (cnt1 == <span class="number">2</span> &amp;&amp; (cnt2 == <span class="number">1</span> || tot2 &gt;= <span class="number">2</span>)) &#123;</span><br><span class="line">        res += <span class="number">3</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    cout &lt;&lt; res &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>新生赛</tag>
      </tags>
  </entry>
  <entry>
    <title>初探 Socket</title>
    <url>/2022/01/17/2022-01-17-Socket%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<hr>
<p>起因是寒假没事干搞了个70块一年的云服务器玩玩，试图整一个联机游戏demo</p>
<p>感谢大佬：<a href="https://www.cnblogs.com/dolphinX/p/3460545.html">https://www.cnblogs.com/dolphinX/p/3460545.html</a></p>
<h4 id="socket是什么"><a href="#socket是什么" class="headerlink" title="socket是什么"></a>socket是什么</h4><p><a href="https://www.bilibili.com/video/BV1eg411G7pW?from=search&amp;seid=14838178887423899974&amp;spm_id_from=333.337.0.0">https://www.bilibili.com/video/BV1eg411G7pW?from=search&amp;seid=14838178887423899974&amp;spm_id_from=333.337.0.0</a></p>
<p>这里以TCP为例</p>
<h4 id="socket通信基本流程"><a href="#socket通信基本流程" class="headerlink" title="socket通信基本流程"></a>socket通信基本流程</h4><p><img src="https://images0.cnblogs.com/blog/349217/201312/05232335-fb19fc7527e944d4845ef40831da4ec2.png"></p>
<p>通信基本流程如上图，在accept之后，客户端和服务端双方都获得了连接对方的Socket，随后即可不断地进行点对点地Send和Receive</p>
<h4 id="通信demo"><a href="#通信demo" class="headerlink" title="通信demo"></a>通信demo</h4><p>以Windows下，C++为例</p>
<p>顺序即实际流程顺序：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">socket</span><span class="params">(<span class="type">int</span> domain, <span class="type">int</span> type, <span class="type">int</span> protocol)</span></span>;</span><br><span class="line"><span class="comment">//客户端和服务端均创建Socket，返回它的标识符，后续用这个int来代表这一socket</span></span><br><span class="line"><span class="comment">//domain： 协议族，常用AF_INET表示使用IPv4</span></span><br><span class="line"><span class="comment">//type：socket类型，常用SOCK_STREAM</span></span><br><span class="line"><span class="comment">//protocol：所用协议，常用IPPROTO_TCP</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">bind</span><span class="params">(SOCKET sockfd, <span class="type">const</span> <span class="keyword">struct</span> sockaddr *addr, <span class="type">socklen_t</span> addrlen)</span></span>;</span><br><span class="line"><span class="comment">//服务端，绑定一个端口，返回错误信号</span></span><br><span class="line"><span class="comment">//用法见下，其中slisten服务端的socket标识符：</span></span><br><span class="line">sockaddr_in sin; <span class="comment">//这是一个地址对象</span></span><br><span class="line">sin.sin_family = AF_INET;</span><br><span class="line">sin.sin_port = <span class="built_in">htons</span>(<span class="number">8888</span>);<span class="comment">//绑定的端口，要改的只有这里</span></span><br><span class="line">sin.sin_addr.S_un.S_addr = INADDR_ANY;</span><br><span class="line"><span class="keyword">if</span>(<span class="built_in">bind</span>(slisten, (LPSOCKADDR)&amp;sin, <span class="built_in">sizeof</span>(sin)) == SOCKET_ERROR) &#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;bind error !\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">listen</span><span class="params">(SOCKET sockfd, <span class="type">int</span> backlog)</span></span>;</span><br><span class="line"><span class="comment">//服务端，开始监听，返回错误信号（这是开启监听的命令，不是阻塞方法）</span></span><br><span class="line"><span class="comment">//backlog：最大可排队连接数</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">connect</span><span class="params">(SOCKET sockfd, <span class="type">const</span> <span class="keyword">struct</span> sockaddr *addr, <span class="type">socklen_t</span> addrlen)</span></span>;</span><br><span class="line"><span class="comment">//客户端：连接服务端，返回错误信号</span></span><br><span class="line"><span class="comment">//用法如下，其中sclient为客户端的socket标识符：</span></span><br><span class="line">sockaddr_in serAddr;</span><br><span class="line">serAddr.sin_family = AF_INET;</span><br><span class="line">serAddr.sin_port = <span class="built_in">htons</span>(<span class="number">8888</span>);<span class="comment">//端口</span></span><br><span class="line">serAddr.sin_addr.S_un.S_addr = <span class="built_in">inet_addr</span>(<span class="string">&quot;124.223.118.118&quot;</span>);<span class="comment">//IP</span></span><br><span class="line"><span class="keyword">if</span>(<span class="built_in">connect</span>(sclient, (sockaddr *)&amp;serAddr, <span class="built_in">sizeof</span>(serAddr)) == SOCKET_ERROR) &#123;</span><br><span class="line">    cerr &lt;&lt; <span class="string">&quot;connect error !\n&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">accept</span><span class="params">(SOCKET sockfd, <span class="keyword">struct</span> sockaddr *addr, <span class="type">socklen_t</span> *addrlen)</span></span>;</span><br><span class="line"><span class="comment">//服务端，阻塞方法，等待来自客户端的connect，等到后返回客户端的socket标识符</span></span><br><span class="line"><span class="comment">//到这一步，服务端知晓了客户端的SOCKET标识和IP，可以记录下来便于未来主动向客户端发送消息</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">send</span><span class="params">(SOCKET sclient, <span class="type">const</span> <span class="type">char</span> *msg, <span class="type">int</span> len, <span class="type">int</span> flags)</span></span>;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">recv</span><span class="params">(SOCKET sclient, <span class="type">char</span> *buf, <span class="type">int</span> len, <span class="type">int</span> flags)</span></span>;</span><br><span class="line"><span class="comment">//收发消息服务端和客户端均可使用，sclient处都使用accept方法得到的客户端的SOCKET标识即可</span></span><br><span class="line"><span class="comment">//这里的通信就自由了，客户度send，服务端recv，随后还可以服务端send，客户端recv进行一个回复；</span></span><br></pre></td></tr></table></figure>

<p>另一提：网络间通信，由于网络波动等，一次消息的发送或接收失败很正常，应做出错误处理而非中断程序；</p>
<p>客户端：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;WINSOCK2.H&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;cstring&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;ctime&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> comment(lib, <span class="string">&quot;ws2_32.lib&quot;</span>)</span></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">char</span> *ipv4 = <span class="string">&quot;124.223.118.118&quot;</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">unsigned</span> <span class="type">short</span> hton = <span class="number">8888</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">sendToServer</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *msg)</span></span>&#123;</span><br><span class="line">    <span class="comment">//连接</span></span><br><span class="line">    SOCKET sclient = <span class="built_in">socket</span>(AF_INET, SOCK_STREAM, IPPROTO_TCP);</span><br><span class="line">    <span class="keyword">if</span>(sclient == INVALID_SOCKET) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;invalid socket!\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    sockaddr_in serAddr;</span><br><span class="line">    serAddr.sin_family = AF_INET;</span><br><span class="line">    serAddr.sin_port = <span class="built_in">htons</span>(hton);<span class="comment">//端口</span></span><br><span class="line">    serAddr.sin_addr.S_un.S_addr = <span class="built_in">inet_addr</span>(ipv4);<span class="comment">//IP</span></span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">connect</span>(sclient, (sockaddr *)&amp;serAddr, <span class="built_in">sizeof</span>(serAddr)) == SOCKET_ERROR) &#123;</span><br><span class="line">        <span class="comment">//连接失败</span></span><br><span class="line">        cerr &lt;&lt; <span class="string">&quot;connect error !\n&quot;</span>;</span><br><span class="line">        <span class="built_in">closesocket</span>(sclient);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//send()用来将数据由指定的socket传给对方主机</span></span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">send</span>(sclient, msg, <span class="built_in">strlen</span>(msg), <span class="number">0</span>) == <span class="number">-1</span>)&#123;</span><br><span class="line">        cerr &lt;&lt; <span class="string">&quot;send failed!\n&quot;</span>;</span><br><span class="line">        <span class="built_in">closesocket</span>(sclient);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//recv()来自服务端的回信</span></span><br><span class="line">    <span class="type">char</span> recData[<span class="number">255</span>];</span><br><span class="line">    <span class="type">int</span> ret = <span class="built_in">recv</span>(sclient, recData, <span class="number">255</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span>(ret&gt;<span class="number">0</span>) &#123;</span><br><span class="line">        recData[ret] = <span class="number">0x00</span>;</span><br><span class="line">        cout &lt;&lt; recData;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">closesocket</span>(sclient);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//初始化</span></span><br><span class="line">    WORD sockVersion = <span class="built_in">MAKEWORD</span>(<span class="number">2</span>, <span class="number">2</span>);</span><br><span class="line">    WSADATA data;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">WSAStartup</span>(sockVersion, &amp;data)!=<span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(<span class="literal">true</span>) &#123;</span><br><span class="line">        string data;</span><br><span class="line">        cin &gt;&gt; data;</span><br><span class="line">        <span class="built_in">sendToServer</span>(data.<span class="built_in">c_str</span>());</span><br><span class="line">        <span class="keyword">if</span>(data == <span class="string">&quot;END&quot;</span>) <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">WSACleanup</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>服务端：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;winsock2.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> comment(lib,<span class="string">&quot;ws2_32.lib&quot;</span>)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">(<span class="type">int</span> argc, <span class="type">char</span>* argv[])</span> </span>&#123;</span><br><span class="line">    <span class="comment">//初始化WSA</span></span><br><span class="line">    WORD sockVersion = <span class="built_in">MAKEWORD</span>(<span class="number">2</span>,<span class="number">2</span>);</span><br><span class="line">    WSADATA wsaData;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">WSAStartup</span>(sockVersion, &amp;wsaData)!=<span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建套接字</span></span><br><span class="line">    SOCKET slisten = <span class="built_in">socket</span>(AF_INET, SOCK_STREAM, IPPROTO_TCP);</span><br><span class="line">    <span class="keyword">if</span>(slisten == INVALID_SOCKET) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;socket error !\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//绑定IP和端口</span></span><br><span class="line">    sockaddr_in sin;</span><br><span class="line">    sin.sin_family = AF_INET;</span><br><span class="line">    sin.sin_port = <span class="built_in">htons</span>(<span class="number">8888</span>);</span><br><span class="line">    sin.sin_addr.S_un.S_addr = INADDR_ANY;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">bind</span>(slisten, (LPSOCKADDR)&amp;sin, <span class="built_in">sizeof</span>(sin)) == SOCKET_ERROR) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;bind error !\n&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//开始监听</span></span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">listen</span>(slisten, <span class="number">5</span>) == SOCKET_ERROR) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;listen error !\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//循环接收数据</span></span><br><span class="line">    SOCKET sClient;</span><br><span class="line">    sockaddr_in remoteAddr;</span><br><span class="line">    <span class="type">int</span> nAddrlen = <span class="built_in">sizeof</span>(remoteAddr);</span><br><span class="line">    <span class="type">char</span> revData[<span class="number">255</span>];</span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Waiting...\n&quot;</span>;</span><br><span class="line">        sClient = <span class="built_in">accept</span>(slisten, (SOCKADDR *)&amp;remoteAddr, &amp;nAddrlen);</span><br><span class="line">        <span class="keyword">if</span>(sClient == INVALID_SOCKET)</span><br><span class="line">        &#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;accept error !\n&quot;</span>;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;received connect: &quot;</span> &lt;&lt;  <span class="built_in">inet_ntoa</span>(remoteAddr.sin_addr) &lt;&lt; <span class="string">&quot; \r\n&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//接收数据</span></span><br><span class="line">        <span class="type">int</span> ret = <span class="built_in">recv</span>(sClient, revData, <span class="number">255</span>, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span>(ret &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            revData[ret] = <span class="number">0x00</span>;</span><br><span class="line">            cout &lt;&lt; revData &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//发送数据</span></span><br><span class="line">        <span class="type">const</span> <span class="type">char</span> * sendData = <span class="string">&quot;how are you? \n&quot;</span>;</span><br><span class="line">        <span class="built_in">send</span>(sClient, sendData, <span class="built_in">strlen</span>(sendData), <span class="number">0</span>);</span><br><span class="line">        <span class="built_in">closesocket</span>(sClient);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">closesocket</span>(slisten);</span><br><span class="line">    <span class="built_in">WSACleanup</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>避个坑：服务端的回信不是必须的（三次握手在更加底层的地方，已经有人完成好了），此处只是为了方便查看通信结果；</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>Socket</tag>
        <tag>计算机网络</tag>
      </tags>
  </entry>
  <entry>
    <title>VHDL小记</title>
    <url>/2021/12/19/2021-12-19-VHDL%E5%B0%8F%E8%AE%B0/</url>
    <content><![CDATA[<hr>
<h5 id="基本结构"><a href="#基本结构" class="headerlink" title="基本结构"></a>基本结构</h5><figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">Library</span> ieee;</span><br><span class="line"><span class="keyword">Use</span> ieee.std_logic_1164.<span class="keyword">all</span>;    <span class="comment">--调库，调包</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">Entity</span> mux21a <span class="keyword">is</span>				<span class="comment">--实体，和文件同名</span></span><br><span class="line">    <span class="keyword">port</span>(a, b, c: <span class="keyword">in</span> <span class="built_in">std_logic</span>;	<span class="comment">--定义输入输出</span></span><br><span class="line">        d: <span class="keyword">out</span> <span class="built_in">std_logic</span>);		<span class="comment">--输出信号不可读！输入信号不可写！</span></span><br><span class="line"><span class="keyword">End</span> <span class="keyword">entity</span> mux21a;</span><br><span class="line"><span class="keyword">Architecture</span> one <span class="keyword">of</span> mux21a <span class="keyword">is</span>   <span class="comment">--结构体</span></span><br><span class="line"><span class="keyword">Begin</span></span><br><span class="line">   	...							<span class="comment">--其他语句</span></span><br><span class="line">    [名字]: <span class="keyword">Process</span> (a, b, c)			<span class="comment">--进程，结构体可以包含多个语句和进程</span></span><br><span class="line">    <span class="keyword">Begin</span></span><br><span class="line">        ...</span><br><span class="line">    <span class="keyword">End</span> <span class="keyword">process</span> [名字];</span><br><span class="line"><span class="keyword">End</span> <span class="keyword">Architecture</span> one;</span><br></pre></td></tr></table></figure>

<p>其中进程中的内容和常规编程思路一致，运行顺序同书写顺序；</p>
<p>而结构体中的其他语句是并发执行的，和书写顺序无关；</p>
<p>故顺序语句（<code>if for</code> 等等）只能出现在进程当中，并行语句（选择、条件赋值语句，元件例化语句）不能放在进程中；</p>
<p>整个 <code>Process</code> 也可看成其他语句的一部分，在括号中的内容 <code>(a, b, c)</code> 改变时进入进程</p>
<h5 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h5><p>常用顺序执行的语句：</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ... <span class="keyword">then</span> ...;</span><br><span class="line"><span class="keyword">elsif</span> ...;</span><br><span class="line"><span class="keyword">else</span> ...;</span><br><span class="line"><span class="keyword">end</span> <span class="keyword">if</span>;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">0</span> <span class="keyword">to</span> <span class="number">7</span> <span class="keyword">loop</span></span><br><span class="line">    ...;</span><br><span class="line"><span class="keyword">end</span> <span class="keyword">loop</span>;</span><br></pre></td></tr></table></figure>



<p>注意选择信号赋值语句：</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">WITH</span> 选择表达式 <span class="keyword">SELECT</span></span><br><span class="line">	赋值目标信号 &lt;= 表达式<span class="number">1</span> <span class="keyword">WHEN</span> 选择值 <span class="number">1</span>，</span><br><span class="line">                  表达式<span class="number">2</span> <span class="keyword">WHEN</span> 选择值 <span class="number">2</span>，</span><br><span class="line">                  表达式n <span class="keyword">WHEN</span> <span class="keyword">OTHERS</span>;</span><br></pre></td></tr></table></figure>

<p>是一个并行语句，不具备优先级，所以它必须指明所有可能的情况或者指定others，且它的条件必须互斥，不能重叠；</p>
<p>而条件信号赋值语句：</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line">赋值目标信号 &lt;= 表达式<span class="number">1</span> <span class="keyword">WHEN</span> 赋值条件<span class="number">1</span> <span class="keyword">ELSE</span></span><br><span class="line">			  表达式<span class="number">2</span> <span class="keyword">WHEN</span> 赋值条件<span class="number">2</span> <span class="keyword">ELSE</span></span><br><span class="line">    		  表达式n <span class="keyword">WHEN</span> 赋值条件n <span class="keyword">ELSE</span></span><br><span class="line">    		  表达式；</span><br></pre></td></tr></table></figure>

<p>也是一个并行语句，但它在赋值时按照优先级从上到下赋值，所以允许条件重叠（最后一条相当于没有条件的ELSE，保证了全覆盖）；</p>
<p>CASE语句：</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CASE</span> &lt;表达式&gt; <span class="keyword">IS</span></span><br><span class="line"><span class="keyword">WHEN</span> &lt;选择值&gt; =&gt; &lt;顺序语句&gt;; ... ; &lt;顺序语句&gt;;</span><br><span class="line"><span class="keyword">WHEN</span> &lt;选择值&gt; =&gt; &lt;顺序语句&gt;; ... ; &lt;顺序语句&gt;;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">WHEN</span> <span class="keyword">OTHERS</span> =&gt; &lt;顺序语句&gt;;</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">CASE</span>;</span><br></pre></td></tr></table></figure>

<p>是顺序语句，必须指明所有可能的情况或者指定others，条件允许重叠</p>
<h5 id="变量和信号"><a href="#变量和信号" class="headerlink" title="变量和信号"></a>变量和信号</h5><p>变量是电路内部的操作，代表临时数据，可以认为是一个局部变量；</p>
<p>信号代表的是电路单元，代表真实电路，是全局变量，不能在进程和子进程中定义；</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">Signal</span> 名称: 类型:= 初始值;</span><br><span class="line"><span class="keyword">variable</span> 名称: 类型:= 初始值;</span><br><span class="line">变量 := 赋值;</span><br><span class="line">信号 &lt;= 赋值;</span><br></pre></td></tr></table></figure>

<p>赋值时，变量是立即赋值的，而信号会延迟赋值</p>
<p>常用类型：<code>std_logic</code> 和 <code>std_logic_vector</code></p>
<p>一般把一个 <code>std_logic</code> 当成一位01信号，而事实上它可以存储9种信号（另有：’X’表示未知，’U’表示未设置，’-‘表示不关心等等），用于充分地表达一处电路线上的真实情况。</p>
<p>用行为描述方式时，两个同位的 <code>std_logic_vector</code> 可以直接相加</p>
<h5 id="元件例化、生成语句"><a href="#元件例化、生成语句" class="headerlink" title="元件例化、生成语句"></a>元件例化、生成语句</h5><p>注意将元件加入library的方法</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line"><span class="comment">--定义和调用</span></span><br><span class="line"><span class="keyword">Architecture</span> one <span class="keyword">of</span> add4 <span class="keyword">is</span></span><br><span class="line">	<span class="keyword">Component</span> add1</span><br><span class="line">		<span class="keyword">port</span>(</span><br><span class="line">			a, b, ci: <span class="keyword">in</span> <span class="built_in">std_logic</span>;</span><br><span class="line">			co, f: <span class="keyword">out</span> <span class="built_in">std_logic</span></span><br><span class="line">		);</span><br><span class="line">	<span class="keyword">End</span> <span class="keyword">component</span>;</span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">begin</span></span><br><span class="line"><span class="comment">--调用</span></span><br><span class="line">[随便写个名字]: add1 <span class="keyword">port</span> <span class="keyword">map</span>(, ,co =&gt; x ,);</span><br><span class="line"><span class="comment">--可以一一对应，也可以使用上述方式指定co端口接x（位置关联和名字关联）</span></span><br><span class="line"><span class="comment">--测试了一下，混合关联好像会有另外的要求，（a=&gt;ai, b=&gt;bi, f）就不行 建议不用</span></span><br></pre></td></tr></table></figure>

<p>元件例化的调用是一个并行语句，不能写在process里面</p>
<p>生成语句：</p>
<figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line">[随便写个名字]: <span class="keyword">for</span> i <span class="keyword">in</span> <span class="number">0</span> <span class="keyword">to</span> <span class="number">7</span> <span class="keyword">generate</span></span><br><span class="line"><span class="keyword">Begin</span></span><br><span class="line">    <span class="comment">--并行语句</span></span><br><span class="line"><span class="keyword">End</span> <span class="keyword">generate</span>;</span><br></pre></td></tr></table></figure>

<p>生成语句循环内的是并行语句，但调用是从0到7按顺序的</p>
<p>可以认为整个for是一个并行语句，不能放进process，但其执行时是按顺序的，每一步又是并行地执行块（个人理解）</p>
<h5 id="边沿判定"><a href="#边沿判定" class="headerlink" title="边沿判定"></a>边沿判定</h5><figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line">clk<span class="symbol">&#x27;event</span> <span class="keyword">and</span> clk = <span class="string">&#x27;1&#x27;</span>; <span class="comment">--上升沿判定</span></span><br><span class="line">clk<span class="symbol">&#x27;event</span> <span class="keyword">and</span> clk = <span class="string">&#x27;0&#x27;</span>; <span class="comment">--下降沿判定</span></span><br><span class="line"><span class="comment">--发生&#x27;event事件时，clk已经变化</span></span><br><span class="line">rising_edge(clk); <span class="comment">--上升沿判定</span></span><br><span class="line">falling_edge(clk); <span class="comment">--下降沿判定</span></span><br></pre></td></tr></table></figure>



<h5 id="描述方式"><a href="#描述方式" class="headerlink" title="描述方式"></a>描述方式</h5><p>逻辑综合：指将电路的行为级描述（代码）转化成门级电路的过程，以供硬件使用</p>
<p>VHDL的三种描述风格：</p>
<ul>
<li>行为描述方式：和高级语言比较接近，大量使用算数运算，关系运算等等，一般只用于系统仿真，难以进行逻辑综合。要使用算数运算，需要<code>use ieee.std_logic_unsigned.all;</code> </li>
<li>数据流描述方式：即表示行为也隐含地表示了结构，一般用布尔方程和并行赋值语句实现，可以进行逻辑综合。</li>
<li>结构化描述方式：多层次，常用元件例化，生成语句，可以进行逻辑综合。</li>
</ul>
<h5 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h5><figure class="highlight vhdl"><table><tr><td class="code"><pre><span class="line">A <span class="keyword">nand</span> B <span class="comment">--与非（不是不等于）</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>VHDL</tag>
      </tags>
  </entry>
  <entry>
    <title>数据库和mysql小记</title>
    <url>/2022/01/02/2022-01-02-%E6%95%B0%E6%8D%AE%E5%BA%93%E5%92%8Cmysql%E5%B0%8F%E8%AE%B0/</url>
    <content><![CDATA[<hr>
<h4 id="基本语法："><a href="#基本语法：" class="headerlink" title="基本语法："></a>基本语法：</h4><h5 id="建表和完整性约束"><a href="#建表和完整性约束" class="headerlink" title="建表和完整性约束:"></a>建表和完整性约束:</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create table student( 	 -- 建表</span><br><span class="line">    sno int unique, -- 属性名 类型 列级（属性上的）完整性约束...</span><br><span class="line">    cno int,</span><br><span class="line">	sname char(6),</span><br><span class="line">    class char(4),</span><br><span class="line">    Foreign key(cno) references class(cno),</span><br><span class="line">    Primary key(sno, cno) -- 最后两行是表级（元组上的）完整性约束，主键有多个时，必须作为表级完整性来写</span><br><span class="line">);</span><br><span class="line">drop table student [cascade|restrict]; -- 删除，其他东西的删除往往也是这个格式</span><br></pre></td></tr></table></figure>

<p>这样创建的是一个表的结构，还没有具体内容</p>
<p>列级（属性级）完整性约束包括<code>NOT NULL</code> <code>UNIQUE</code> <code>CHECK(条件)</code></p>
<p><code>CHECK</code> 也可以定义表级完整性约束，对元组各个属性之间的关系作出要求</p>
<p>完整性约束命名：就是在约束前面加上 <code>constraint 名字</code></p>
<p>断言是一种更高级别的约束，可以定义表与表之间的约束关系：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">create assertion 名字</span><br><span class="line">	check(...);</span><br></pre></td></tr></table></figure>

<p>任何操作都需要检查所有和它有关的完整性约束，若不符合则进行违约处理：拒绝，级联更新或置空</p>
<h5 id="查询："><a href="#查询：" class="headerlink" title="查询："></a>查询：</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select sno, student.sname			-- 投影，保留了sno和sname两属性列</span><br><span class="line">from student, class					-- 连接</span><br><span class="line">where student.sname = class.sname;	-- 选择</span><br></pre></td></tr></table></figure>

<p>多表查询时，属性名唯一的可以直接写，有冲突的需要 <code>表名.属性名</code> 这样；</p>
<p>上面是一个典型的自然连接，在做等值连接的同时，仅select了其中一个同名属性列，生成的表将含 <code>sname</code> 而非 <code>student.sname</code> 或 <code>sname(1)</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select sno, sum(score)</span><br><span class="line">from student;</span><br></pre></td></tr></table></figure>

<p>使用聚集函数时，如果没有跟上 <code>group by</code> 分组，则会把所有东西拼成一组，像上面那样得到的结果就没什么实际含义；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select class, sum(score) as total -- as total 造了一个别名，输出时将名为total，as可略</span><br><span class="line">from student</span><br><span class="line">group by class</span><br><span class="line">having avg(score) &gt; 80;</span><br><span class="line">-- group by紧跟着一个having，表示仅筛选满足条件的组</span><br><span class="line">-- 这里面，当然，也可以用聚集函数</span><br></pre></td></tr></table></figure>

<p>这样即可对 <code>class</code> 属性列分组，相同的为一组，聚集函数对每个组求一次值。<br>这里便能得到各个班级的总分；</p>
<p>查询聚集函数得到的结果，可以直接当成数值来用：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 嵌套查询，查询平均分之上的同学</span><br><span class="line">select *</span><br><span class="line">from student</span><br><span class="line">where score &gt;= (</span><br><span class="line">		select avg(score)</span><br><span class="line">		from student</span><br><span class="line">	);</span><br></pre></td></tr></table></figure>



<h5 id="修改："><a href="#修改：" class="headerlink" title="修改："></a>修改：</h5><p>修改表结构：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">alter table class ...; -- 注意alter只能根一个修改语句，不是语句块</span><br><span class="line"></span><br><span class="line">add teacher char(8) unique;</span><br><span class="line">drop teacher;</span><br><span class="line">change 列名 新列名 新类型; 	-- 这里和书上不一样</span><br><span class="line">add constraint 表级完整性约束;</span><br><span class="line">drop constraint 表级完整性约束名;</span><br></pre></td></tr></table></figure>

<p>修改数据：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-- 插入</span><br><span class="line">insert into student(a, b, c)</span><br><span class="line">values(1, 2001, &quot;tyj&quot;, ), (..), (..);</span><br><span class="line">-- 值的顺序和上面的列a, b, c列一一对应</span><br><span class="line">-- 可不提供(a, b, c)这样的顺序，按照默认顺序</span><br><span class="line">-- 可以一次插入多个值</span><br><span class="line"></span><br><span class="line">-- 删除</span><br><span class="line">delete</span><br><span class="line">from student</span><br><span class="line">where score &lt; 60; -- 删除60分以下的学生</span><br><span class="line"></span><br><span class="line">-- 修改</span><br><span class="line">update student</span><br><span class="line">set cno = 2</span><br><span class="line">where cno = 1;</span><br></pre></td></tr></table></figure>



<h5 id="授权："><a href="#授权：" class="headerlink" title="授权："></a>授权：</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Grant update(sno) -- 权限名: select(查询),insert(插入),update(修改),all privileges等</span><br><span class="line">on table student</span><br><span class="line">to u1; -- 用户/角色，public表示所有用户</span><br><span class="line">[with grant option] -- 加上表示允许该用户传递此权限</span><br><span class="line"></span><br><span class="line">Revoke update(sno)</span><br><span class="line">on table student</span><br><span class="line">from u1 [cascade|restrict];	-- 和删除类似，级联即同时回收u1传递出去的权限，默认cascade</span><br></pre></td></tr></table></figure>

<p>【角色】是权限的集合——比如职工，经理等角色；可以在给角色分配好权限后，给多个用户直接分配这一角色的权限。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Grant 角色1,角色2...</span><br><span class="line">to 角色/用户</span><br><span class="line">[with grant option]</span><br></pre></td></tr></table></figure>



<h4 id="范式："><a href="#范式：" class="headerlink" title="范式："></a>范式：</h4><p>好东西：<a href="https://blog.csdn.net/yangbodong22011/article/details/51619590">https://blog.csdn.net/yangbodong22011/article/details/51619590</a></p>
<ul>
<li>候选码：能唯一标识一个元组，且其子集不能的属性组，一个表可以有多组候选码</li>
<li>选择其中的一组候选码，即作为主码</li>
<li><strong>所有候选码的属性称为主属性</strong>（个人感觉定义最奇怪的地方，也是导致BCNF理解困难的原因）</li>
</ul>
<p>简单概括：</p>
<ul>
<li>1NF：数据项不可分割</li>
<li>2NF：非主属性对主属性不能存在部分函数依赖</li>
<li>3NF：非主属性对主属性不能存在传递函数依赖</li>
<li>BCNF：<strong>任一主属性</strong>对<strong>其他候选码</strong>不存在部分和传递依赖<ul>
<li>上面那句话不好理解，个人有另一种理解：</li>
<li>BCNF是3NF的扩展：如果有多个候选码，则把其中任一组看成主属性，其他看成非主属性（按定义，本来应是主属性）后，仍然满足3NFs</li>
</ul>
</li>
<li>4NF：属性间不存在非平凡且非函数的多值依赖（我暂且记个定义）</li>
</ul>
<h4 id="一些细节："><a href="#一些细节：" class="headerlink" title="一些细节："></a>一些细节：</h4><p>代码不加分号也能过：分号有类似分区的作用，出现错误时，其他加了分号的部分仍然正常执行</p>
<p>空值的判断是 <code>is [not] null</code></p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>数据库</tag>
        <tag>mysql</tag>
      </tags>
  </entry>
  <entry>
    <title>开发简记-多人联机游戏demo（socket实现）</title>
    <url>/2022/01/19/2022-01-19-%E5%BC%80%E5%8F%91%E7%AE%80%E8%AE%B0-%E5%A4%9A%E4%BA%BA%E8%81%94%E6%9C%BA%E6%B8%B8%E6%88%8Fdemo%EF%BC%88socket%E5%AE%9E%E7%8E%B0%EF%BC%89/</url>
    <content><![CDATA[<hr>
<h4 id="目的"><a href="#目的" class="headerlink" title="目的"></a>目的</h4><p>做一个仿MC的简单场景，多名玩家可以联机在其中移动，破坏和放置方块</p>
<p>要求是尽量简单、可拓展性强，能作为联机模板使用</p>
<p>本篇是一个开发简记（乱记）</p>
<h4 id="主要逻辑"><a href="#主要逻辑" class="headerlink" title="主要逻辑"></a>主要逻辑</h4><p>工程文件已上传Github，<del>这里就不贴了</del></p>
<p>服务端保存所有在线玩家的昵称和位置信息，以及世界中每一个方块的信息（当然，世界要是大的话这并不科学…）</p>
<p>然后处理的请求包括用户登录，登出，修改方块，修改玩家位置；另外需要支持向所有玩家广播消息，需要广播的有所有玩家的位置信息，和玩家登录登出消息。</p>
<p>客户端处理所有游戏本身的东西，向服务端发送登录登出，位置信息，修改方块请求。</p>
<p>连接时，双方建立Connect之后，服务端即为这一客户端开启一个子线程专门用来接收，客户端也开启监听；随后客户端可以发送登录请求，由服务端检测用户名重复等问题后，回应一个允许登录或错误信息，若允许登录，服务端就把这一用户名和Socket等信息录入，客户端接收到允许登录后，请求世界信息（第一次要求发送全部世界的信息，文本量较大容易失败），若请求成功则登入，若请求失败则发送登出信息，同时关闭客户端的监听，服务端收到登出消息也会删除其用户信息和关闭相应监听。</p>
<hr>
<p>一个坑：</p>
<p>发现对Socket的理解不够准确，这里明确一些东西：</p>
<ul>
<li>Socket无需频繁地建立，一次Connect和Accept即可让双方都获得对方Socket，将这个Socket保存起来即可不断地进行Send和Rreceive；在服务端中，每监听到一个连接就为其单独开一个子线程来循环接收其消息。这里不需要担心端口占用的问题，我的理解是：Socket当中会存储两组IP+端口信息，分别是自身的和对方的，在服务端同时执行多个Socket的Receive方法时，消息到达端口后还会根据其存储的源IP+端口信息，再次分配给指定的Socket接收。简言之，一个进程中的多个Socket监听同一个端口，来自这个端口的消息还是会根据来源地址信息分流。</li>
<li>规范而言，一次Send后需要Receive一次来确定消息是否到达（因为Send并不是阻塞方法，发出去就不管了，而网络通信由于网络波动，中间出错并不少见），但出于性能考虑我们应该灵活变通，对于重要消息（如登录等）需要确认，对于频繁调用的不重要消息（玩家更新位置）等可以不必确认到达。</li>
</ul>
<hr>
<p>一个坑：</p>
<p>之前为了避免连接问题影响本地运行，把客户端的监听放到子线程里了，但是现在发现——unity不允许子线程修改任何跟unity有关的东西，包括场景切换，修改场景物体等；</p>
<p>百度了一波发现，untiy其实是一个单线程引擎，多个脚本间和协程其实是通过Time slicing（时间分片）完成的，这样做的话能有固定的运行顺序，不容易引发异步导致的各种冲突。故不允许子线程修改游戏本身内容其实很好理解；</p>
<p>绕过去的方法，最粗暴的就是搞一个bool，在子线程中设为1，主线程中监控这个bool。。。</p>
<p>显然这样的做法过于不雅，但也没找到特别优雅的解决方案。我目前的做法是搞一个todoList队列，子进程只负责监听，收到消息后将消息扔进队列，主线程中发现队列中有内容就去完成它。</p>
<p>这个思路同样可以用于服务端，在服务端为多个客户开多个监听线程时，这样避免异步导致的冲突</p>
<p>为了进一步避免线程间的资源竞争，需要引入封锁机制，这里也只需要对toDoList的存取做封锁就可以了</p>
<hr>
<p>遇到了一个很奇怪的bug：</p>
<p>向服务器发送消息成功了，返回消息也成功了，但客户端没有任何反应</p>
<p>反复查找，最后发现客户端在刚连上时接收到了下面这条消息：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span><span class="attr">&quot;type&quot;</span><span class="punctuation">:</span><span class="string">&quot;UpdateAllUser&quot;</span><span class="punctuation">,</span><span class="attr">&quot;info&quot;</span><span class="punctuation">:</span><span class="string">&quot;[&#123;\&quot;playername\&quot;:\&quot;tyin\&quot;,\&quot;px\&quot;:20.0,\&quot;py\&quot;:20.0,\&quot;pz\&quot;:20.0,\&quot;rx\&quot;:0.0,\&quot;ry\&quot;:0.0,\&quot;rz\&quot;:0.0&#125;]&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">&#123;</span><span class="attr">&quot;type&quot;</span><span class="punctuation">:</span><span class="string">&quot;UpdateAllUser&quot;</span><span class="punctuation">,</span><span class="attr">&quot;info&quot;</span><span class="punctuation">:</span><span class="string">&quot;[&#123;\&quot;playername\&quot;:\&quot;tyin\&quot;,\&quot;px\&quot;:20.0,\&quot;py\&quot;:20.0,\&quot;pz\&quot;:20.0,\&quot;rx\&quot;:0.0,\&quot;ry\&quot;:0.0,\&quot;rz\&quot;:0.0&#125;]&quot;</span><span class="punctuation">&#125;</span><span class="punctuation">&#123;</span><span class="attr">&quot;type&quot;</span><span class="punctuation">:</span><span class="string">&quot;UpdateAllUser&quot;</span><span class="punctuation">,</span><span class="attr">&quot;info&quot;</span><span class="punctuation">:</span><span class="string">&quot;[&#123;\&quot;playername\&quot;:\&quot;tyin\&quot;,\&quot;px\&quot;:20.0,\&quot;py\&quot;:20.0,\&quot;pz\&quot;:20.0,\&quot;rx\&quot;:0.0,\&quot;ry\&quot;:0.0,\&quot;rz\&quot;:0.0&#125;]&quot;</span><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure>

<p>接收到的三条正常的信息合并到一条了，导致Json转义失败</p>
<p>这是由于服务端处理用户登录请求后，就把用户信息登记上了，此时登录成功的消息还没有发给客户，客户尚未开始监听，而服务端已经开始向客户推送消息；这些消息就堆叠了起来。。在客户开始监听时一次性接收。。</p>
<p>解决方案：</p>
<p>让服务端接受登录后延迟推送显然不可行（就变成来自客户端的消息堆叠了）</p>
<p>所以就在每条消息的后面加上了一个’\0’，接收后先根据此标识拆分成多个字符串。</p>
]]></content>
      <categories>
        <category>游戏</category>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>unity</tag>
        <tag>Socket</tag>
        <tag>联机</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 770 div.2 EF</title>
    <url>/2022/02/06/2022-02-06-Codeforces%20Round%20770%20div.2%20EF/</url>
    <content><![CDATA[<h4 id="E-Fair-Share"><a href="#E-Fair-Share" class="headerlink" title="E. Fair Share"></a>E. Fair Share</h4><p>题意：有 $n$ 组物品，第 $i$ 组有 $m_i$ 个，每个物品有一个编号，保证每种物品出现偶数次，每组物品有偶数个，要求将所有物品划分为两组，使得一组中有每种物品各一半，每组物品各一半。 </p>
<p>首先将问题抽象出来，组别和物品类别其实没有什么区别，都是物品的属性；</p>
<p>建图：左边点表示组别，右边点表示类别，$i$ 组中包含物品 $j$，则连边 $i,j$</p>
<p>因为保证了物品出现偶数次，一组也有偶数个物品，那么这张图必然存在若干条欧拉回路</p>
<p>寻找欧拉回路，对于每条欧拉回路，如果一条边是由左到右遍历，则分至 $L$ 组，否则分至 $R$ 组，由于欧拉回路的性质（一个点的入次数和出次数相等，这样一定能保证恰好均分）</p>
<p>然后我发现我好像从来没写过欧拉回路，想当然的写了一波TLE了一页（</p>
<p>关键在于，欧拉回路标记vis的是边而不是点，一个点可能遍历多次，如果还是每遍历到一个点就去遍历它的所有连边，即使有vis标记忽略已经走过的边，还是会变成 $n^2$ </p>
<p>需要记录每个店最后一次遍历的边是哪条，下次再跑直接从这开始即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Edge</span>&#123;</span><br><span class="line">    <span class="type">int</span> x, y, pj, vis;</span><br><span class="line">&#125;e[maxn];</span><br><span class="line"><span class="type">int</span> ecnt = <span class="number">0</span>;</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; vp[maxn];</span><br><span class="line"><span class="type">int</span> lst[maxn];</span><br><span class="line">string ans[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> j=lst[p];j&lt;vp[p].<span class="built_in">size</span>();j=lst[p])&#123;</span><br><span class="line">        <span class="type">int</span> i = vp[p][j];</span><br><span class="line">        lst[p] = j+<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(e[i].vis) <span class="keyword">continue</span>;</span><br><span class="line">        e[i].vis = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(p &lt;= n)&#123;</span><br><span class="line">            ans[e[i].x][e[i].pj] = <span class="string">&#x27;L&#x27;</span>;</span><br><span class="line">            <span class="built_in">dfs</span>(e[i].y);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            ans[e[i].x][e[i].pj] = <span class="string">&#x27;R&#x27;</span>;</span><br><span class="line">            <span class="built_in">dfs</span>(e[i].x);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    map&lt;<span class="type">int</span>, <span class="type">int</span>&gt; lsh;</span><br><span class="line">    <span class="type">int</span> tot = n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; m;</span><br><span class="line">        ans[i] = <span class="built_in">string</span>(m, <span class="string">&#x27;\0&#x27;</span>);</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">0</span>;j&lt;m;j++)&#123;</span><br><span class="line">            <span class="type">int</span> x; cin &gt;&gt; x;</span><br><span class="line">            <span class="keyword">if</span>(!lsh[x]) lsh[x] = ++tot;</span><br><span class="line">            e[++ecnt] = &#123;i, lsh[x], j, <span class="number">0</span>&#125;;</span><br><span class="line">            vp[lsh[x]].<span class="built_in">push_back</span>(ecnt);</span><br><span class="line">            vp[i].<span class="built_in">push_back</span>(ecnt);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=tot;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(vp[i].<span class="built_in">size</span>() % <span class="number">2</span> != <span class="number">0</span>)&#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=tot;i++) <span class="keyword">if</span>(lst[i] &lt; vp[i].<span class="built_in">size</span>()) <span class="built_in">dfs</span>(i);</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cout &lt;&lt; ans[i] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="F-Fibonacci-Additions"><a href="#F-Fibonacci-Additions" class="headerlink" title="F. Fibonacci Additions"></a>F. Fibonacci Additions</h4><p>一道差分好题</p>
<p>题意：有长度为 $n$ 的 $a$ 数组和 $b$ 数组，接下来有若干次修改：使 $a$ 或者 $b$ 数组的 $[l,r]$ 段依次加上斐波那契数列，即 $a_l+1$，$a_{l+1}+1$，$a_{l+3}+2$…</p>
<p>每次修改完成后，要求输出 $a$ 与 $b$ 是否完全相等。</p>
<p>首先需要利用斐波那契数列的性质：$f_i&#x3D;f_{i-1}+f_{i-2}$</p>
<p>在空数组上随便修改几段试一下就会发现，不论怎么修改，在修改量上，永远有 $d_i&#x3D;d_{i-1}+d_{i-2}$</p>
<p>既然如此，可以考虑对修改量差分（前缀和的形式改为斐波那契形式）</p>
<p>写个例子理解一下，比如要对3-6这段区间加斐波那契数列：</p>
<p>修改量：0 0 f1 f2 f3 f4   0   0   0</p>
<p>差分d： 0 0  f1  0  0  0  -f5 -f4 0</p>
<p>如果我们对差分数组这样修改，那么它的斐波那契前缀和即为修改量；</p>
<p>这样就简单啦，显然任意一组修改量和它的差分数组是一一对应的，我们直接把初始情况转化为差分数组，然后每次在差分数组上进行3次单点修改，就可以很方便的判断是否一样了。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ll n, q, mod, a[maxn], tot = <span class="number">0</span>;</span><br><span class="line">ll f[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">modify</span><span class="params">(ll x, ll ad)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(x &gt; n) <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">if</span>(a[x] == <span class="number">0</span>) tot--;</span><br><span class="line">    a[x] = (a[x] + ad + mod) % mod;</span><br><span class="line">    <span class="keyword">if</span>(a[x] == <span class="number">0</span>) tot++;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; q &gt;&gt; mod;</span><br><span class="line">    f[<span class="number">1</span>] = f[<span class="number">2</span>] = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">3</span>;i&lt;=n;i++) f[i] = (f[i<span class="number">-1</span>] + f[i<span class="number">-2</span>]) % mod;</span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        ll x; cin &gt;&gt; x;</span><br><span class="line">        a[i] -= x;</span><br><span class="line">        a[i] = (a[i] % mod + mod) % mod;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(ll i=n;i&gt;=<span class="number">2</span>;i--) a[i] -= a[i<span class="number">-1</span>] + a[i<span class="number">-2</span>], a[i] = (a[i] % mod + mod) % mod;</span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">1</span>;i&lt;=n;i++) tot += !a[i];</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">for</span>(ll i=<span class="number">1</span>;i&lt;=q;i++)&#123;</span><br><span class="line">        <span class="type">char</span> c; ll x, y;</span><br><span class="line">        cin &gt;&gt; c &gt;&gt; x &gt;&gt; y;</span><br><span class="line">        <span class="keyword">if</span>(c == <span class="string">&#x27;A&#x27;</span>)&#123;</span><br><span class="line">            <span class="built_in">modify</span>(x, <span class="number">1</span>);</span><br><span class="line">            <span class="built_in">modify</span>(y+<span class="number">2</span>, -f[y-x+<span class="number">1</span>]);</span><br><span class="line">            <span class="built_in">modify</span>(y+<span class="number">1</span>, -f[y-x+<span class="number">2</span>]);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="built_in">modify</span>(x, <span class="number">-1</span>);</span><br><span class="line">            <span class="built_in">modify</span>(y+<span class="number">2</span>, f[y-x+<span class="number">1</span>]);</span><br><span class="line">            <span class="built_in">modify</span>(y+<span class="number">1</span>, f[y-x+<span class="number">2</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">        cout &lt;&lt; vector&lt;string&gt;&#123;<span class="string">&quot;NO&quot;</span>, <span class="string">&quot;YES&quot;</span>&#125;[tot == n] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 772 div.2 A-F</title>
    <url>/2022/02/20/2022-02-20-Codeforces%20Round%20772%20div.2/</url>
    <content><![CDATA[<h4 id="A-Min-Or-Sum"><a href="#A-Min-Or-Sum" class="headerlink" title="A. Min Or Sum"></a>A. Min Or Sum</h4><p>题意：有一个数组 $a$，你可以进行任意次操作：取两个数 $a_i,a_j$，将这两个数分别改为 $x,y$，要求 $a_i|a_j&#x3D;x|y$ 问可能得到数组总和最小值。</p>
<p>我们显然可以把二进制中每一位上多余的部分全部删光，只留一个，答案即所有数或起来</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ll n, m;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    ll ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        ll x; cin &gt;&gt; x;</span><br><span class="line">        ans |= x;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="B-Avoid-Local-Maximums"><a href="#B-Avoid-Local-Maximums" class="headerlink" title="B. Avoid Local Maximums"></a>B. Avoid Local Maximums</h4><p>题意：给一个数组 $a$，每次操作将任一个数修改为任意其他数，问至少操作几次，使得数组中不存在山峰（“山峰”即 $a_i &gt;a_{i-1}$ 且 $a_i &gt; a_{i+1}$ ）</p>
<p>我们可以把每个山峰都抹平，使之变成它两侧的较小值，但还有一种情况：两个山峰相邻（即下标相差2），那么可以把中间位置抬高到两山峰中的较大值，一次性消除两个山峰，模拟即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ll n, m;</span><br><span class="line"><span class="type">int</span> a[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; f;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">2</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i] &gt; a[i<span class="number">-1</span>] &amp;&amp; a[i] &gt; a[i+<span class="number">1</span>]) f.<span class="built_in">push_back</span>(i);</span><br><span class="line">    &#125;</span><br><span class="line">    ll ans = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;f.<span class="built_in">size</span>();i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(i != f.<span class="built_in">size</span>()<span class="number">-1</span> &amp;&amp; f[i] == f[i+<span class="number">1</span>] - <span class="number">2</span>)&#123;</span><br><span class="line">            a[f[i] + <span class="number">1</span>] = <span class="built_in">max</span>(a[f[i]], a[f[i] + <span class="number">2</span>]);</span><br><span class="line">            ans++, i++;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            a[f[i]] = <span class="built_in">max</span>(a[f[i]<span class="number">-1</span>], a[f[i]+<span class="number">1</span>]);</span><br><span class="line">            ans++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cout &lt;&lt; a[i] &lt;&lt; <span class="string">&quot; \n&quot;</span>[i == n];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="C-Differential-Sorting"><a href="#C-Differential-Sorting" class="headerlink" title="C. Differential Sorting"></a>C. Differential Sorting</h4><p>题意：给一个数组 $a$，每次操作可以选择三个位置 $x,y,z(x&lt;y&lt;z)$，使得 $a_x$ 变为 $a_y-a_z$，要求构造一种操作方案，使得整个数组单调不降。</p>
<p>显然，最后两位是无法修改的，那么如果最后两位是递增的，一定不成立</p>
<p>排除这种情况后，此时 $a_n$ 为最大值，如果 $a_n&lt;0$，$a_{n-2}$ 必然比 $a_{n-1}$ 大，一定不行</p>
<p>现在有 $a_n\ge0$，我们向前枚举，后面的部分已经有序，那么直接拿 $a_{i+1}-a_n$ 得到 $a_i$，就能使 $a_i$ 越来越小。 </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">ll n, m;</span><br><span class="line">ll a[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    <span class="type">bool</span> f = <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(a[i] &gt; a[i+<span class="number">1</span>])&#123;</span><br><span class="line">            f = <span class="literal">false</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(f)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;0\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">max</span>(a[n], a[n<span class="number">-1</span>]) &lt; <span class="number">0</span> || a[n] &lt; a[n<span class="number">-1</span>])&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;-1\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    cout &lt;&lt; n<span class="number">-2</span> &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=n<span class="number">-2</span>;i&gt;=<span class="number">1</span>;i--)&#123;</span><br><span class="line">        cout &lt;&lt; i &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; i+<span class="number">1</span> &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; n &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="D-Infinite-Set"><a href="#D-Infinite-Set" class="headerlink" title="D. Infinite Set"></a>D. Infinite Set</h4><p>题意：有一个集合 $a$，初始有一些数，如果 $x$ 在集合中，那么 $2x+1$ 和 $4x$ 都在集合中，现在问集合中小于 $2^p$ 的数共有多少个？（$p\le2\times10^5$）</p>
<p>首先我们把问题简化：$2x+1$ 其实就是在 $x$ 的二进制表示下添个1，$4x$ 就是添两个0</p>
<p>对任一个 $x$​ 而言，我们不断的对它进行上面两种操作，产生的新数字是不会重复的。</p>
<p>设 $f_i$ 表示二进制最高位为 $i$ 的数的个数，那么有递推式 $f_i &#x3D; f_{i-1}+f_{i-2}$</p>
<p>为了方便处理，首先把本质相同的 $a_i$ 去掉（只保留一个），即如果 $a_j$ 的二进制可以表示为 $a_i+00100..$ 则 $a_j$ 和 $a_i$ 本质相同，后面的 $00100$ 要求是由 $1$ 和 $00$ 组成的串，判重可以用01字典树完成。</p>
<p>这样我们把本质不同的数先加入 $f$ 数组，就得到了初值，随后dp即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="type">int</span> a[maxn], cnt[maxn];</span><br><span class="line">ll f[maxn];</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">tire</span>&#123;</span><br><span class="line">    <span class="type">int</span> ch[<span class="number">2</span>];</span><br><span class="line">    <span class="type">bool</span> end;</span><br><span class="line">&#125;t[maxn &lt;&lt; <span class="number">5</span>];</span><br><span class="line"><span class="type">int</span> tcnt = <span class="number">0</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">mxk</span><span class="params">(<span class="type">int</span> x)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> mxk = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(x &gt; <span class="number">0</span>)&#123;</span><br><span class="line">        x /= <span class="number">2</span>;</span><br><span class="line">        mxk++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> mxk;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x)</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> p = <span class="number">0</span>;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vp;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="built_in">mxk</span>(x);i&gt;=<span class="number">1</span>;i--)&#123;</span><br><span class="line">        <span class="type">int</span> v = (x &gt;&gt; (i<span class="number">-1</span>)) &amp; <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(!t[p].ch[v]) t[p].ch[v] = ++tcnt;</span><br><span class="line">        p = t[p].ch[v];</span><br><span class="line">        vp.<span class="built_in">push_back</span>(p);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(t[p].end) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    t[p].end = <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">if</span>(vp.<span class="built_in">empty</span>()) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=vp.<span class="built_in">size</span>()<span class="number">-1</span>;i&gt;=<span class="number">0</span>;i--)&#123;</span><br><span class="line">        <span class="keyword">if</span>(i != vp.<span class="built_in">size</span>()<span class="number">-1</span> &amp;&amp; t[vp[i]].end) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        <span class="type">int</span> v = x % <span class="number">2</span>;</span><br><span class="line">        x /= <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(v) <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">if</span>(x &gt; <span class="number">0</span> &amp;&amp; x % <span class="number">2</span> == <span class="number">0</span>) x /= <span class="number">2</span>, i--;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    <span class="built_in">sort</span>(a+<span class="number">1</span>, a+n+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">add</span>(a[i]))&#123;</span><br><span class="line">            cnt[<span class="built_in">mxk</span>(a[i])]++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ll tot = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(i &gt; <span class="number">1</span>) f[i] = (f[i<span class="number">-1</span>] + f[i<span class="number">-2</span>]) % mode;</span><br><span class="line">        (f[i] += cnt[i]) %= mode;</span><br><span class="line">        tot = (tot + f[i]) % mode;</span><br><span class="line"><span class="comment">//        cout &lt;&lt; f[i] &lt;&lt; &#x27;\n&#x27;;</span></span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; tot &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="E-Cars"><a href="#E-Cars" class="headerlink" title="E. Cars"></a>E. Cars</h4><p>题意：$x$ 轴上有 $n$ 辆车，每辆车有位置和朝向，不知道他们的具体情况，但知道 $m$ 条信息：</p>
<ul>
<li>$i,j$ 两辆车方向相反，接下来会撞上</li>
<li>$i,j$ 两辆车方向相反，但是会越来越远</li>
</ul>
<p>要输出所有车的情况，或者输出-1表示不存在这种情况</p>
<p>方向和位置信息一起考虑比较复杂，我们分开考虑：</p>
<p>首先任意一对关系，都说明他们的方向相反，我们在 $i,j$ 之间连一条边，得到一张图之后染色，判断方向是否存在冲突。</p>
<p>因为左右对称性，第一辆车的方向是无所谓的，我们假设第一辆车的方向是 $L$，然后再跑一次这张图，这次可以根据两个相邻点的方向和它们之间的关系，判断谁在谁左边。假设 $i$ 在 $j$ 左边，那么在一个新的图中让 $i$ 向 $j$ 连一条有向边。</p>
<p>构成的这个新图中，如果存在环即不存在答案。</p>
<p>随后按照拓扑序给每个节点分配位置即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line">vector&lt;pair&lt;<span class="type">int</span>, <span class="type">int</span>&gt;&gt; vp[maxn];</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; vp2[maxn];</span><br><span class="line"><span class="type">int</span> col[maxn];</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> c)</span></span>&#123;</span><br><span class="line">    col[p] = c;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> v:vp[p])&#123;</span><br><span class="line">        <span class="keyword">if</span>(col[v.first])&#123;</span><br><span class="line">            <span class="keyword">if</span>(col[v.first] == <span class="number">3</span> - c) <span class="keyword">continue</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(!<span class="built_in">dfs</span>(v.first, <span class="number">3</span> - c)) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="type">bool</span> vis[maxn];</span><br><span class="line"><span class="type">int</span> du[maxn], dir[maxn], pos[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs2</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> d)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(vis[p]) <span class="keyword">return</span>;</span><br><span class="line">    dir[p] = d;</span><br><span class="line">    vis[p] = <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> v:vp[p])&#123;</span><br><span class="line">        <span class="keyword">if</span>(v.second == <span class="number">1</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(d == <span class="number">1</span>) vp2[v.first].<span class="built_in">push_back</span>(p), du[p]++;</span><br><span class="line">            <span class="keyword">else</span> vp2[p].<span class="built_in">push_back</span>(v.first), du[v.first]++;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">if</span>(d == <span class="number">0</span>) vp2[v.first].<span class="built_in">push_back</span>(p), du[p]++;</span><br><span class="line">            <span class="keyword">else</span> vp2[p].<span class="built_in">push_back</span>(v.first), du[v.first]++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">dfs2</span>(v.first, d ^ <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=m;i++)&#123;</span><br><span class="line">        <span class="type">int</span> t, x, y;</span><br><span class="line">        cin &gt;&gt; t &gt;&gt; x &gt;&gt; y;</span><br><span class="line">        vp[x].<span class="built_in">emplace_back</span>(y, t);</span><br><span class="line">        vp[y].<span class="built_in">emplace_back</span>(x, t);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(!col[i] &amp;&amp; !<span class="built_in">dfs</span>(i, <span class="number">1</span>))&#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) <span class="built_in">dfs2</span>(i, <span class="number">1</span>);</span><br><span class="line"> </span><br><span class="line">    queue&lt;<span class="type">int</span>&gt; q;</span><br><span class="line">    <span class="type">int</span> tot = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) <span class="keyword">if</span>(!du[i]) q.<span class="built_in">push</span>(i);</span><br><span class="line">    <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())&#123;</span><br><span class="line">        <span class="type">int</span> p = q.<span class="built_in">front</span>(); q.<span class="built_in">pop</span>();</span><br><span class="line">        pos[p] = ++tot;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> v:vp2[p])&#123;</span><br><span class="line">            du[v]--;</span><br><span class="line">            <span class="keyword">if</span>(du[v] == <span class="number">0</span>) q.<span class="built_in">push</span>(v);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(!pos[i])&#123;</span><br><span class="line">            cout &lt;&lt; <span class="string">&quot;NO\n&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;YES\n&quot;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;LR&quot;</span>[dir[i]] &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; pos[i] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="F-Closest-Pair"><a href="#F-Closest-Pair" class="headerlink" title="F. Closest Pair"></a>F. Closest Pair</h4><p>题意：有 $n$ 个点，每个点有坐标 $x_i$（$x_i$ 保证有序） 和权值 $w_i$，接下来有一堆询问，每次问 $[l, r]$ 这段点中，点对 $i,j$ 的 $|x_ i-x_j|\times(w_i+w_j)$ 的最小值。</p>
<p>这道题的思维比较巧妙</p>
<p>在一段区间中，如果 $i,j$ 这个点对是值最小的点对，那么 $i,j$ 之间一定不存在另一个 $w_k$ 介于 $w_i, w_j$ 之间（不然最小值就是 $i,k$ 了）</p>
<p>所以对于每个点 $i$，取它左右两端第一个 $w_j&lt;w_i$ 的 $j$，答案一定在这 $2n$ 个点对当中。</p>
<p>这样问题就转换为了：有 $2n$ 条带权的线段，每次询问一个区间，问这个区间包含的线段的最大值是多少。</p>
<p>离线+线段树维护最小值即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">SegTree</span>&#123;</span><br><span class="line">    ll mi[maxn &lt;&lt; <span class="number">2</span>];</span><br><span class="line">    <span class="function"><span class="keyword">inline</span> <span class="type">void</span> <span class="title">push_up</span><span class="params">(<span class="type">int</span> p)</span></span>&#123;</span><br><span class="line">        mi[p] = <span class="built_in">min</span>(mi[p*<span class="number">2</span>], mi[p*<span class="number">2</span>+<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">modify</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> l, <span class="type">int</span> r, <span class="type">int</span> pos, ll to)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(l == r)&#123;</span><br><span class="line">            mi[p] = <span class="built_in">min</span>(mi[p], to);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> mid = (l + r) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(pos &lt;= mid) <span class="built_in">modify</span>(p*<span class="number">2</span>, l, mid, pos, to);</span><br><span class="line">        <span class="keyword">else</span> <span class="built_in">modify</span>(p*<span class="number">2</span>+<span class="number">1</span>, mid+<span class="number">1</span>, r, pos, to);</span><br><span class="line">        <span class="built_in">push_up</span>(p);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">ll <span class="title">query</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> l, <span class="type">int</span> r, <span class="type">int</span> L, <span class="type">int</span> R)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(R &lt; l || r &lt; L) <span class="keyword">return</span> <span class="number">4e18</span>;</span><br><span class="line">        <span class="keyword">if</span>(L &lt;= l &amp;&amp; r &lt;= R) <span class="keyword">return</span> mi[p];</span><br><span class="line">        <span class="type">int</span> mid = (l + r) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">min</span>(<span class="built_in">query</span>(p*<span class="number">2</span>, l, mid, L, R), <span class="built_in">query</span>(p*<span class="number">2</span>+<span class="number">1</span>, mid+<span class="number">1</span>, r, L, R));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;segt;</span><br><span class="line"> </span><br><span class="line">vector&lt;pair&lt;<span class="type">int</span>, ll&gt;&gt; seg[maxn];</span><br><span class="line">vector&lt;pair&lt;<span class="type">int</span>, <span class="type">int</span>&gt;&gt; qr[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="type">int</span> n, q;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; q;</span><br><span class="line">    <span class="function">vector&lt;ll&gt; <span class="title">x</span><span class="params">(n+<span class="number">1</span>)</span>, <span class="title">w</span><span class="params">(n+<span class="number">1</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        cin &gt;&gt; x[i] &gt;&gt; w[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">fill</span>(segt.mi, segt.mi + (maxn &lt;&lt; <span class="number">2</span>), <span class="number">4e18</span>);</span><br><span class="line">    stack&lt;<span class="type">int</span>&gt; st;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">while</span>(!st.<span class="built_in">empty</span>() &amp;&amp; w[i] &lt;= w[st.<span class="built_in">top</span>()])&#123;</span><br><span class="line">            seg[i].<span class="built_in">emplace_back</span>(st.<span class="built_in">top</span>(), (w[i] + w[st.<span class="built_in">top</span>()]) * (x[i] - x[st.<span class="built_in">top</span>()]));</span><br><span class="line"><span class="comment">//            cout &lt;&lt; i &lt;&lt; &#x27; &#x27; &lt;&lt; st.top() &lt;&lt; &#x27;\n&#x27;;</span></span><br><span class="line">            st.<span class="built_in">pop</span>();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(!st.<span class="built_in">empty</span>())&#123;</span><br><span class="line">            seg[i].<span class="built_in">emplace_back</span>(st.<span class="built_in">top</span>(), (w[i] + w[st.<span class="built_in">top</span>()]) * (x[i] - x[st.<span class="built_in">top</span>()]));</span><br><span class="line"><span class="comment">//            cout &lt;&lt; i &lt;&lt; &#x27; &#x27; &lt;&lt; st.top() &lt;&lt; &#x27;\n&#x27;;</span></span><br><span class="line">        &#125;</span><br><span class="line">        st.<span class="built_in">push</span>(i);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function">vector&lt;ll&gt; <span class="title">ans</span><span class="params">(q+<span class="number">1</span>)</span></span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=q;i++)&#123;</span><br><span class="line">        <span class="type">int</span> l, r; cin &gt;&gt; l &gt;&gt; r;</span><br><span class="line">        qr[r].<span class="built_in">emplace_back</span>(l, i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">auto</span> v:seg[i])</span><br><span class="line">            segt.<span class="built_in">modify</span>(<span class="number">1</span>, <span class="number">1</span>, n, v.first, v.second);</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">auto</span> v:qr[i])&#123;</span><br><span class="line">            ans[v.second] = segt.<span class="built_in">query</span>(<span class="number">1</span>, <span class="number">1</span>, n, v.first, n);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=q;i++) cout &lt;&lt; ans[i] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 773 div.2 F</title>
    <url>/2022/02/23/2022-02-20-Codeforces%20Round%20773%20div.2%20F/</url>
    <content><![CDATA[<h4 id="F-Two-Arrays"><a href="#F-Two-Arrays" class="headerlink" title="F. Two Arrays"></a>F. Two Arrays</h4><p>题意：给出 $n$ 个数组，每个数组都有 $m$ 个不同元素，有权值 $w_i$，我们称两个不包含相同元素的数组<strong>是匹配的</strong>，要求匹配的数组对当中， $w_i+w_j$ 的最小值（$n\le10^5,m\le5,a_i\le10^9$）</p>
<p><strong>奇技淫巧：</strong></p>
<p>哈希，我们可以把 $w$ 映射到一个很小的范围上去（比如1000以内），可能会产生冲突，但要导致最终结果出错，当前仅当作为答案选取的两个数组 $i,j$ 中的至多10个不同元素映射产生了冲突，可以估计，产生冲突的概率是很小的。</p>
<p>这样我们就将 $w$ 的值域压缩到了一个很小的范围。</p>
<p>这个冲突概率大概是1%左右，为了避免运气不好，可以卡个时多次跑，取最小ans</p>
<p><strong>解法一：</strong></p>
<p>首先考虑bitset的 $n^2$ 暴力</p>
<p>先将所有数组按照 $w$ 排序</p>
<p>对每个权值，用bitset记录它在哪些数组上出现过</p>
<p>然后暴力枚举每个数组，把它的元素出现过的位置或起来</p>
<p>然后取最早的从未出现的位置，更新答案。</p>
<p>复杂度 $O(n^2m&#x2F;64)$，值域压缩后，空间复杂度 $10000\times n$（）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">node</span>&#123;</span><br><span class="line">    <span class="type">int</span> v[<span class="number">6</span>];</span><br><span class="line">    <span class="type">int</span> w;</span><br><span class="line">&#125;a[maxn];</span><br><span class="line">bitset&lt;maxn&gt; t[<span class="number">13335</span>], p;</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=m;j++)&#123;</span><br><span class="line">            cin &gt;&gt; a[i].v[j];</span><br><span class="line">            a[i].v[j] %= <span class="number">13335</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        cin &gt;&gt; a[i].w;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">sort</span>(a+<span class="number">1</span>, a+n+<span class="number">1</span>, [](node x, node y)&#123; <span class="keyword">return</span> x.w &lt; y.w;&#125;);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=m;j++) t[a[i].v[j]][i<span class="number">-1</span>] = <span class="literal">true</span>;</span><br><span class="line"> </span><br><span class="line">    ll ans = <span class="number">1e18</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        p = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=m;j++) p |= t[a[i].v[j]];</span><br><span class="line">        <span class="type">int</span> pos = <span class="number">1</span> + (~p)._Find_first();</span><br><span class="line">        <span class="keyword">if</span>(pos &lt; i &amp;&amp; pos &gt; <span class="number">0</span>) ans = <span class="built_in">min</span>(ans, <span class="number">1ll</span> * a[pos].w + a[i].w);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span>(ans &gt; (ll)<span class="number">1e17</span>) ans = <span class="number">-1</span>;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p><strong>解法二：</strong></p>
<p>同样压缩值域，狠一点直接压缩到20</p>
<p>这样我们就可以用一个int来表示一个数组</p>
<p>然后问题就转换为了：有 $n$ 个数对： $a_i,w_i$ ，要求一个数对 $i,j$，满足 $a_i&amp;a_j$ 为0，使 $w_i+wj$ 最大</p>
<p>然后似乎可以SOSDP？</p>
<p><strong>解法三（正解）：</strong></p>
<p>如果要无概率呢？</p>
<p>我们需要一个东西来快速<strong>判断一段区间内有无数组能和一个新数组匹配</strong></p>
<p>考虑两个数组 $a,b$，如何判断它们能否匹配，当然可以简单地扫一遍，但这不能扩展</p>
<p>先说结论：我们把 $a$ 的所有子集都存进一个set，然后对于 $b$ 的每个子集，如果它在 $a$ 中出现过：</p>
<ul>
<li>如果这个子集有奇数个元素，记1</li>
<li>如果这个子集有偶数个元素，记-1</li>
</ul>
<p>可以预想，假设 $a$ 与 $b$ 共有 $k$ 个相同元素，它们重复的子集个数则有 $2^k$ 个，去掉空集，奇数子集将比偶数子集多一个。</p>
<p>那么这么计数得到的结果一定是1，除非 $a$ 与 $b$ <strong>完全不同</strong>，结果才为0</p>
<p>这样下来，如果我们要拿 $b$ 和 $a_1,a_2…a_t$ 这 $t$ 个数组匹配，就只需要把这 $m$ 个数组的所有子集加入集合（不去重），然后再拿 $b$ 去按上述方式计数，如果 $b$ 和所有 $a$ 都无法匹配，计数结果即为 $t$</p>
<p>不过如果这里我们用mutiset的话，复杂度为 $O(n\times2^m\times log(n\times2^m))$ ，有点难以接受，可以用字典树来统计每个集合出现了多少次。</p>
<p>最后的解法：</p>
<p>先将数组们按照 $w$ 排序，然后用双指针 $l,r$，先不断将 $r$ 右移，找到第一个可以和前 $r-1$ 个数匹配的位置，然后将 $l$ 从这个 $r$ 的位置不断左移，直到 $r$ 不能再和前 $l$ 个数组匹配为止。</p>
<p>这样我们就找到了第一组解，因为 $w$ 是排序好的，后续 $l$ 不需要再向右移动，右移 $r$ 的过程中发现前 $l$  个数组与 $r$ 不匹配了，就将 $l$ 左移，然后更新答案。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> Tire&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">tire</span>&#123;</span><br><span class="line">        map&lt;<span class="type">int</span>, <span class="type">int</span>&gt; ch;</span><br><span class="line">        <span class="type">int</span> num;</span><br><span class="line">    &#125;t[<span class="number">3200020</span>];</span><br><span class="line">    <span class="type">int</span> cnt = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">insert</span><span class="params">(vector&lt;<span class="type">int</span>&gt; &amp;arr, <span class="type">int</span> ad)</span></span>&#123;</span><br><span class="line">        <span class="type">int</span> p = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> v:arr)&#123;</span><br><span class="line">            <span class="keyword">if</span>(!t[p].ch.<span class="built_in">count</span>(v)) t[p].ch[v] = ++cnt;</span><br><span class="line">            p = t[p].ch[v];</span><br><span class="line">        &#125;</span><br><span class="line">        t[p].num += ad;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">query</span><span class="params">(vector&lt;<span class="type">int</span>&gt; &amp;arr)</span></span>&#123;</span><br><span class="line">        <span class="type">int</span> p = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> v:arr)&#123;</span><br><span class="line">            <span class="keyword">if</span>(!t[p].ch.<span class="built_in">count</span>(v)) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            p = t[p].ch[v];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> t[p].num;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">node</span>&#123;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; v;</span><br><span class="line">    <span class="type">int</span> w;</span><br><span class="line">&#125;a[maxn];</span><br><span class="line"> </span><br><span class="line">vector&lt;<span class="type">int</span>&gt; csd;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">add</span><span class="params">(<span class="type">int</span> p, vector&lt;<span class="type">int</span>&gt; &amp;arr, <span class="type">int</span> ad)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(p &gt;= arr.<span class="built_in">size</span>())&#123;</span><br><span class="line">        <span class="keyword">if</span>(csd.<span class="built_in">empty</span>()) <span class="keyword">return</span>;</span><br><span class="line">        <span class="keyword">if</span>(ad &gt; <span class="number">0</span>) Tire::<span class="built_in">insert</span>(csd, <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">else</span> Tire::<span class="built_in">insert</span>(csd, <span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">add</span>(p+<span class="number">1</span>, arr, ad);</span><br><span class="line">    csd.<span class="built_in">push_back</span>(arr[p]);</span><br><span class="line">    <span class="built_in">add</span>(p+<span class="number">1</span>, arr, ad);</span><br><span class="line">    csd.<span class="built_in">pop_back</span>();</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">calcu</span><span class="params">(<span class="type">int</span> p, vector&lt;<span class="type">int</span>&gt; &amp;arr)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(p &gt;= arr.<span class="built_in">size</span>()) <span class="keyword">return</span> Tire::<span class="built_in">query</span>(csd) * ((csd.<span class="built_in">size</span>() % <span class="number">2</span>) ? <span class="number">1</span> : <span class="number">-1</span>);</span><br><span class="line">    <span class="type">int</span> res = <span class="number">0</span>;</span><br><span class="line">    res += <span class="built_in">calcu</span>(p+<span class="number">1</span>, arr);</span><br><span class="line">    csd.<span class="built_in">push_back</span>(arr[p]);</span><br><span class="line">    res += <span class="built_in">calcu</span>(p+<span class="number">1</span>, arr);</span><br><span class="line">    csd.<span class="built_in">pop_back</span>();</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        a[i].v.<span class="built_in">resize</span>(m);</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=m;j++)&#123;</span><br><span class="line">            cin &gt;&gt; a[i].v[j<span class="number">-1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">sort</span>(a[i].v.<span class="built_in">begin</span>(), a[i].v.<span class="built_in">end</span>());</span><br><span class="line">        cin &gt;&gt; a[i].w;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">sort</span>(a+<span class="number">1</span>, a+n+<span class="number">1</span>, [](node &amp;x, node &amp;y)&#123; <span class="keyword">return</span> x.w &lt; y.w;&#125;);</span><br><span class="line">    <span class="type">int</span> l, r = n + <span class="number">1</span>, ans = <span class="number">2e9</span> + <span class="number">1</span>;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">calcu</span>(<span class="number">0</span>, a[i].v) == i<span class="number">-1</span>)&#123;</span><br><span class="line">            <span class="built_in">add</span>(<span class="number">0</span>, a[i].v, <span class="number">1</span>);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            l = i<span class="number">-1</span>, r = i+<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">while</span>(<span class="built_in">calcu</span>(<span class="number">0</span>, a[i].v) != l)&#123;</span><br><span class="line">                <span class="built_in">add</span>(<span class="number">0</span>, a[l].v, <span class="number">-1</span>);</span><br><span class="line">                ans = <span class="built_in">min</span>(ans, a[i].w + a[l].w);</span><br><span class="line">                l--;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=r;i&lt;=n &amp;&amp; l&gt;<span class="number">0</span>;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">calcu</span>(<span class="number">0</span>, a[i].v) != l)&#123;</span><br><span class="line">            <span class="keyword">while</span>(l &gt; <span class="number">0</span> &amp;&amp; <span class="built_in">calcu</span>(<span class="number">0</span>, a[i].v) != l)&#123;</span><br><span class="line">                <span class="built_in">add</span>(<span class="number">0</span>, a[l].v, <span class="number">-1</span>);</span><br><span class="line">                ans = <span class="built_in">min</span>(ans, a[i].w + a[l].w);</span><br><span class="line">                l--;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(ans &gt; (<span class="type">int</span>)<span class="number">2e9</span>) ans = <span class="number">-1</span>;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>2022浙江工商大学acm校赛总结</title>
    <url>/2022/03/14/2022-03-05-2022%E6%B5%99%E6%B1%9F%E5%B7%A5%E5%95%86%E5%A4%A7%E5%AD%A6%E6%A0%A1%E8%B5%9B/</url>
    <content><![CDATA[<p>开局首先乱开选到了炸弹题，快速想了个bfs搞法后就开始写了，连wa三发之后发现不对劲，仔细想了一下发现思路是假的，这个时候已经半个小时了，榜上两个签到题已经被过穿，我还只有一个-3，开局稀碎</p>
<p>然后选择先把代码存起来去写签到，回文子序列属于经典的那种cfA题，平时会打cf的新生写这个应该不需要5分钟（</p>
<p>质因数分解题 我还稍微卡了一会 数学属实是碰的太少了</p>
<p>搞完两个签到回去搞炸弹题，重新想了一下发现是个缩点，抄一波tarjian板子过掉</p>
<p>然后就开始了大寄特寄的0号题，一个sb最短路+路径计数，硬是找不到wa点，造的数据都过，交上去全是wa。再结合这个题目描述（直观看起来是无向图但是没有明说；边权只写了上限没有写下界；数据规模允许 $n^2$），总之存在诸多疑点，那么dij换成spfa试了试，无向图改有向图试试，同时改试试… 于是对 $n$ 项疑点开始 $2^n$ 爆搜式提交，然而都没有过（我现在还不知道为什么wa）</p>
<p>此时的情况非常不妙，这个0号题已经过了十几个人了，差不多是所有大二三 + 一些新生，感觉非常危，但硬wa也没什么办法，于是弃题去搞别的了</p>
<p>很快发现取石子是个诈骗题，直接输出Alice，没关同步流还T一发</p>
<p>然后发现一棵刻入DNA的线段树，快速切掉没有什么压力</p>
<p>接着是那个函数题，看错题目绕了一会，回过神来发现其实开个优先队列乱搞就行了，$nlogn$ 没有什么问题</p>
<p>到这个时候差不多还有四十分钟，我做了个错误的决定：继续回去卡那个最短路…于是又是交了一堆wa，中途简单看了一下字符串题和dp题，字符串题面稀碎，没读明白；dp题想了个有点复杂的搞法，感觉时间来不及而且不一定对，就回去wa最短路了…一直wa到比赛结束，最后-19没搞出来，但场上一共过了差不多20个，快变成签到了。</p>
<p>最后6题rk3，没打过大一爷，非常不满意（寄，主要是该写出来的没写出来</p>
<p>总结一下，感觉心态还有点问题，一wa就容易乱交，另外弃题弃的不够彻底，dp题不一定能搞出来，但那个字符串，稍微花点时间读明白的话就会发现是个sb字典树，最后时间不重新回去卡最短路的话，这个也应该能出来（虽然出来了还是打不过大一爷，orz</p>
<h2 id="补题："><a href="#补题：" class="headerlink" title="补题："></a>补题：</h2><h4 id="Dwendwen给大佬分组"><a href="#Dwendwen给大佬分组" class="headerlink" title="Dwendwen给大佬分组"></a>Dwendwen给大佬分组</h4><p>众所周知，实验室的同学们都是大佬。现在实验室共有N位大佬，为了发挥大佬们的辐射能力，Dwendwen准备按座位将其分为k个学习小组。</p>
<p>为了简化问题，我们假设实验室的座位是一种长度为N（N个座位）的线状结构，坐在第i个位置的大佬有影响力ai。在不能调整座位的前提下，Dwendwen需要将大佬们分为k组，其中每个组内的大佬都是座位连续的。一个小组的战斗力为小组所有大佬中，最大影响力与最小影响力之差，即max(a)-min(a)。</p>
<p>现在Dwendwen希望让实验室k个小组的总战斗力最大化，你能帮帮他吗？</p>
<h5 id="Input"><a href="#Input" class="headerlink" title="Input:"></a>Input:</h5><p>第一行为两个空格分割的整数N(1≤N≤10000)和k(1≤k≤N)，分别表示实验室人数和小组个数。</p>
<p>第二行为N个空格分割的正整数，第i个数字ai(1≤ai≤500000)为坐在第i个位置的大佬的影响力。</p>
<h5 id="Output"><a href="#Output" class="headerlink" title="Output:"></a>Output:</h5><p>实验室所有小组的总战斗力的最大值。</p>
<h5 id="Sample-Input"><a href="#Sample-Input" class="headerlink" title="Sample Input:"></a>Sample Input:</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">5 1</span><br><span class="line">2 4 5 6 3</span><br></pre></td></tr></table></figure>

<h5 id="Sample-Output"><a href="#Sample-Output" class="headerlink" title="Sample Output:"></a>Sample Output:</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">4</span><br></pre></td></tr></table></figure>



<p>先是朴素的 $n^3$ dp做法，$dp[i][j]$ 表示前 $i$ 个数，分成 $j$ 组的最大答案</p>
<p>转移有 $dp[i][j] &#x3D; dp[k][j-1]+max(a[k+1..i])-min(a[k+1..i])$，枚举这个 $k$</p>
<p>第一反应是拿某种单调队列来优化掉这一维 $k$，但是比较难搞</p>
<p>题解给了个不错的思路：放宽解空间</p>
<p>也就是说，我们可以把题意转换一下，转换后多了一些无意义的解，并不影响答案，但是能让优化变得更简单</p>
<p>本题的权值是 $max-min$，典中典，我们可以把它改成<strong>任意两数相减</strong>，显然最大的结果仍然是 $max-min$</p>
<p>那么转移时，考虑新加进来的 $a[i]$ 的成分：</p>
<ul>
<li>不参与加减运算，$dp[i][j] &#x3D; dp[i-1][j]$</li>
<li>作为被减数，$dp[i][j] &#x3D; dp[k][j-1]+a[k+1]-a[i]$，这里相当于在枚举减数</li>
<li>作为减数，$dp[i][j]&#x3D;dp[k][j-1]+a[i]-a[k+1]$，相当于枚举被减数</li>
</ul>
<p>可以发现，我们只需要知道 $dp[k][j-1]+a[k+1]$ 的最大值，以及 $dp[k][j-1]-a[k+1]$ 的最大值，就能做到 $O(1)$ 转移了</p>
<p>这两个值沿途记录一下即可，不难维护。</p>
<hr>
<p>再写另一种理解方式：</p>
<p>原题中，一个区间的权值是 $max-min$，那么可以发现，如果一个区间的边界元素不是其最大或最小值，那么把这个边界元素丢给隔壁区间一定不会更差。</p>
<p>所以最后我们其实是把整个数组分成了两种区间：两端分别是极大极小值的区间，和放哪都一样的区间（可以忽略的区间）</p>
<p>那么 $dp$ 转移的思路也比较清晰了（dp含义和上文相同）：</p>
<ul>
<li>当前元素不是区间的边界（在区间内部或者被忽略），$dp[i][j] &#x3D; dp[i-1][j]$</li>
<li>当前元素是区间边界，且是极小值，$dp[i][j] &#x3D; dp[k][j-1]+a[k+1]-a[i]$</li>
<li>当前元素是区间边界，且是极大值，$dp[i][j]&#x3D;dp[k][j-1]+a[i]-a[k+1]$</li>
</ul>
<p>就和上面的转移方程是一样的。</p>
<p>最后，这个题的数据范围是 $10^4$，滚动数组优化一下空间。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="type">int</span> a[maxn];</span><br><span class="line"></span><br><span class="line">ll f[<span class="number">10003</span>][<span class="number">2</span>];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cin &gt;&gt; a[i];</span><br><span class="line">    <span class="type">int</span> mxa = a[<span class="number">1</span>], mia = a[<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        mxa = <span class="built_in">max</span>(mxa, a[i]);</span><br><span class="line">        mia = <span class="built_in">min</span>(mia, a[i]);</span><br><span class="line">        f[i][<span class="number">1</span>] = mxa - mia;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> k=<span class="number">2</span>;k&lt;=m;k++)&#123;</span><br><span class="line">        <span class="type">int</span> nw = k % <span class="number">2</span>;</span><br><span class="line">        f[k<span class="number">-1</span>][nw] = <span class="number">0</span>;</span><br><span class="line">        ll mx = f[k<span class="number">-1</span>][nw^<span class="number">1</span>] + a[k], mi = a[k] - f[k<span class="number">-1</span>][nw^<span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=k;i&lt;=n;i++)&#123;</span><br><span class="line">            f[i][nw] = <span class="built_in">max</span>(&#123;f[i<span class="number">-1</span>][nw], mx - a[i], a[i] - mi&#125;);</span><br><span class="line">            mx = <span class="built_in">max</span>(mx, f[i][nw^<span class="number">1</span>] + a[i+<span class="number">1</span>]);</span><br><span class="line">            mi = <span class="built_in">min</span>(mi, a[i+<span class="number">1</span>] - f[i][nw^<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; f[n][m%<span class="number">2</span>] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>题解</category>
        <category>总结</category>
      </categories>
      <tags>
        <tag>题解</tag>
        <tag>校赛</tag>
      </tags>
  </entry>
  <entry>
    <title>openGL入门</title>
    <url>/2022/03/17/2022-03-17-openGL%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<p><a href="https://learnopengl-cn.github.io/">https://learnopengl-cn.github.io/</a></p>
<p>基本是跟着这个网站来的，感谢大佬们提供了这么好的资源</p>
<h4 id="关于VBO，VAO"><a href="#关于VBO，VAO" class="headerlink" title="关于VBO，VAO"></a>关于VBO，VAO</h4><p>首先关注他们的名字：</p>
<ul>
<li>顶点缓冲对象：Vertex Buffer Object，<strong>VBO</strong></li>
<li>顶点数组对象：Vertex Array Object，<strong>VAO</strong></li>
</ul>
<p>每个顶点可能会包含一堆数据（常见的有坐标，颜色等），CPU需要把这些数据发送给显卡，为了一次性发送大量数据，会使用一个缓冲来暂存顶点数据，这就是VBO，它仅仅只是一堆本质上没区别的数据，具体如何访问这些数据，就是VAO决定的。</p>
<p>可以认为，VAO对VBO的数据定义了一种分类，组装的方式，将一堆数据拼成了有意义的一个数组，VAO就是把这种访问方式保存下来。</p>
<p>具体而言，我们可以创建一个VBO来保存东西：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">int</span> VBO;</span><br><span class="line"><span class="comment">//glGenXXX就是创建一个对象，为其分配空间</span></span><br><span class="line"><span class="built_in">glGenBuffers</span>(<span class="number">1</span>, &amp;VBO);</span><br><span class="line"><span class="comment">//将VBO绑定上缓冲GL_ARRAY_BUFFER，可以理解为设置状态：当前存储顶点的位置是VBO</span></span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ARRAY_BUFFER, VBO);</span><br><span class="line"><span class="comment">//往一个缓冲中塞数据，这时的VBO已经绑定GL_ARRAY_BUFFER，这样就能保存在这个VBO当中</span></span><br><span class="line"><span class="built_in">glBufferData</span>(GL_ARRAY_BUFFER, <span class="built_in">sizeof</span>(dataSource), dataSource, GL_STATIC_DRAW);</span><br></pre></td></tr></table></figure>

<p>这样就有一块纯纯的数据了，可以用下面两行东西来指定这块东西的解读方式：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//设置对数据的解读方式</span></span><br><span class="line"><span class="comment">//首先，每个顶点属性有一个属性值（标号）（即vertexShader中的layout）</span></span><br><span class="line"><span class="comment">//这里的意思是：对于属性值为0的属性，它要读取3个float，不需要标准化，下一个顶点的对应属性在3个步长之后，从0开始读。</span></span><br><span class="line"><span class="comment">//然后设置：将属性值为0的顶点属性启用（默认是禁用的）</span></span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">0</span>, <span class="number">3</span>, GL_FLOAT, GL_FALSE, <span class="number">3</span> * <span class="built_in">sizeof</span>(<span class="type">float</span>), (<span class="type">void</span>*)<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>然后就能一行完成渲染啦！第一个参数为图元装配方式，指定为三角形，也就是说会把我们输入的顶点，每三个组成一个三角形传入片元着色器。从0位置开始，一共读3个顶点</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">glDrawArrays</span>(GL_TRIANGLES, <span class="number">0</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<p>因为openGL是一个大状态机，在渲染不同物体时，必须绑定对应的VBO，更改对应的解读方式才行。这样换起来太麻烦了，于是有了VAO：它能把这些都保存起来，然后直接绑定VAO就能自动绑定上VBO和切换glVertexAttribPointer。为什么它叫<strong>顶点数组对象</strong>呢？我理解为，它获取到一块数据和对应的解读方式，就直接将顶点分类包装成数组了。它具体存储的是什么，目前理解比较浅。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">int</span> VAO;</span><br><span class="line"><span class="built_in">glGenVertexArrays</span>(<span class="number">1</span>, &amp;VAO);</span><br><span class="line"><span class="built_in">glBindVertexArray</span>(VAO);</span><br><span class="line">...</span><br><span class="line"><span class="built_in">glBindVertexArray</span>(<span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>VAO是工作在bind和unbind之间的，也就是说，在glVertexAttribPointer之前，先将VAO绑定，然后VAO将记录你执行的glVertexAttribPointer，保存这一解读方式（同时也保存了当前绑定的VBO），记录完成后解绑VAO即可。渲染时如果要调用这组数据，直接绑定这个VAO就可以了，将自动设置glVertexAttribPointer和VBO，非常方便。</p>
<p><img src="https://learnopengl-cn.github.io/img/01/04/vertex_array_objects.png"></p>
<h5 id="对于更多顶点属性的glVertexAttribPointer实例："><a href="#对于更多顶点属性的glVertexAttribPointer实例：" class="headerlink" title="对于更多顶点属性的glVertexAttribPointer实例："></a>对于更多顶点属性的glVertexAttribPointer实例：</h5><p>如果顶点包含两个属性：位置和颜色，分别将属性值设置为0和1</p>
<p>属性值只是一种标号，和内存地址无关</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#version 330 core</span></span><br><span class="line"><span class="built_in">layout</span> (location = <span class="number">0</span>) in vec3 aPos;   <span class="comment">// 位置变量的属性位置值为 0 </span></span><br><span class="line"><span class="built_in">layout</span> (location = <span class="number">1</span>) in vec3 aColor; <span class="comment">// 颜色变量的属性位置值为 1</span></span><br></pre></td></tr></table></figure>

<p>然后是VBO中的数据，里面有三个顶点：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> vertices[] = &#123;</span><br><span class="line">    <span class="comment">// 位置              // 颜色</span></span><br><span class="line">     <span class="number">0.5f</span>, <span class="number">-0.5f</span>, <span class="number">0.0f</span>,  <span class="number">1.0f</span>, <span class="number">0.0f</span>, <span class="number">0.0f</span>,   <span class="comment">// 右下</span></span><br><span class="line">    <span class="number">-0.5f</span>, <span class="number">-0.5f</span>, <span class="number">0.0f</span>,  <span class="number">0.0f</span>, <span class="number">1.0f</span>, <span class="number">0.0f</span>,   <span class="comment">// 左下</span></span><br><span class="line">     <span class="number">0.0f</span>,  <span class="number">0.5f</span>, <span class="number">0.0f</span>,  <span class="number">0.0f</span>, <span class="number">0.0f</span>, <span class="number">1.0f</span>    <span class="comment">// 顶部</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>对于这样的情况，解读方式应该是：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//位置属性</span></span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">0</span>, <span class="number">3</span>, GL_FLOAT, GL_FALSE, <span class="number">6</span> * <span class="built_in">sizeof</span>(<span class="type">float</span>), (<span class="type">void</span>*)<span class="number">0</span>);</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">0</span>);</span><br><span class="line"><span class="comment">//颜色属性</span></span><br><span class="line"><span class="built_in">glVertexAttribPointer</span>(<span class="number">1</span>, <span class="number">3</span>, GL_FLOAT, GL_FALSE, <span class="number">6</span> * <span class="built_in">sizeof</span>(<span class="type">float</span>), (<span class="type">void</span>*)(<span class="number">3</span>* <span class="built_in">sizeof</span>(<span class="type">float</span>)));</span><br><span class="line"><span class="built_in">glEnableVertexAttribArray</span>(<span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>注意：属性长度仍然是3，两个属性都只需要读3float的数据，步长是6，因为一个位置属性和下一个位置属性间隔6float，颜色属性的偏移为3，即起始点从3开始，6格6格跳</p>
<h4 id="EBO-索引缓冲"><a href="#EBO-索引缓冲" class="headerlink" title="EBO 索引缓冲"></a>EBO 索引缓冲</h4><p>实际绘制物体时，可以用一个个小三角拼成，比如要拼一个正方形，顶点数组可以这样：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">float</span> dataSource2[] = &#123;</span><br><span class="line">        <span class="number">0.9f</span>, <span class="number">0.9f</span>, <span class="number">0.0f</span>,</span><br><span class="line">        <span class="number">0.9f</span>, <span class="number">0.5f</span>, <span class="number">0.0f</span>,</span><br><span class="line">        <span class="number">0.5f</span>, <span class="number">0.5f</span>, <span class="number">0.0f</span>,</span><br><span class="line"></span><br><span class="line">        <span class="number">0.5f</span>, <span class="number">0.5f</span>, <span class="number">0.0f</span>,</span><br><span class="line">        <span class="number">0.5f</span>, <span class="number">0.9f</span>, <span class="number">0.0f</span>,</span><br><span class="line">        <span class="number">0.9f</span>, <span class="number">0.9f</span>, <span class="number">0.0f</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>图元装配时指定装配6个顶点，可以轻松得到一个正方形，它是两个三角形拼起来的</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">glDrawArrays</span>(GL_TRIANGLES, <span class="number">0</span>, <span class="number">6</span>);</span><br></pre></td></tr></table></figure>

<p>但是这样显然有大量冗余，所谓索引缓冲对象，就是指定了遍历顶点的方式，默认是从0..n这样遍历的，</p>
<p>我们可以指定遍历顺序。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">unsigned</span> <span class="type">int</span> indices[] = &#123; <span class="comment">// 注意索引从0开始!</span></span><br><span class="line">        <span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>, <span class="comment">// 第一个三角形</span></span><br><span class="line">        <span class="number">2</span>, <span class="number">3</span>, <span class="number">0</span>  <span class="comment">// 第二个三角形</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> EBO;</span><br><span class="line"><span class="built_in">glGenBuffers</span>(<span class="number">1</span>, &amp;EBO);</span><br><span class="line"><span class="built_in">glBindBuffer</span>(GL_ELEMENT_ARRAY_BUFFER, EBO);</span><br><span class="line"><span class="built_in">glBufferData</span>(GL_ELEMENT_ARRAY_BUFFER, <span class="built_in">sizeof</span>(indices), indices, GL_STATIC_DRAW);</span><br></pre></td></tr></table></figure>

<p>这里用和VBO相似的方式，配置了一个EBO，同样地，这个信息可以被VAO存储，只需要写在VAO的bind和unbind之间即可。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">glDrawElements</span>(GL_TRIANGLES, <span class="number">6</span>, GL_UNSIGNED_INT, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>把绘图函数改成这个，即可按照当前绑定在GL_ELEMENT_ARRAY_BUFFER上的索引表来遍历。</p>
<h4 id="着色器"><a href="#着色器" class="headerlink" title="着色器"></a>着色器</h4><p>主要编写的是顶点着色器和片段着色器</p>
<p>顶点着色器的输出信息在光栅化阶段会被各种“插值”，变成大量片元输出到片元着色器。比如经典的：用三个顶点坐标显示一个宏观的三角形，实际上片元着色器接受到的片段有成百上千个，但我们仅输入了三个顶点，每一个片段的具体位置均是根据插值得到的。</p>
<p>同时，各种其他顶点输出的信息也都会被插值，比如颜色等等。</p>
<p>着色器中可以使用uniform定义一个唯一的变量，在外部通过一些操作更改（实现外部直接把值传入shader任意一个阶段）</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="keyword">uniform</span> <span class="type">float</span> offX = <span class="number">0</span>, offY = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>

<p>外部，传入名称和值即可，glUniform后+数字+类型，如3f表示更改的是一个3维float向量类型</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">glUniform1i</span>(<span class="built_in">glGetUniformLocation</span>(shaderProgram, name), value);</span><br></pre></td></tr></table></figure>

<p>外部获取这个值？没有找到资料，可能从外部获取它的值本就不合逻辑</p>
<p>uniform不能在shader内部复制，但可以初始化（设置缺省值）</p>
<h5 id="特殊的uniform"><a href="#特殊的uniform" class="headerlink" title="特殊的uniform"></a>特殊的uniform</h5><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">uniform sampler2D texture1;</span><br></pre></td></tr></table></figure>

<p>用于获取纹理，它的值可以用int方式更改，如果它为2，那么它就表示GL_TEXTURE2这个纹理单元上的纹理图片，在shader中使用 <code>texture(texture1, Texpos) </code>获得这张纹理具体位置上的颜色。</p>
<p>目前不太清楚的是，这个sampler2D属性似乎在我渲染的什么阶段会被清空，需要每次useshader的时候都设置一次，不像其他shader中的属性设置一次即可</p>
<h4 id="变换和摄像机"><a href="#变换和摄像机" class="headerlink" title="变换和摄像机"></a>变换和摄像机</h4><p>处理变换是一定谨记顺序：我们一般将被变换的矩阵放在右边，在左边乘上变换矩阵。另外为了不将“朝向”，“旋转”等概念混淆，我们只需要记住：我们始终是在对单个顶点作变换。</p>
<p>一开始这个顶点具备的是它自己的局部坐标，我们用一个transform矩阵表示这整个物体经历的变化，顶点着色器中，用transform*顶点坐标得到顶点的世界坐标。</p>
<p>如果我们希望在物体当前状态的基础上叠加一个变换 $p$，可以在它的transform左边乘上 $p$。但需要注意整个变换矩阵的含义：即让顶点在经历了已有的transform之后，进行一个 $p$ 变化。思考一下：对transform左乘一个位移矩阵，是在其自身坐标系下位移还是在世界坐标系下位移？</p>
<p>之所以这么考虑，是因为transform中可能含有旋转</p>
<p>即：先旋转，再位移。从顶点的角度考虑，它仍然相当于在世界坐标系下进行了这一位移。</p>
<p>如果对transform右乘呢？即先移动，再旋转。那么这一位移相当于也被旋转了，相当于在自身坐标系下进行了这一位移。</p>
<p>总结：当由于旋转导致自身朝向（坐标系）和世界不一致时，<strong>左乘（接着变换）相当于在世界坐标系下变换，右乘（提前变换）相当于在自身坐标系下变换。</strong></p>
<p>测试了一下发现<code>glm::translate</code>和glm的旋转，缩放等方法，都是相当于在右边乘了一个变换矩阵。</p>
<p>矩阵让我们得以将物体的平移，旋转缩放组合，得到一个<strong>transform</strong>矩阵，仅仅需要这个矩阵，我们就能保存物体当前的状态。</p>
<figure class="highlight glsl"><table><tr><td class="code"><pre><span class="line"><span class="built_in">gl_Position</span> = projection * view * transform * <span class="type">vec4</span>(aPos, <span class="number">1.0</span>);</span><br></pre></td></tr></table></figure>

<p>在顶点着色器中通常有这种形式</p>
<ul>
<li>transform矩阵将局部坐标转化到世界坐标</li>
<li>view矩阵将世界坐标转化为观察坐标，即我们构想的摄像机坐标系下的坐标</li>
<li>projection矩阵用于处理透视</li>
</ul>
<p>控制摄像机，其实就是控制这一个view矩阵，它其实就是摄像机这个物体的transform的逆矩阵，应该挺好理解的？</p>
<p>我们可以直接对摄像机的transform进行变换，将它实时应用到view上即可</p>
<h4 id="光照"><a href="#光照" class="headerlink" title="光照"></a>光照</h4><p>当我们把一个光照施加到一个物体上时，输出光照颜色和物体颜色的乘积（各个分量各自相乘）即可，注意这里的颜色量值域为(0, 1)，而非(0, 255)</p>
<p>冯氏光照模型主要分三部分：环境光照，漫反射，镜面反射</p>
<p><strong>环境光照</strong>：始终使物体接受到某个光照，例如(0.1, 0.1, 0.1)</p>
<p><strong>漫反射</strong>：对于每个片元，取其法向量和光照方向的点乘积（二者都标准化到模为1）作为光照强度，即正对光照的亮度高，背对的亮度低。（这个阶段我们一般不考虑阻挡、距离等因素）</p>
<p><strong>镜面反射</strong>：对每个片元，取其光照反射方向（通过法向量计算）和玩家视觉方向的乘积，作为光照强度，意义是玩家在某个片元反射方向附近时，能看到它上面的高光</p>
<p>问题来了，法线方向怎么来？</p>
<p>开始我认为这并不是一个难事，在用几个顶点拼接图元时，应该容易计算出这个图元的法向量。但很遗憾，openGL并不会帮我们做这件事情。</p>
<p>原因我感觉大概是：法线方向是要随着物体的变换而变换的，openGL最多帮我们计算好片元在自身坐标系下的法向量（如果它想），而通常，物体的变换和观察矩阵都储存在顶点着色器中，法线的变换也应当在顶点着色器中进行，这符合直觉。片元的数量是顶点的千百倍，在fs上运算是非常低效的。</p>
<p>因此，很遗憾，通常法线方向需要我们直接写在顶点信息中，在顶点着色器中经历法线变换后输出。如果是平滑曲面，直接由片元着色器插值得到具体片元的法线方向；如果是棱角上的顶点，则必须拆分成多个顶点，对每个连接面写不同法线方向。</p>
<p>一般模型顶点数据中会自带法线方向。</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>openGL</tag>
        <tag>计算机图形学</tag>
      </tags>
  </entry>
  <entry>
    <title>Codeforces Round 774 div.2 DEF</title>
    <url>/2022/03/05/2022-03-05-Codeforces%20Round%20774%20div.2%20DEF/</url>
    <content><![CDATA[<h4 id="D-Weight-the-Tree"><a href="#D-Weight-the-Tree" class="headerlink" title="D. Weight the Tree"></a>D. Weight the Tree</h4><p>题意：给定一棵树，每个节点有一权重 $w_i$，若一个节点的权值等于其相邻点的权值之和，则这个节点是好的。现在你可以为每个，使得好的点数量最多，在这基础上，还要使得总权值最小，输出赋值方案。</p>
<p>首先想到，除了 $n&#x3D;2$ 的情况，相邻两个点不可能都是好的</p>
<p>对于不好的点，肯定让它为1（$w_i\ge1$），那么好的点的权值一定等于其度数，我们要选上一些不相邻的点使得它<strong>好</strong>。</p>
<p>考虑树上dp，$dp[i,0&#x2F;1]$ 表示以 $i$ 为根节点的子树，选&#x2F;不选根节点时的最优解。这里比较难想的一点在于，解居然包括了两个信息：好节点数量和总权值。似乎和通常的dp不太一样，但仔细想想是没问题的，因为两个解仍然可以排序：先按照好节点数，再按照总权值。</p>
<p>当前节点选，则其子节点都不能选，当前节点不选，则其子节点任意。</p>
<p>最后要输出方案，从上往下跑一遍就可以了：每个节点的 $dp[i,1]$ 和 $dp[i, 0]$ 比较一下，确定当前点是否选，如果其父节点选了，则强制这个点不选，这样往下搜下去就能确定方案了，</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, du[maxn];</span><br><span class="line">vector&lt;<span class="type">int</span>&gt; vp[maxn];</span><br><span class="line"><span class="type">int</span> f[maxn][<span class="number">2</span>], g[maxn][<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">chose</span><span class="params">(<span class="type">int</span> x)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(f[x][<span class="number">1</span>] == f[x][<span class="number">0</span>]) <span class="keyword">return</span> g[x][<span class="number">1</span>] &lt; g[x][<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">return</span> f[x][<span class="number">1</span>] &gt; f[x][<span class="number">0</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">dfs</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> fa = <span class="number">-1</span>)</span></span>&#123;</span><br><span class="line">    f[p][<span class="number">0</span>] = <span class="number">0</span>, f[p][<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">    g[p][<span class="number">0</span>] = <span class="number">1</span>, g[p][<span class="number">1</span>] = du[p];</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> v: vp[p])&#123;</span><br><span class="line">        <span class="keyword">if</span>(v != fa)&#123;</span><br><span class="line">            <span class="built_in">dfs</span>(v, p);</span><br><span class="line">            f[p][<span class="number">1</span>] += f[v][<span class="number">0</span>];</span><br><span class="line">            g[p][<span class="number">1</span>] += g[v][<span class="number">0</span>];</span><br><span class="line">            <span class="type">int</span> cs = <span class="built_in">chose</span>(v);</span><br><span class="line">            f[p][<span class="number">0</span>] += f[v][cs];</span><br><span class="line">            g[p][<span class="number">0</span>] += g[v][cs];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> ans[maxn];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">getans</span><span class="params">(<span class="type">int</span> p, <span class="type">int</span> fa, <span class="type">int</span> cs)</span></span>&#123;</span><br><span class="line">    ans[p] = cs ? du[p] : <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> v: vp[p])&#123;</span><br><span class="line">        <span class="keyword">if</span>(v != fa)&#123;</span><br><span class="line">            <span class="keyword">if</span>(cs) <span class="built_in">getans</span>(v, p, <span class="number">0</span>);</span><br><span class="line">            <span class="keyword">else</span> <span class="built_in">getans</span>(v, p, <span class="built_in">chose</span>(v));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">if</span>(n == <span class="number">2</span>)&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;2 2\n1 1\n&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;n;i++)&#123;</span><br><span class="line">        <span class="type">int</span> x, y;</span><br><span class="line">        cin &gt;&gt; x &gt;&gt; y;</span><br><span class="line">        du[x]++, du[y]++;</span><br><span class="line">        vp[x].<span class="built_in">emplace_back</span>(y);</span><br><span class="line">        vp[y].<span class="built_in">emplace_back</span>(x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">dfs</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="type">int</span> cs = <span class="built_in">chose</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">getans</span>(<span class="number">1</span>, <span class="number">-1</span>, cs);</span><br><span class="line">    cout &lt;&lt; f[<span class="number">1</span>][cs] &lt;&lt; <span class="string">&#x27; &#x27;</span> &lt;&lt; g[<span class="number">1</span>][cs] &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) cout &lt;&lt; ans[i] &lt;&lt; <span class="string">&quot; \n&quot;</span>[i == n];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="E-Power-Board"><a href="#E-Power-Board" class="headerlink" title="E. Power Board"></a>E. Power Board</h4><p>题意：给出一个 $n\times m$ 的图，$(i,j)$ 点上的数字为 $i^j$ ，请问这个图里有多少个不同的数字？</p>
<p><a href="https://codeforces.com/blog/entry/100584">没法比题解写的更清楚了</a>（摆</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n, m;</span><br><span class="line"><span class="type">bool</span> vis[maxn &lt;&lt; <span class="number">5</span>];</span><br><span class="line"><span class="type">int</span> cnt[<span class="number">32</span>];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n &gt;&gt; m;</span><br><span class="line">    <span class="type">int</span> tot = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=<span class="number">23</span>;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=m;j++)&#123;</span><br><span class="line">            <span class="keyword">if</span>(!vis[i * j])&#123;</span><br><span class="line">                vis[i * j] = <span class="literal">true</span>;</span><br><span class="line">                tot++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        cnt[i] = tot;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">fill</span>(vis, vis+n+<span class="number">1</span>, <span class="literal">false</span>);</span><br><span class="line">    ll ans = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">2</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(!vis[i])&#123;</span><br><span class="line">            <span class="type">int</span> k = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span>(ll j=i;j&lt;=n;j*=i)&#123;</span><br><span class="line">                vis[j] = <span class="literal">true</span>;</span><br><span class="line">                k++;</span><br><span class="line">            &#125;</span><br><span class="line">            ans += cnt[k];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h4 id="F-Playing-Around-the-Table"><a href="#F-Playing-Around-the-Table" class="headerlink" title="F. Playing Around the Table"></a>F. Playing Around the Table</h4><p>题意：有 $n$ 名玩家坐成一个环，一共有 $n^2$ 张卡牌，$1-n$ 每种卡牌各 $n$ 张，开局每名玩家持有其中 $n$ 张，接下来进行游戏：每轮中，每名玩家交给下一名玩家一张卡牌（所有人同时进行），如果玩家 $i$ 的所有卡牌都为 $i$，则其“牢固”。现要你构造一种游戏方案，至多 $n^2-n$ 轮，使得每名玩家都“牢固”。</p>
<p>一开始有个暴力的想法，每轮每个人都直接找一个不属于自己的卡牌传出去，然而这样显然会遇到死循环的情况。</p>
<p>全变成一样的不好搞，能不能先全变成不一样的呢？也就是让每个人都拥有1到n的所有卡牌。</p>
<p>这个比较好搞，每次每人都找一张自己有重复的卡牌交出去，如果自己没有重复的，就把上一个人传过来的交出去，这样最终一定能满足。</p>
<p>第一步能在可接受的步数内完成吗？对于每一种卡牌，考虑其最坏的情况：初始所有这张牌都在一个人手上，那么根据上述传递方案，这张牌一共会被传递 $1+2+…+n-1$ 次，即 $\frac{n\times(n-1)}{2}$ 次，那么一共 $n$ 张牌，一轮能传递 $n$ 次，总轮次不会超过 $\frac{n\times(n-1)}{2}$。</p>
<p>现在所有人的牌都是 $1..n$ 了，下一步怎么搞都行</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> n;</span><br><span class="line"><span class="type">int</span> cnt[<span class="number">103</span>][<span class="number">103</span>];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">solve</span><span class="params">()</span></span>&#123;</span><br><span class="line">    cin &gt;&gt; n;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;j++)&#123;</span><br><span class="line">            <span class="type">int</span> x; cin &gt;&gt; x;</span><br><span class="line">            cnt[i][x]++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">pass</span><span class="params">(n+<span class="number">1</span>, <span class="number">0</span>)</span></span>;</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; ans;</span><br><span class="line">    <span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">            pass[i] = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;=n;j++)&#123;</span><br><span class="line">                <span class="keyword">if</span>(cnt[i][j] &gt; <span class="number">1</span>)&#123;</span><br><span class="line">                    pass[i] = j;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) <span class="keyword">if</span>(pass[i] == <span class="number">0</span>) pass[i] = pass[(i<span class="number">-1</span>+n<span class="number">-1</span>)%n+<span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++) <span class="keyword">if</span>(pass[i] == <span class="number">0</span>) pass[i] = pass[(i<span class="number">-1</span>+n<span class="number">-1</span>)%n+<span class="number">1</span>];</span><br><span class="line">        <span class="keyword">if</span>(pass[<span class="number">1</span>] == <span class="number">0</span>) <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">            cnt[i][pass[i]]--;</span><br><span class="line">            cnt[i%n+<span class="number">1</span>][pass[i]]++;</span><br><span class="line">        &#125;</span><br><span class="line">        ans.<span class="built_in">push_back</span>(pass);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> k=<span class="number">1</span>;k&lt;n;k++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> d=k;d&gt;=<span class="number">1</span>;d--)&#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">                pass[i] = (i + d - <span class="number">1</span>) % n + <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            ans.<span class="built_in">push_back</span>(pass);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; ans.<span class="built_in">size</span>() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">auto</span> ps: ans)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;ps.<span class="built_in">size</span>();i++) cout &lt;&lt; ps[i] &lt;&lt; <span class="string">&quot; \n&quot;</span>[i == ps.<span class="built_in">size</span>()<span class="number">-1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>题解</category>
      </categories>
      <tags>
        <tag>codeforces</tag>
        <tag>题解</tag>
      </tags>
  </entry>
  <entry>
    <title>旋转，四元数</title>
    <url>/2022/03/17/2022-03-17-%E5%9B%9B%E5%85%83%E6%95%B0%E7%9A%84%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<p>最早接触四元数是unity3d里面的旋转，当时不太理解使用四元数的意义（在我看来欧拉角已经可以完全表示一个朝向了），后来在旋转时出了很多莫名其妙的问题，发现有必要remake一下。</p>
<p>参考资料，感谢大佬们</p>
<p><a href="https://krasjet.github.io/quaternion/quaternion.pdf">https://krasjet.github.io/quaternion/quaternion.pdf</a></p>
<p><a href="https://www.bilibili.com/video/BV1SW411y7W1?spm_id_from=333.999.0.0">https://www.bilibili.com/video/BV1SW411y7W1?spm_id_from=333.999.0.0</a></p>
<h4 id="Unity的骗局"><a href="#Unity的骗局" class="headerlink" title="Unity的骗局"></a>Unity的骗局</h4><p>提问一个问题：物体Inspector面板中的rotation，到底指的是“<strong>绕自身坐标轴旋转</strong>”，还是“<strong>绕世界坐标轴旋转</strong>”？</p>
<p>如果你已经非常了解旋转了，可能会觉得这个问题莫名其妙</p>
<p>但如果你没发现这个问题有什么不对，建议一定去尝试一下，多试几次。</p>
<p>结论：<strong>有时绕世界坐标转，有时绕自身坐标转</strong></p>
<p>unity当中，我们直接更改Inspector面板属性，一切旋转“看起来”都是那么自然，以至于我们一直把旋转当做一个“简单”的东西。产生上述悖论的原因在于，我们从根本上错误理解了旋转。</p>
<h4 id="rotation的三个值到底是什么"><a href="#rotation的三个值到底是什么" class="headerlink" title="rotation的三个值到底是什么"></a>rotation的三个值到底是什么</h4><p>如果你随便拿一支笔（不要用unity），用手先绕X轴旋转90度，再绕Y轴旋转90度；再反过来试一下，会发现得到的位置不一样了。</p>
<p><strong>旋转不能交换顺序</strong>，这本质上是因为矩阵乘法不具有交换律</p>
<p>所以unity中有一个<strong>顺规</strong>，rotation的(x,y,z)的含义是：先绕世界坐标Z轴旋转z，再绕世界坐标X轴旋转x，最后绕世界坐标Y轴旋转y得到的方向。</p>
<p>悖论解决了：我们直接在rotation面板上修改z坐标值，实际上相当于是在另外两个轴旋转之前，令物体绕z轴旋转了一些。并不是直接在当前状态的基础上绕z轴旋转。</p>
<p>初学者常常（可能只有我）试图在代码中直接修改eulerAngle进行旋转，导致一堆无法解释的bug。</p>
<p>如果我们严格按照zxy的顺序去修改rotation，就能发现它是严格绕世界坐标轴旋转的。</p>
<h4 id="我们到底怎么表达旋转"><a href="#我们到底怎么表达旋转" class="headerlink" title="我们到底怎么表达旋转"></a>我们到底怎么表达旋转</h4><p>我们不再探究unity，试想一下，如果让我们自己设计，怎么样去维护一个物体的旋转，或者说朝向。</p>
<h5 id="欧拉角表示"><a href="#欧拉角表示" class="headerlink" title="欧拉角表示"></a>欧拉角表示</h5><p>即上文所说的，记录三个轴的旋转量，规定顺规，表达一个旋转。当然它有很多缺点：它不能连接和增量旋转，对同一方向的表达方式不唯一。</p>
<p>欧拉角也分静态欧拉角和动态欧拉角，静态欧拉角即三个轴都为世界坐标，动态欧拉角即三个轴都为自身坐标。使用动态欧拉角还会遇到万向节死锁的问题。</p>
<h5 id="旋转矩阵表示"><a href="#旋转矩阵表示" class="headerlink" title="旋转矩阵表示"></a>旋转矩阵表示</h5><p>我们通常用一个transform矩阵表达一个物体的变换，包含了物体的位置，大小，方向的等，它也是物体顶点从局部坐标变换到世界坐标时需要乘上的矩阵。</p>
<p>仅仅对物体施加一个特定的旋转不难：指定一个轴和旋转角度，都可以构建出一个旋转矩阵，让transform乘上这个矩阵即可。绕 $x,y,z$ 轴旋转的矩阵很容易推导，绕任意轴旋转的矩阵，也有一堆开源库帮我们构建。</p>
<p>旋转矩阵对一个方向的表达是唯一的。</p>
<p>注意顺序，在下面我们规定在左边乘矩阵为直接变换。</p>
<p>如果我们已有一个经历过旋转的矩阵 $T$，表示物体当前transofrm，现要进行某种位移，矩阵表示为 $p$，那么 $p\times T$ 为直接对 $T$ 进行一个位移，即全局坐标下的位移。而 $T\times p$ 意味着在所有变换之前，首先进行 $p$ 位移，那么在进行 $T$ 变换时，$p$ 这部分位移也会被旋转，相当于在 $T$ 的自身坐标系下位移。</p>
<p>如果我们想要无脑把变换矩阵乘在右边（glm的translate等），那么在我们希望在全局坐标系下对 $T$ 进行 $p$ 变换时，可以令 $p&#x3D;T^{-1}pT$，等价于 $p$ 乘在左边，这个形式是不是有点熟悉？相似矩阵正是为了“在另一个参考系下进行某种变换”而存在的。</p>
<p>简单总结：</p>
<ul>
<li>左边乘变换矩阵，相当于在世界坐标系下变换</li>
<li>右边乘变换矩阵，相当于在局部坐标系下变换</li>
</ul>
<p>用旋转矩阵表示旋转不会遇到歧义、死锁的问题，缺点是占用空间大，运算量大（欧拉角使用3个数，四元数使用4个数，而旋转矩阵使用了16个数），另外不太直观，不能直接把矩阵展现为物体的旋转角度，不过在这一点上来说，欧拉角也不怎么样，前面我们所说的unity骗局即欧拉角表示造成的。</p>
<h5 id="四元数表示"><a href="#四元数表示" class="headerlink" title="四元数表示"></a>四元数表示</h5>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>数理基础</tag>
      </tags>
  </entry>
</search>
